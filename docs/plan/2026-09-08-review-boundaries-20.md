# Review boundaries 20 — specification fuzzing

## Selection and authority

Skill version 0.8.1. N=1: story:fuzz-the-specification-surface, serving vision:O2.
The standing implementation and publication approval records authorize the opening design/store
commit, unit source commits and corrections, integration merges, closing evidence/store commit,
publication of the green wave to ESS main, and exact owned-worktree cleanup. The user's continued
instructions retain these grants. No numerical remaining model balance is exposed; none is invented.

Base: 9ab8a1b16dec7c08ba2dd2ac3b507f48254514f5, published on ESS main. Wave19's output ownership
passed main CI 34234834142 and docs validation 34234834128. All three owned wave19 trees, owned
scratch roots and branches are retired after evidence retention. Progress: 28/31 original stories
integrated; three originals plus execution-recovery implementation remain outstanding.

The current draft/proposed list, graph, blocked result and complete waves result were read before
selection. Nothing is blocked; no cycles or unassessed stories were returned. The complete
unfiltered computed waves, collisions and unassessed set are retained verbatim in the
[opening selection evidence](../reviews/2026-09-08-review-boundaries-20-opening-waves.md).
Root selects this single story because its finite binding has already had two reviews and its
measured Go repair has a bounded owner. Primitive semantics still needs its semantic/migration
decision; typed diagnostics and recovery implementation remain separate, larger obligations.
Other backlog stories are outside this review-remediation wave.

Scoping used aep-drive:story-scoper 0.8.1. Implementation and independent source attacks use
aep-drive:implementor and aep-drive:adversary 0.8.1 through the collaboration adapter with their
exact installed charters; this harness exposes no plugin subagent_type selector. Root owns all
AEP, Git, integration, shared metadata and cleanup. The six reservations remain 3 cited/3 inferred.

## Accepted contract

The [binding](../design/review-specification-fuzzing.md) retains the reviewed carrier, property,
finite budgets, actual callback requirement, observation admission, watchdog and mutation controls.
The two candidate review records remain historical evidence; this source refresh adds no third
design attack. Source and recovered input identities are in the
[opening receipt](../reviews/2026-09-08-review-boundaries-20-opening.json).

Stage one establishes the two separate offline lock graphs, exact compiler/runtime readiness
including an actual dated-nightly ordinary-ASan compile/link/run probe, and complete source-pinned
positive Go artifact maps/plans. No Go repair starts before all three prerequisites are evidenced.
Stage two establishes real failing verifiers, repairs the measured missing-owner panic, and completes
the stable/live harness and assigned package checks. Stage one alone never completes this unit.
Any further production defect receives a concrete owner decision; it is not silently absorbed.

## Managed work and resources

| Role | Managed id and branch | Build and scratch | Current stage |
|---|---|---|---|
| Coordinator | ess-review-boundaries-20; wave/review-boundaries-20 | Own target; target/review-boundaries-20/preparation | Opening design/store record and cheap gate |
| Implementor | Planned ess-specification-fuzzing-wave20; impl/fuzz-the-specification-surface | Own root target, standalone and engine targets; target/review-specification-fuzzing-wave20 | Not provisioned until opening checks pass |
| Adversary | Assigned after source handoff | Separate managed tree and task-owned records | Not dispatched |

Coordinator workspace: repository root.
Root lease: ess-review-boundaries-coordinator-wave20. Actual opening commit, provisioned unit,
scratch, TMP and compiler authorities are added before dispatch.

Fresh preflight measured 84,316,540,928 available SSD bytes, 24,538,263,552 tmpfs bytes and
43,074,191,360 memory-available bytes. The opening compiler target allowance is 2 GiB; the selected
unit gets 20 GiB total for its root/standalone/engine targets and scratch. All producer starts check
a hard 8,589,934,592-byte SSD floor and 8 GiB memory floor; stop owned producers on crossing a cap.
Only one owned compiler producer runs at a time, two build jobs, two test threads, debug 0,
incremental off. No shared target or CARGO_TARGET_DIR. Unrelated trees/processes are preserved.
Prior wave19 measured approximately 10 GiB total local target and consumer output at its peak;
its exact resource-stop incident is retained, not converted into a successful no-stop claim.
The larger current unit allowance accommodates separately locked stable and instrumented targets.

The retained complete Rust/Cargo 1.98.1 payload is selected for stable builds, after current hash
readback. Dated nightly-2026-07-28 and cargo-fuzz 0.13.2 require fresh stage-one full required-payload
readback and actual ASan readiness. Four wrapper variables are explicitly blank. Cached dependencies
are offline inputs, not a shared compiler output. Every command retains its direct status/output.
Root refreshes capacity before integration and records every task check lane plus site-build.

The wave completes at verified ESS main publication and owned-worktree cleanup. No release tag,
version bump or downstream Website/Atlas work is selected.
