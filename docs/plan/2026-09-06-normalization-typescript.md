# TypeScript normalization wave — 2026-09-06

Skill version 0.8.0. This continues the operator's authorized ESS work, owned commits and pushes: “publish validated checkpoint now, do ESS, publish updates”. The separately requested release and consumer adoption follow validated implementation. No new human approval or model budget is asserted.

## Selection

One unit: `story:typescript-normalization-target`, serving `vision:O2`. Its positional dependency is implemented and published in `b55efa4cd6c379217f60b98fb30f28f70e526ee6`; that checkpoint passed the 1,980-case workspace gate and site build. The refreshed computed output has seven sets, 67 collisions, no unassessed stories and no cycles; TypeScript appears in computed set 2. All three lists and the cycle list are retained verbatim in [the opening observation](../reviews/2026-09-06-normalization-typescript-opening-waves.md). Other candidates are separate review workstreams, outside this continued normalization unit.

The final scope has 31 paths: 11 cited existing paths and 20 inferred new files, independently checked against integration `1f8e319cf153c348a6c434c6e74939f4aa587125`. The published closing commit changed only planning evidence. The final source receipt contains 84 read hashes, and its exact delta body has SHA-256 `5669cc4b2fd108dae6b9b2c9b6ed9a20c20ce131e8d20bc19e6d642f0946eb11`. The binding design and governed story record it. The 19 new positional adversary vectors bring the planned applicable runtime inventory to 3,091; these are not execution results. `uniqueItems:true` remains an explicit located generation refusal, with a dedicated control.

## Ownership and protocol

The implementor owns the five existing source/Cargo paths and 20 new source/test/template paths. Root owns all six documentation paths, planning, Git operations, lifecycle operations and integration. The implementor submits exact proposed public-documentation patches in its assigned scratch, without applying them. Before shared source edits it captures all twelve complete Rust/Go format-1–6 maps at the actual 0.19.0 generator version and records baseline and observed red tests. No producer-version normalization is permitted for this same-version compatibility witness.

Generic harness agents load the `aep-drive:implementor` and `aep-drive:adversary` charters; the harness cannot select those plugin names as typed subagents, so the substitution is explicit. One implementor runs, followed by an independent adversary with at most two passes. Root records immutable findings before any correction and runs the full workspace and site gates on integration. Native TypeScript qualification is an additional explicit lane; ordinary default gates must retain their documented external-tool behavior.

The authorized commits are the opening planning record, unit source/tests, any reviewed correction, integration merges, common documentation, closing evidence and publication to main after gates. The final version bump and tag use the separately authorized release procedure; neither this wave page nor a generated binary version string proves a release.

## Resources and cleanup

Cargo/native execution is serial, four build jobs. Each tree has its own `target/`; `RUSTC_WRAPPER=/usr/bin/sccache`, incremental compilation is disabled and unit development/test debug information is zero. Node 22.23.1 and TypeScript 6.0.3 have been observed, and are requalified by implementation. No model budget was supplied; one implementor and one later adversary stay below the four-agent default. Token/cost telemetry is unavailable.

The preceding four completed unit heads are ancestors of published main. Wanted native verification scratch is archived and every file hash checked before clearing the three ignored Cargo targets. `worktree finish` initially refused an ignored target; after exact build-path review and `cargo clean`, all four finishes succeeded. Exact-ID managed GC removed the four reviewed IDs successfully and their paths were confirmed absent before the new unit is created. Other owners' worktrees are not cleanup candidates. The ongoing coordinator retains its published integration history and build cache.

Resource observation `2026-09-06T13:37:23.329200+00:00`: **23,370,809,344 bytes available**, **6,442,450,944 bytes** unit allowance, **8,589,934,592 bytes** minimum reserve. The preceding units measured 3,218,030,730, 2,452,718,216 and 2,477,226,176 bytes before cleanup.

## Dispatch record

Integration branch `impl/normalization-base64-resume` in manager record `wt-bf45625a6a50`. `$MANAGED` is the manager pool and `$SCRATCH` is the assigned private recovery root; exact machine paths remain in private receipts.

| Unit | Branch | Managed path | Build path | Scratch | Stage |
| --- | --- | --- | --- | --- | --- |
| TypeScript | impl/normalization-typescript | $MANAGED/b10x/ess/wt-1a25421922fc | $MANAGED/b10x/ess/wt-1a25421922fc/target | $SCRATCH/ess-typescript-design/implementation | Created at a2f02fc; baseline/red phase ready |

Opening commit `a2f02fceb01f9844c984f67fd251f94f7c6df94c` passed formatting, release consistency and all five layout cases before managed creation. Root holds the session lease. Implementor scope_typescript is assigned the sole Cargo/native lane; the exact private unit brief records environment, source scope and evidence paths.
