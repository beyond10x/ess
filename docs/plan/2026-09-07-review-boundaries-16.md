# ESS review boundaries — wave 16

**Skill version 0.8.0 — aep-drive:wave.** This is the continuing interactive run under the recorded
standing implementation, publication and exact owned cleanup approvals. The standing grant covers
one delivery unit commit, its required review/correction commits, coordinator opening/closing
records, integration merges and publication of green source to main. A release, version bump,
tag or real deployment remains a separate task. The four-agent ceiling applies; implementation N=1.

## Selection and source

Select story:review-delivery-trust-contract alone, serving O2 and closing the original F11 delivery
trust finding. The accepted B01–B07 binding is docs/design/review-delivery-trust.md, including every
T01–T18 variant. Release metadata retains its /1 consistency meaning and conservative unverified
qualifiers. The new positive conformance gate uses an explicitly supplied report/2, independent
expected selection, compiled model and delivery context; publication owns admitted bytes through
staging. Generic check logs remain generic checks. The action's required-input migration and the
three public pages are part of this unit.

Published source is 95ef5be70dbce966056d0484b66db7ed836fa406. Its runtime, tests, lockfiles and public
site bytes equal browser integration3408bbf049d10215487c50f6f7b5597486b14127. The first delivery
binding review is recorded as review-result:delivery-trust-binding-pass1 and found no concrete
issue, with no executed runtime cases. Root read the complete integrated refresh, binding, work
order and story; all74 inputs (1,825,961 bytes) and8 outputs matched their retained hashes. Only
browser Web help shifted relevant CLI citations; no delivery API or case family changed. No
second binding attack is added for these mechanical source/publication annotations.

Both declared prerequisites, persisted-delivery-validation and report-reader-validation, are
implemented. The fresh store has no blocked artifact. Original remediation progress is24 of31
implemented, with seven original drafts and the separately owed recovery implementation open.
Root has published and structurally validated its recovery model/binding; that does not implement
recovery or add it to this source unit.

The [complete computed draft waves](../reviews/2026-09-07-review-boundaries-16-opening-waves.md)
retain every wave, collision and unassessed/cycle list verbatim. After replacing all five old broad
delivery tokens with fourteen exact reservations, AEP returns4 waves,30 collisions and no
unassessed artifact or cycle. Delivery appears in computed wave3. The CLI compares literal tokens;
root additionally reads directory containment. In particular, a broad ess-cli reservation can
contain delivery's main.rs even when literal-token grouping places them together. Selecting N=1
avoids that overlap without claiming parallel safety from literal inequality.

Delivery has a current accepted finite contract and available offline executable/test owners.
Recovery's large implementation requires its separately recorded story/work order. Consumer
coverage needs its baseline checkpoint; authored discovery needs model/design adoption; fuzzing
needs tool readiness and its frozen baseline; primitive semantics and diagnostics retain their
specific contract/parser decisions; output ownership retains its ownership/durability decisions.
Those drafts are not silently assigned here. The review's other work remains in scope for later
replans, and no further objective is introduced by this selection.

## Preflight and concrete ownership

The continuing managed coordinator is wt-752828a285ba on wave/review-boundaries-16, created from
clean published95ef. Worktree doctor returned zero. The completed browser unit, its implementation
branch and all five assigned raw roots are retired with verified retained archives. The primary
checkout and unrelated managed trees remain preserved. Atlas authority is the clean managed
wt-53892beacf0d at remote-main d10b7484d64c28830774c9dae0ec531fcc47acb2; refresh authority before
bot publication. Connectors lacks the needed GitHub Actions operation, already reported before
using the Atlas App helper for CI.

Fresh free bytes: 73,332,178,944. Preserve the8,589,934,592-byte floor and reserve an additional
8,589,934,592-byte allowance for the unit target and its assigned TMP. The previous unit's default
target used1,328,743,251 logical bytes; full original Firefox temporary outputs were measured and
retired separately. Use one producer at a time, two Rust jobs, offline compilation, no incremental
or debug data. Check allocation/free space around every producer; retain refusals rather than
cleaning unrelated work to fit. Harness aggregate agent token/tool metrics are unavailable; record
actual exposed command durations and runner counts.

Planned managed ID: ess-delivery-trust-contract.
Planned branch: impl/review-delivery-trust-contract.
Planned worktree: /home/timo/.local/state/worktree/trees/b10x/ess/ess-delivery-trust-contract.
Default target: that worktree's target, with no target environment override.
Implementor scratch: target/review-boundaries-16/delivery-trust inside the unit.
Adversary scratch: target/review-boundaries-16/adversary-pass-1 inside the unit.
Private Cargo home: target/review-boundaries-16/cargo-home, with explicitly assigned read-only
registry/git inputs and private mutable bookkeeping.
Owned short TMP: /home/timo/.cache/ess-w16-delivery-tmp.
Lease: ess-review-delivery-trust-implementation. Root creates these after opening checks.

Frozen Rust1.98.1 authority remains the complete bin/lib/WASM snapshot with manifest SHA256
387c58849c86996b2aa20d63de19d6e4d925c91552682ed5eb49a1b0a1ce731d. Existing actual Firefox tests
use the complete installed153.0 payload manifest eaac01c1bafaf70d1392d46914f0fd69ff247f7bd9ad0895334add758543bd9f.
Root assigns exact read access, verifies before/after producers, and provides a filled launch
environment with all four Rust wrappers empty, default target and owned Cargo/TMP/XDG paths.
Actual Bash/jq and local file utilities remain real; the offline Rust fixture simulates only the
finite external release tools, refusing unexpected invocations. No real registry or deployment is
called by the assigned tests.

## Execution and closure

The collaboration adapter carries the complete aep-drive:implementor0.8.0 and separate
 aep-drive:adversary0.8.0 charters; it has no plugin-specific subagent_type selector. Root owns
AEP, Git, changelog, wave records, independent review routing, integration, publication and cleanup.
The unit confirms all fourteen scoped paths, writes meaningful failing cases before production
edits and implements every T01–T18 branch through actual ESS and the actual action. It preserves
canonical bytes, existing readers/cache controls, current browser help and namespace import.

Run affected-package suites, the specified conformance-reader controls, package formatting and
strict Clippy with individual direct exits and actual baseline/final counts. Return quiescent with
sealed source, report and raw process/test evidence. Root records and routes at most two independent
source attacks. Green unit integration is followed by every current task-check lane and site-build
on the exact source; no skipped lane becomes an executed pass. Root records observed evidence,
confirms scope and moves the story through AEP, then publishes verified source incrementally.
Public documentation changes require source publication first, deterministic Website source-lock
refresh, Atlas snapshot and both delivery gates. This wave includes those public changes.

Retain and independently verify the exact assigned untracked evidence before removal. Root ends
only its owned lease, finishes the managed unit, reviews every fresh GC assessment and applies
only its exact reviewed ID, then deletes its published merged branch without force. The next
wave starts from fresh graph/scope/readiness observations.

## Recovery obligation recorded before dispatch

Root created story:review-execution-recovery-implementation in draft after the exact model and
consolidated binding were published at95ef. It delivers the already-open obligation of that name,
decomposes the existing remediation epic, serves O2 and depends only on the implemented recovery
design. This is the separately owed implementation, with no new review objective or completion
claim. All34 accepted paths are registered:21 cited and13 inferred; only the two now-published
model paths changed confidence from the historical scope.

Root read the complete refreshed body/report and independently verified297 textual inputs
(34,241,947 bytes) and five outputs. All12 acceptance items, R01–R29 indexed families, supplemental
dimensions and the offline shared-production-driver qualification remain unchanged. The retained
story body SHA256 is4c3c46c0b15b2ae79dec7fcab248091e68ee3f817fd100666bee15c225bdf468. Fresh actual
published-path validation and compilation each returned zero, with the same36,251-byte compiled
result SHA25640a9fb6e2ac3c67d27e87b6c935422ada17490637a93d3517e9c9dc28cc7cd20. These establish
declaration validity only. No recovery code, authority provisioning or runtime result is claimed.

Before creation root refreshed the graph and confirmed the ID absent and all four relation
targets present in their recorded states. After creation AEP validated200 artifacts with the same
41 previously relayed advisories and returned zero. The [complete post-creation draft lists](../reviews/2026-09-07-review-boundaries-16-recovery-waves.md)
are retained verbatim. Delivery is already active and is therefore absent from that draft-only
query; recovery remains draft and unselected. Their main.rs, command_surface.rs and two public
page reservations overlap, so no parallel recovery unit is dispatched. Original review progress
remains24 of31 complete; delivery is active, six originals remain draft, and recovery is separately
draft. Root records all these facts before provisioning the one selected delivery worktree.
