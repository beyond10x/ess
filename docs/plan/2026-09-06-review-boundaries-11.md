# ESS review boundaries — wave 11

**Skill version 0.8.0 — aep-drive:wave.** Standing approval records
`review-remediation-standing-implementation` and `review-remediation-standing-publication`
cover implementation, green source publication and exact task-owned retirement. This is the
continuing approved remediation loop. No release, tag, version bump, default switch or installed
binary replacement is selected. Other sessions' published work and dirty primaries are preserved.

## Fresh selection

Select `story:review-conformance-coverage`, serving `vision:O2`, alone. Its three declared
prerequisites are implemented. The fresh store has 24 draft stories, no proposed story and no
blocked artifact; 13 drafts belong to the 31-story remediation epic, of which 18 are implemented.
The [complete computation](../reviews/2026-09-06-review-boundaries-11-opening-waves.md)
retains all seven waves, all 62 collisions, the empty unassessed and cycle lists, and validation
output verbatim. The selected story is alone in computed wave 4. The CLI matches literal scope
tokens; it does not prove directory containment or cross-session isolation.

The refreshed independent scope report at source `239996d846460aee342ce42514378c25b2be5152`
has SHA256 `74f2a32a05c920f411bc10c0d17f7e0b5ccaf31a2a25aafcdda0a8c770acd74a`.
All 47 prior source inputs were checked; the later closing commits change internal records only.
Root replaced the historical Scope through AEP and upgraded transport authority to cited.
The nine machine tokens are eight cited paths and the retained inferred `website/docs` token.
The accepted 75-row matrix, P1–P10 and transport binding govern the work; no new contract is
inferred from the scoper's proposed private names.

Coverage now has its reader-first prerequisite: AEP source `658cf76e6371b1628f6de69548e724b52803f5c2`
passed exact CI, bundle and documentation runs and joint publication `34044223213`. Its closing
records are published at `70ec336c00fa67b3d83f5c05bfbe71189462c01e`. The compiled compatibility
helper matches all 327 published compiled inputs and remains unexecuted. Actual frozen Rust/Go
producer correspondence through both AEP readers, checked mutation and replay is required before
ESS writer publication. Atlas ADR 0040 remains the cross-repository migration authority.

The writer spans conformance, CLI and impact and must prove actual generated Go and browser
behavior. N=1 reserves capacity for independent source attack and producer verification. Other
remediation drafts remain for later fresh selection; no unrelated feature or release story is
selected. Browser pairing here preserves the declared reduced-model limitations and does not
close the separate browser-fidelity story.

## Measured preflight and ownership

The managed coordinator starts clean on `wave/review-boundaries-8`, at published
`1c7b38bc98711fc8a80434de4429ac15a5e23191`. The primary ESS checkout is clean at `c4f3d14`,
on main, and is not used as the branch source. The dirty primary Atlas status is retained in the
complete opening record; none of its organization-brain paths intersects this ESS unit.
The managed Atlas authority is clean at exact remote `78d96cc706f735c3a160369e3ce2f2133f4b1d41`.
Manager/Git inventories retain the other sessions' worktrees without mutation.

The unchanged production source's measured full test lane passed 2,018 cases in 143.793475881
seconds. This is a retained-build measurement, not a cold-build claim. The continuing ESS target
occupies 12,549,611,520 allocated bytes; fresh free disk is 34,921,013,248 bytes against the
8,589,934,592-byte floor. N=1 uses its own target and two build jobs with debug and incremental
disabled. The existing direct-compilation cache exception continues; installed sccache being
present does not change that prior session decision. Recheck the floor before provisioning and
on returned units. Four total agent slots remain the established budget, with no model override.

The prior schema, AEP reader and joint Website unit paths are absent; their sealed archives have
independent readback. Continuing coordinators and Atlas authority trees retain next-step records.
No old task unit or its assigned build directory is carried into this wave.

Planned unit ID: `ess-conformance-coverage-writer`.
Planned branch: `impl/review-conformance-coverage`.
Planned worktree: `/home/timo/.local/state/worktree/trees/b10x/ess/ess-conformance-coverage-writer`.
Build: the unit's `target` directory. Scratch: `target/review-boundaries-11/coverage-writer`
inside that unit; TMP and Cargo home remain beneath scratch. The manager's creation result must
match this triple before dispatch. A separate coordinator-owned producer preparation directory
is `target/review-boundaries-11/producer-verification` in the continuing ESS coordinator.

The implementor owns source/tests inside `ess-conformance`, `ess-cli` and `ess-diff`, plus the
internal format catalog, conformance guide and public format reference. Root owns the two
accepted bindings, changelog, store, wave/review records, Git, full gates and publication.
No manifest, lockfile, Taskfile, normalization behavior or fourth production package is assigned.
Concrete expansion requests return an unapplied patch and measured reason to the coordinator.

## Execution and evidence

Use `aep-drive:implementor` 0.8.0 and a different `aep-drive:adversary` 0.8.0, each reading its
installed charter. Native plugin role selectors are unavailable; existing collaboration threads
continue the recorded harness-adapter deviation. Maximum two source attacks. Root serializes all
store/Git/lifecycle mutations. Package suites, strict all-target Clippy, formatting and actual
Go/browser controls belong to the unit; all declared integration lanes and site-build run on the
integrated source afterward. Each command retains its own exit and actual executed-case counts.

The independent producer preparation fixes semantic expectations before inspecting new writer
reports. Transport hashes can be pinned only after actual frozen producers execute. The AEP
helper is neither a producer nor proof of inventory honesty. Its real correspondence checks,
the selected actual producer receipts and the complete accepted matrix must be accounted for
before source publication. No hand-built report is described as an actual Rust/Go export.

Standing approval authorizes this opening commit, the unit and necessary correction/review-test
commits, integration merge, closing evidence/store commits, green main publication and exact
owned-id cleanup. It supplies no missing gate result. Opening metadata receives the cheap checks
before a unit is created. The wave page and file brief are updated at each handoff.

## Current stage

Scope refreshed; selection and resources recorded. Opening lifecycle moves and checks are next.
No writer implementation, producer correspondence, source attack or wave 11 full gate is claimed.
