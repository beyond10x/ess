# ESS wave 26 — proposal

Skill `aep-drive:wave` **0.9.1**. Dispatches would use `aep-drive:implementor` and
`aep-drive:adversary`, both with their plugin prefix. Adversary budget 2 passes per unit.

**Stage 1 only. Nothing is dispatched and no branch exists.** Wave 25 has not closed — its unit 2 is
in correction round 2 — so this page is written now and the pre-flight below refuses the wave until
it does.

## Selection

`aep plan artifact waves --kind story --status draft --format json`, exit 0, after this coordinator
wrote typed scope entries for three of the four stories the verb returned as unassessed.

### The verb's three lists, verbatim

| set | members (`I:` = inferred scope) |
|---|---|
| 1 | `a-browser-that-answered-http-once-is-still-a-slow-start`, `a-field-constrained-by-a-charset-publishes-that-charset`, `a-killed-childs-outcome-says-which-signal-ended-it`, `a-refusal-records-the-document-it-was-read-from`, `I:create-only-command-cannot-refuse`, `I:crosswalk-verb-external-names-held-to-declarations`, `native-realization-ci`, `one-name-held-by-two-kinds-is-refused-whether-or-not-it-converts`, `release-status-publication-state`, `I:scrub-the-planning-store-or-say-why-not`, `I:the-design-page-is-held-to-the-fixture-it-describes` |
| 2 | `a-marked-region-is-not-a-scan-of-what-runs`, `a-wrong-trailing-key-guess-is-reported-as-a-line`, `integrate-source-driven-realizations`, `java-conformance-target`, `I:normalization-equality-eligibility`, `the-lane-does-not-pin-a-count-that-its-own-bookkeeping-moves`, `I:the-metadata-guard-rejects-every-build-but-one` |
| 3 | `I:outcome-decided-by-environment`, `I:the-browser-fixture-abandons-a-profile-per-start`, `I:the-generated-go-runtime-is-gofmt-clean`, `the-unread-tree-bullet-is-read-whole` |
| 4 | `the-startup-clamp-does-not-outlive-the-startup` |
| 5 | `the-startup-lock-does-not-cover-the-first-round-trip` |
| collisions excluded | **26** |
| unassessed | **1** — `story:deleting-a-scratch-tmpdir-breaks-sccache-for-every-other-agent` |

The one remaining unassessed story is left unassessed **deliberately and is named as such**: its
surfaces are a brief template that exists as prose in a session and `~/.claude/CLAUDE.md`, neither of
which is a file in this repository. It cannot be a wave unit until somebody decides where that rule
lives.

## The units

| # | Story | Scope | Serves |
|---|---|---|---|
| 1 | `story:the-startup-clamp-does-not-outlive-the-startup` | `ess-cli/tests/support/browser.rs` | `vision:O2` |
| 2 | `story:the-unread-tree-bullet-is-read-whole` | `ess-xtask/tests/host_paths.rs`, `host_paths_lane/mod.rs` | `vision:O2` |
| 3 | `story:a-killed-childs-outcome-says-which-signal-ended-it` | `ess-cli/src/recovery/process.rs`, `ess-cli/tests/execution_recovery.rs` | `vision:O2` |
| 4 | `story:the-metadata-guard-rejects-every-build-but-one` | `ess-xtask/src/consumer_coverage/metadata_tests.rs`, `Taskfile.yml` | `vision:O2` |
| 5 | `story:integrate-source-driven-realizations` | `ess-cli/src/main.rs`, `ess-synth/tests/feasibility.rs`, `CHANGELOG.md`, 3 docs | `vision:O2` |

Scope confidence: units 1–3 and 5 are **cited**; unit 4 is **inferred** and is the one weak line in
this proposal — see *What is weak* below.

Three of the five are defects this session's own adversaries measured, and two of those are
regressions with controls rather than opinions.

## Two things the verb could not decide, both reported rather than followed silently

**1. The verb said `java-conformance-target` is disjoint from the other four. It is not, and I
excluded it.** Its typed scope is the *directory* `crates/edge/ess-cli`, which contains
`tests/support/browser.rs` (unit 1), `src/recovery/process.rs` (unit 3) and `src/main.rs` (unit 5).
The verb compares exact path strings and does no containment, so a directory-granularity entry is
invisible to it as a collision.

The skill's rule is that the verb wins and the disagreement is reported. I am reporting it and **not
following it**, because this is not one agent's inference over prose — it is a containment fact
anybody can re-check, and the typed entry is the thing that is wrong. Deferring to the verb here
would put two agents in one directory.

**The correction belongs in the store**: `story:java-conformance-target` needs a file-granularity
scope before it can be selected. It is not in this wave.

**2. Five candidates are held by wave 25's uncommitted work**, which the verb also cannot see —
it reads the store, not the working tree.

| candidate | file held by |
|---|---|
| `a-field-constrained-by-a-charset-publishes-that-charset` | `component.rs`, `ess.schema.json` — wave 25 unit 3 |
| `a-refusal-records-the-document-it-was-read-from` | `resolve.rs` — unit 3 |
| `a-wrong-trailing-key-guess-is-reported-as-a-line` | `resolve.rs` — unit 3 |
| `one-name-held-by-two-kinds-is-refused-whether-or-not-it-converts` | `spec.rs` — unit 2 |
| `the-design-page-is-held-to-the-fixture-it-describes` | `typed_diagnostics.rs`, `review-typed-diagnostics.md` — unit 2 |

All five become selectable the moment wave 25 merges. Three of them are the direct follow-ups to
wave 25's own findings, so the next wave after this one has an obvious shape.

## What else was excluded, and on which property

| story | property it failed |
|---|---|
| `native-realization-ci` | **1, implementable tonight.** Its acceptance is "every pull request and release candidate executes…", which cannot be observed without a CI run |
| `release-status-publication-state` | **1.** Asks GitHub |
| `scrub-the-planning-store-or-say-why-not` | **1.** Store mutation, which the protocol reserves to the coordinator and does not give to an implementor |
| `a-refusal-records-the-document-it-was-read-from` | **3, blast radius.** 159 `ValidationError::new` sites across 53 files, measured. Also held by wave 25 |
| `a-browser-that-answered-http-once…`, `a-marked-region…`, `the-startup-lock…` | **2.** All on `browser.rs`, colliding with unit 1. One browser story per wave |
| `the-lane-does-not-pin-a-count…` | **2.** On `host_paths.rs`, colliding with unit 2 |
| `create-only-command-cannot-refuse`, `crosswalk-verb…`, `normalization-equality-eligibility`, `outcome-decided-by-environment`, `the-browser-fixture-abandons…`, `the-generated-go-runtime-is-gofmt-clean` | **inferred scope.** Unscoped is not the same as safe; they stay out until somebody scopes them |

## What is weak, stated before you approve it

- **Unit 4's scope is `inferred`, not cited.** I filed that story from an adversary's measurement of
  `metadata_tests.rs:253` and wrote the scope from the finding rather than from reading the tree. It
  is the one unit here whose disjointness rests on an inference. It is also the highest-value one —
  see below — which is exactly the combination worth flagging.
- **Unit 5 is a coordination story, not a defect.** "A coordinated landing, not another
  implementation of either workstream." It is the only unit whose acceptance is not a failing test
  somebody already wrote, and it touches six files across three areas. It is the likeliest to come
  back needing a decision rather than a fix.
- **N = 5 exceeds the skill's default of 4.** You asked for five. The default exists because six
  agents on the largest model returned HTTP 429 and four were killed mid-flight.

## Why unit 4 is worth a slot

`crates/edge/ess-xtask/src/consumer_coverage/metadata_tests.rs:253` fails under **any** build
environment but the exact `env` line in `Taskfile.yml`'s `test:` task. Measured three ways by the
wave-24 unit-2 adversary: plain `cargo test -p ess-xtask` fails on `DEBUG`; the profile knobs alone
fail on `NUM_JOBS`; the Taskfile's full line passes.

The cost is not the gate — `task check` uses the right environment. It is that **the per-crate
command every implementor brief in this session prescribes runs that lane red**, so an implementor
either reports a red it did not cause or learns to ignore a red. Both happened in wave 24.

## Pre-flight, and it currently refuses

| Check | Reading | Verdict |
|---|---|---|
| working tree clean, on base | `main` @ `ce583ce8`, 1 tracked change | **refuses** — see below |
| previous wave's trees gone | **2 still standing** — `wt-c2495d2e6f3a`, `wt-e0cb562e0319` | **refuses.** Wave 25 has not closed |
| free disk | **19G, 98% used** | **refuses.** Five cold worktrees at ~25G each is not available |
| model budget | not asked | **refuses.** N=5 must be sized against what is left |
| measured build | wave 25: implementor 134k–283k tokens, adversary 141k–176k, 11 agents | recorded |
| `AGENTS.md` read | yes, this session | pass |

The one tracked change is `crates/specify/ess-compiler/tests/locator_citations.rs` — wave 25 unit 2's
stale-`#[ignore]` patch, which is mine to apply and belongs to wave 25, not here.

**This wave cannot start until wave 25 closes and disk is reclaimed.** Both are in progress.

## Commits approval would authorise

Five unit commits, five merges into `wave/ess-wave-26`, one closing store commit, one merge of
`wave/ess-wave-26` into `main`. Nothing else — no push, no tag, no release. The grant closes when the
wave closes.
