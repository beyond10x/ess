# ESS review boundaries — wave 17

**Skill version 0.8.0 — aep-drive:wave.** This interactive continuation uses the recorded standing
implementation, publication and exact owned cleanup approvals. They cover the opening record,
one unit and necessary review/correction commits, integration merges, closing evidence/store
records and publication of green source to main. No release tag, version bump or live ESS
deployment is authorized. Implementation N=1.

## Selection

Select story:review-authored-discovery, serving O2, from published
603dd90f855e994ce41aeab4788ea72f4fa01dbe. Its only difference from published delivery-trust
4c941557f4bdc894b4e61ec03dbb0f9972df0bae is the reviewed AGENTS.md release block.
The continuing managed coordinator fast-forwarded without modifying the primary checkout.

The [accepted binding](../design/review-authored-discovery.md) introduces optional immediate-directory
ess-inputs.yaml with exact specification/scenario lists. It retains legacy layouts, direct-file
selection, omitted scenarios and the precise committed-suite bypasses. All A1–A24 families are
required. Manifest mode performs no walk or content classification; the reader validates the
whole configuration before accessing only the active list, preserves original source bytes and
identities, and refuses discovery failures before effects.

The prior candidate received review-result:authored-discovery-binding-pass1 with no findings.
The current independent scoper identified two added acquisition callers, observed bindings and
qualified delivery, which require additive A19/A21 coverage. Nine reservations cover the CLI
package, binding, two model files, two guides, CLI/formats references and engineering format
catalog. No new library, registry, specification-schema or dependency owner is selected.
Root read the report and independently verified ten current source/Git pins.

Both named model files under docs/design/models/authored-discovery/ validated and compiled with
the retained current-source CLI: direct exits zero, `discovery v1 — 2 file(s), valid`, and 2,192
compiled bytes. This establishes declaration validity only; all runtime reader obligations remain.

The [complete computed draft waves](../reviews/2026-09-07-review-boundaries-17-opening-waves.md)
retain four waves, 34 collisions and empty unassessed/cycle lists verbatim after scope registration.
The selected story appears in wave 3. Root also accounts for directory containment, which the
literal-token comparison does not compute. Selecting one unit avoids overlapping writers.

The graph has no blocked artifact; scenarios-directory-compiles-nothing is implemented. Progress
remains 25 of 31 original stories published: this story is active, five originals remain draft,
and execution recovery remains a separate draft obligation. Consumer coverage needs its measured
baseline checkpoint; output ownership its ownership/durability decision; primitive semantics and
diagnostics their contract/parser decisions; fuzzing its frozen baseline and tool readiness.
Recovery retains its accepted separate model and runtime work order. None is included here.

## Preflight and ownership

Coordinator: managed wt-752828a285ba, branch wave/review-boundaries-17. Its existing target retains
evidence and frozen tools. The prior delivery unit, merged branch and four assigned temporary roots
are retired with verified archives. Unrelated trees remain untouched.

The operator instructed, “please forget about atlas.” Atlas follow-up is removed from this
continuation. Unfinished Website local commits, task and evidence remain preserved, without a
claim of completed Website source publication or cleanup. ESS source/site gates and incremental
publication continue through the required existing bot tooling. No Atlas refresh, build or
reconciliation is assigned.

| Planned unit record | Value |
|---|---|
| Managed id | ess-authored-discovery-wave17 |
| Branch | impl/review-authored-discovery-wave17 |
| Worktree | ~/.local/state/worktree/trees/b10x/ess/ess-authored-discovery-wave17 |
| Build | Worktree's default target directory |
| Scratch | target/review-boundaries-17/authored-discovery inside the unit |
| Cargo bookkeeping | target/review-boundaries-17/cargo-home inside the unit |
| Assigned temporary root | ~/.cache/ess-w17-discovery-tmp |
| Lease | ess-review-authored-discovery-wave17 |
| Stage | Integrated gate complete; closing record and publication in progress |

Initial free space was 23,472,275,456 bytes; later preflight measured 20,236,275,712 bytes. Use one
producer, two Rust jobs, a 10 GiB combined unit target/TMP allowance and an 8 GiB free floor. This
narrows the initially considered 12 GiB allowance before dispatch. Recheck capacity before
provisioning and during producers. Previous actual unit measurements remain in wave 16; aggregate
agent token/tool metrics are not exposed and are not invented.

Root reverified all 378 frozen Rust 1.98.1 bin/lib entries, 1,392,727,899 bytes. Verify the full
Firefox payload before dispatch. The filled launch environment assigns private Cargo/Go/XDG/TMP
paths, offline operation, default target, two jobs, disabled incremental/debug output and all four
Rust wrappers empty. Preserve HOME. No floating installation or shared target is assigned.

## Execution and closure

The collaboration adapter carries complete aep-drive:implementor 0.8.0 and separate
aep-drive:adversary 0.8.0 charters; this harness has no plugin-specific subagent_type selector.
Root owns all AEP, Git, changelog, shared records, routing, integration, publication and cleanup.

Write and observe meaningful failing cases first. Run complete ess-cli baseline/final tests,
formatting and strict Clippy, preserving actual delegated Rust/Go and Firefox executions. Report
every A1–A24 family, reached boundary, raw output, direct exit and executed-case count. Root records
and routes at most two source attacks, integrates green source, then runs every current repository
gate lane and site-build. Learned scope and actual completion evidence go through AEP.

Publish verified source incrementally. Read, retain and independently verify native evidence before
retirement. End only owned leases, finish through worktree, read every fresh GC assessment and
apply only the exact owned id. Delete only the published merged unit branch without force, then
replan from fresh source and store observations.

## Dispatch observed

Opening commit b20f0da03986c00bdc79ab1397b4ad44a064f7ed is clean and has both required bot
identities. Its eight files are planning, design and model records; source/runtime/public-site
bytes remain unchanged. All four opening checks returned zero: formatting, action validation,
site-build and diff checking. Site-build took 25.771948 seconds and exercised the real Rust/WASM
lab with 21 claims and 28 deterministic steps. Frozen Rust source remained unchanged.

Before provisioning, root retained and independently verified both completed wave-16 gate
temporary roots, then removed only those exact roots. Their complete archives are SHA256
ad524db320a5a82a0bc704d12e547aecd80ffe4a95fd03e133a60101c0c0b85e and
ea96be8e1c5356eb97b047627e65e4077ce4e8a7fdef8f693111ea9ae47dcba4.
Every regular payload, native metadata record and literal link was verified; eight quiescent
socket nodes retain metadata only. The managed coordinator and its gate records remain present.

Managed create, branch creation and the assigned lease start each returned zero. The unit
ess-authored-discovery-wave17 is at b20f0da on its planned implementation branch, with the exact
worktree/build/scratch/TMP triple above. At 2026-09-07T18:06:57Z free space was 24,989,712,384 bytes;
the assigned allowance remains 10,737,418,240 bytes with an 8,589,934,592-byte free floor.
The complete native Firefox inventory was also reverified: 50 entries, 304,148,344 bytes.

The authored_discovery_implementor agent received the complete filled work order. Its SHA256 is
cb97b1913f7ae776166ebdb10bece34f9ba18f75a1ff52568a168ed99c6539c8; binding SHA256
ee12f6228174f672e68a9bca9e659ffc8affb78e36e9a42d923715cb087d0092; active story SHA256
3a9bf409ddc7356f65c1f5b424f09bf6a83ba06afbd5eb7380f411050bbb4414; environment SHA256
6b9d43e4837dcbfab10e145b1aad6333f2bff7940da134b47ae0e120a34490b5.
The fresh dispatch graph confirms active discovery and the implemented prerequisite. Exact
receipts and all 1,187 source pins remain under target/review-boundaries-17/preparation and the
unit's assigned scratch. Implementation and source review remain in progress, not complete.

## Baseline and regression execution

The implementation baseline ran the full ess-cli package with the frozen launch environment:
315 passed across 41 native summaries, zero failed or ignored, direct exit zero in 267.520874
seconds. Root read the direct-child and completion receipts, verified both complete output hashes
and independently derived these counts from stdout. The completion receipt is SHA256
7181aed0da7ead855d139587b6cbf2000e35f730cb8882eab5d660bac126784d. Its final measured allocation was
3,387,129,856 bytes with 18,928,562,176 bytes free.

Before production edits, the first authored-discovery regression run executed 20 cases: one
passed and nineteen failed, direct exit 101. Separate current-caller runs retained further failing
cases for qualified release, normalization and offline observed bindings. These are expected
test-first results, not completion evidence. Root verified the recorded stdout/stderr hashes and
direct statuses; the implementor retains the complete fixtures and reached-boundary evidence.
The final implementation and independent source attack remain outstanding.

The coordinator also prepared the wave-17 integration runner in assigned scratch, without running
it. It retains all eleven current gate lanes and uses new wave-specific temporary, Cargo and Go
bookkeeping paths. Its SHA256 is 683cbb5b8e65df4c4159f3e8dd908d3e7b7d9d6af8808a0c306d55bc7790d76b;
the complete adaptation diff is retained beside it. Exact integrated source and resource checks
are still required before launch. The runner explicitly supplies ESS_DISCOVERY_OLD_CLI and pins
the immutable pre-manifest binary so the compatibility controls actually execute. No Atlas or
Website follow-up has resumed.

## Package result and retained executables

The final ess-cli package run returned zero in 209.729813 seconds: 340 cases across the same
41 native summaries, zero failed or ignored. All 25 added discovery cases executed. Root read
the direct-child/completion records, verified both complete output hashes and independently
derived the counts. Completion receipt SHA256:
596605e96ab2bfcb73bd9e5f2cbdfbba4097968c1e591a432a88a818b424e8a0.

Root verified 42 independent executable copies, 474,677,496 bytes, against both their recorded
hashes and the current source executable bytes. Native manifest SHA256:
ad4d60ee1c86be9abbf4900cf03eb5a1089744cd5777a61faa3762cf65bbf5b0. The actually executed final CLI
is 7bae801da9a71c6d677b34044ee39d1176c249900b669be12a0698ef6863efc8.

The original baseline CLI d8b1fd6db5438f3ac294f44f1e760358d247219e68b3259564c3c517e4ae2c25 was
replaced by implementation builds before a separate native copy was made. The recorded complete
48-executable search found no match. Its baseline source snapshots, output and process records
remain, but these executable bytes are not claimed retained. The immutable 94983b compatibility
control is a distinct executable and does not substitute for that missing baseline image.
Root's partial readback is retained in preparation/implementation-progress-readback.json; the
final report/seal, independent source review and integration gate remain outstanding.

## Sealed implementation and first source review

Implementation report SHA256 60dc069169f24be2009cdf0ecfdafcd7726a5044a01d7b89df7dd2000770fa76
and seal SHA256 e3db830a522dbcb9590579e44aecfb5c829dcd8aabeff5912d78d5ad3055b9ca are retained.
Root read the complete narrative and acceptance map, matched all 26 embedded lane outputs and
completion records to their native files, and verified all 111 seal file pins. Final package,
strict Clippy and formatter checks share identical source manifests. The final executable manifest
is 8c50babcfa73cb46d184117422ab9af1117a64ab59146b6622ad46bd7904b8bd after the implementor's last
readback; the previously verified 42 executable copies remain unchanged.

Root's separate native readback returned zero in 15.492898 seconds: all 117,690 entries matched,
including 89,830 regular files containing 7,147,526,231 bytes, 343 literal symlinks and 15 socket
metadata entries. Losslessly escaped non-UTF-8 paths remain intact. The actual 14 changed source
paths, 504,953 bytes, match the declared scope; the index and accepted story/binding/model bytes
remain unchanged. No producer remained at handoff. This result supports proceeding to source review;
the baseline executable limitation above remains explicit.

The first source reviewer receives target/review-boundaries-17/adversary-pass-1 in the same unit
and ~/.cache/ess-w17-discovery-adversary-1-tmp as its exclusive new temporary root. It has separate
Cargo/Go/XDG bookkeeping and the immutable old CLI explicitly supplied. The filled brief is SHA256
fb81921021f91c0d1d19c18690e6480bd08970fd2e0cc95bb676afa23893cb1b; environment SHA256
5dcb9612159165b4c2134c72fb6a365b24528303b725f433b74b66ae2d0de6e9; complete 1,189-file source
manifest SHA256 5c21e3c1f06edd4d8b15452ec2185676d24b25ba065d357f30216b56677dafb3.

This sequential review has a 12 GiB combined target/TMP ceiling, accounting for the measured
7.34 GB of retained implementation evidence and its separate caches and producer outputs. The
8 GiB free floor and one-producer/two-job limits remain. Free space after setup at
2026-09-07T19:08:03Z was 22,762,049,536 bytes. This is a new measured reviewer allowance, not a
retroactive change to the implementor's 10 GiB ceiling. No cleanup created that capacity.

Dispatch to authored_discovery_source_pass1 succeeded with the exact filled work order above.
The reviewer alone may add tests in the unit; source attack one is running and has no verdict yet.

The reviewer's initial turn was rejected by an automatic content filter as a possible cybersecurity
task before any review result. Root clarified the existing assignment as local YAML-reader
correctness and compatibility review, retaining its tests-only scope and complete work order.
The same reviewer resumed successfully and confirmed reading the work order, repository instructions
and implementation delta. The rejected turn is not a completed review pass. No verdict is claimed
while its independent regression cases are being prepared.

The reviewer added three independent CLI cases: named-field manifest admission, the documented
mixed root with default model path and omitted scenarios, and exact scalar-looking string filenames
with inactive-role type checks. Each first isolated execution returned zero, and the complete
authored targets returned zero. Root read the complete new test file.

The first full package attempt was interrupted by the resource observer: an ordinary model-types
fixture disappeared between directory enumeration and metadata read. The observer terminated Cargo;
its actual direct exit was -15 after 171.375220 seconds. This was neither a test verdict nor a capacity
breach: the last sample recorded 9,090,809,856 allocated bytes and 40,320,675,840 free. Original runner,
raw outputs and the correction diff remain. The correction tolerates vanished transient entries in
the live resource estimate and samples again after native retention; root read the complete diff.

The separate package-final rerun returned zero after 207.165715 seconds: 343 passed across 42 native
summaries, zero failed or ignored. All three independent cases executed in addition to the 340 handed
cases. Root read the actual direct and completion records, verified full output hashes and derived
the counts independently. Completion receipt SHA256:
fd7751328bb550f77536804cb10f1c14b40271deff3e427ec4509bfe0e26a33b.
The reviewer retained 43 final executable copies before further builds. Its final report, complete
root readback and review routing remain pending at this point.

The user deliberately interrupted the session after the reviewer reported Clippy zero in
22.988488 seconds and formatter zero in 0.264805 seconds. On resumption, no original agent session
remained. Root checked the actual unit/coordinator Git state, retained lane receipts and process
state before dispatching `authored_discovery_review_handoff` to finish only the existing pass's
report and seal. No tests, builds or source changes were repeated. This is continuation of pass 1,
not another source attack. The handoff follows the newly installed 0.8.1 charter and owns a separate
session lease; root retains its coordinator and unit leases.

Root independently verified all 1,189 handed source files and the exact one added reviewer test,
with unchanged index, plus all 43 final source/copy executable payloads, sizes, modes and independent
inodes: 482,558,464 bytes. The receipt is
preparation/adversary-pass1-root-readback/source-and-executable-readback.json. The current executable
manifest SHA256 is 6515360171a58ffb992a3dbaee35b95e81fa180f4c98f69bc77d5dc6548119fe.

The user's replacement workspace instructions explicitly make ordinary public documentation
delivery asynchronous after validated ESS source/manifest publication. No Website/Atlas delivery
work resumes here. Connectors 0.7.0 again reported a healthy local deployment and no admitted
GitHub Actions status operation; root relayed that capability gap before the existing bot-App
helper fallback. The updated Worktree inspection is retained; its coordinator storage observation
stopped at 250,000 entries and is a lower bound, not a complete or reclaimable size.

## Review routing and integration

First source review completed with no findings. Report SHA256
911515b2b3b5d28d09c9f21fa5cffceea238edc523ad1220e8028fe758bf5ed9 and seal SHA256
bf55f8e238c0dc0d0f0584faef47d67b8096c9970dc8c86f3bd8881e085ec8b5 remain in the unit's
adversary-pass-1 scratch. Root read the complete narrative, verified all 1,325 seal-file pins,
matched all nine embedded lane outputs/direct records and the complete outside-path listing to
their original files, and checked all 3,860 recorded PIDs and the sealer absent. The report's
interrupted-run phrase "217 passed summaries" describes 217 passing cases in an incomplete run;
only the later complete 343-case result is used for routing.

Root repeated the separate native reader without writing in the unit or either retained TMP.
Actual root session 64352 returned zero in 37.341054 seconds: the complete 196,244-entry census,
149,497 regular files containing 12,354,235,474 bytes, 602 literal symlinks and 29 socket metadata
entries matched. The ten self-exclusions were independently covered by the seal pins. No cleanup
or baseline reconstruction occurred. Receipts and reading extents are retained under
preparation/adversary-pass1-root-readback.

The complete report was recorded byte for byte as review-result:authored-discovery-source-pass1
before routing, followed by a no-op review outcome. All three AEP calls returned zero, including
validation of 204 artifacts. Its output was relayed verbatim; 43 existing-style empty-findings
advisories remain, including this report's explicit empty findings list. No new product finding or
follow-up story was created. This was one completed source pass; no second pass is required for
the no-findings result.

Unit commit 6701d88ce66bd63a1ae6307450d0c0589ea8b925 contains exactly the 15 verified source paths.
Both author and committer are b10x-bot[bot], and the unit is clean. This commit is local and awaits
the integrated repository/site gate and publication. The coordinator's pending metadata and
Unreleased changelog entry travel with that integration.

## Integrated gate and story closure

Reviewed unit 6701d88ce66bd63a1ae6307450d0c0589ea8b925 merged cleanly at
f82fbc67a4c1cfaef920ac94ca1449bf8d947575; the merge tree matched its preflight exactly.
Every introduced direct commit has the required bot author and committer. Root ran all eleven
current repository/site gate lanes on that clean merge, with individual direct statuses:

| Lane | Exit | Seconds | Passing Rust cases / summaries |
|---|---:|---:|---:|
| site-build | 0 | 29.042834 | 0 / 0 |
| fmt-check | 0 | 1.603639 | 0 / 0 |
| clippy | 0 | 41.351392 | 0 / 0 |
| test | 0 | 423.613815 | 2233 / 198 |
| doc-check | 0 | 19.124404 | 0 / 0 |
| example-check | 0 | 1.703664 | 0 / 0 |
| projection-check | 0 | 9.512717 | 0 / 0 |
| support-check | 0 | 2.204249 | 0 / 0 |
| release-check | 0 | 0.101745 | 0 / 0 |
| action-check | 0 | 0.101654 | 0 / 0 |
| planning | 0 | 38.545587 | 0 / 0 |

The complete gate finished at 2026-09-07T20:15:17.585456Z. Root observed session 99580 exit
zero and independently verified all eleven raw log hashes and direct-child records, the complete
1,191-file source manifest unchanged, and 2,233 passing Rust cases across 198 native summaries,
zero failed or ignored. Site-build executed the real WASM/browser lab (21 claims, 28 deterministic
steps, 64 rows) and built the static site. Existing npm dependency/install warnings remain in its
raw output; no dependency or Docs System upgrade is part of this wave.

Actual Rust, Go and control producer records are retained, together with independently verified
native copies of their executed CLI and test binary. The ten observed process PIDs were absent at
root readback. The CLI alias had changed in a later build; the runner retained the exact recorded
fb21c5c258f8c02093715d39c7aa4af13b20733920478fb8e9833b281b8c27bd executable from its matching
native deps path, never a reconstructed substitute. The immutable old-reader control was explicitly
supplied and pinned. Live-cluster tests remain outside this offline gate.

Records: target/review-boundaries-17/gate-f82fbc67a4c1-attempt1.
Completion SHA256 99fcf39445faf2fe8a0c4723b7fc76804dffa2694a4a5cb7e0faa75f0e5db3e4;
results SHA256 915337861a2d16d7a2baaf87fa645d9a1ddd1cd1e31b96cc93b9e2efbd85045d.
Root independent readback SHA256 f4de5a168e2da23b33cbf21e3d89adf7134a72dc72ca4ceda41a508df33d2622
at preparation/integration-gate-root-readback.json. Aggregate agent tokens and tool-use counts
are unavailable in this harness; they are not estimated. Individual native durations are retained.

The closing story body confirms all nine actual reservations, preserving the original cited or
inferred provenance and the baseline executable limitation. Six inferred typed scope entries are
now cited. AEP rejected the fractional evidence timestamp, so the successful record uses the same
observed finish time at whole-second precision; the full native timestamp is unchanged. Completion
evidence names the exact gated merge. Story closure and validation receipts are retained in
preparation/closure. The only changes after the gate are this wave record and AEP planning metadata.

Source publication and exact managed-unit retirement remain the next operations. The unit target
and both assigned temporary roots are still present. Three prepared complete native archives and
separate readbacks are required before removing those owned disposable roots. The coordinator is
retained for the remaining remediation work. No release tag or downstream delivery is claimed.
