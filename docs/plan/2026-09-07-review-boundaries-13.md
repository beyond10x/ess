# ESS review boundaries — wave 13

**Skill version 0.8.0 — aep-drive:wave.** The interactive standing implementation and publication
approvals (`review-remediation-standing-implementation` and
`review-remediation-standing-publication`) cover selection, required opening/unit/review/closing
commits, integration, green main publication and exact owned cleanup. The four-agent ceiling
persists from the approved continuation. No new approval or non-interactive bypass is inferred.

## Selection and accepted boundary

Select `story:review-glossary-boundaries` alone (N=1). It serves O2 and closes F12's ambiguity
between logical components, interfaces, delivery descriptors, composition aliases, stack members,
runtime requirements/mappings and physical realization. Its complete acceptance and validated
scope name existing owners and supported limitations; no new typed entity or format is introduced.

The freshly read store contains 22 draft stories and no proposed or blocked artifact. Eleven
of those drafts belong to the 31-story remediation epic, with 20 implemented. The draft-only
scope computation has five waves and 49 collisions, with no unassessed artifacts or cycles.
The glossary story is in computed wave 2. The [complete CLI output](../reviews/2026-09-07-review-boundaries-13-opening-waves.md)
retains every wave, collision and empty list verbatim. Root also checks concrete directory
containment because machine scope tokens are literal. No other implementation is selected.

The independent refresh at `dbe78c5` verified 32 repository blobs and five local inputs;
report SHA256 `45974a2d7c601baf1d01324c17f0aed8a528b0a6494534756d16f1bd51cb16fa`.
All model/projector/example owners are unchanged from the preceding scope. The fourth reservation
repairs the earlier omission of the realization guide's broad interface statement. Candidate v2,
SHA256 `c104139cc76790960c049a5e889f24d1981464aaabb7c170eca4c77877a5f1b5`, is accepted as
`docs/design/review-concept-boundaries.md`, with only acceptance/source-status changes.

The implementation owns three public files: `website/docs/concepts/ess.md`,
`website/docs/guides/write-a-specification.md` and `website/docs/guides/record-realization.md`.
Root owns the binding, planning, changelog, internal records, source/public delivery and lifecycle.
Code, examples and model declarations are read-only owners. Scope expansions return an unapplied
patch and request to root. The source-review subject includes every owned public page.

The binding keeps semantic reach and CLI grammar in authored/compiled model identity, and
physical invocation, attachment and support in realization descriptions. One logical component
still has one semantic reach; multiple entrypoint records do not add simultaneous CLI/HTTP
synthesis. The reference walks CLI, HTTP, composition and delivery examples without implying
runtime execution, authorization or supported transport beyond actual source evidence.

## Preflight and owned paths

The continuing managed ESS coordinator is clean at published
`3464ea639258be53355f4f738c766efad2b6a3ba` on `wave/review-boundaries-8`.
Its implementation and public source are unchanged from gated `dbe78c5`. Primary ESS remains
clean on main at `e5e4fdf899b8afb59c59b6182a8f1e47115569a6` and is preserved. All three cache-wave
unit/support trees, their assigned temporary roots and the merged implementation branch are
absent after verified retention and managed retirement. No `impl/*` branch remains.

Preflight measured 18,317,348,864 free bytes against the 8,589,934,592-byte floor.
The previous full integration test lane measured 2,093 cases in 339.464207763 seconds. The
continuing coordinator retains that build and the fully copied, verified Rust 1.98.1 toolchain
including WASM std. Its exact tool snapshot is reused read-only. The prior Website support
measured 1,089,074,969 logical output bytes including dependencies and immutable artifact;
normal public delivery remains budgeted. N=1 leaves room for the independent document attack,
coordinator gates and Website support. Read-only diagnostics scoping is preparation, not another
implementation unit, and creates no build or source reservation in this wave.

Planned managed ID: `ess-glossary-boundaries`.
Planned branch: `impl/review-glossary-boundaries`.
Planned worktree: `/home/timo/.local/state/worktree/trees/b10x/ess/ess-glossary-boundaries`.
Build directory: that tree's `target`, with no unit compiler build selected.
Implementor scratch: `target/review-boundaries-13/glossary` within that tree.
Adversary scratch: `target/review-boundaries-13/adversary-pass-1` within that tree.
No external unit scratch is assigned. Root provisions the exact opening commit only after cheap
opening checks pass. Every later delivery-support tree is recorded before creation.

## Execution and evidence

The `aep-drive:implementor` and separate `aep-drive:adversary` 0.8.0 charters apply through the
existing collaboration adapter. The developer instruction against tests that mirror reversible
prose changes takes precedence over generic red/green instructions: this unit adds no phrase
search test or fabricated assertion failure. The implementor retains original paragraphs,
complete source citations and changed-file/scratch inventories, and runs real diff/link checks.
The independent document attack checks all terms, examples, identity boundaries and public links.
It makes no production edits and returns a findings block even if empty. At most two attacks.

Root reads the returned evidence, commits only owned source and green review additions, merges
into the coordinator, and runs each declared full integration lane with its own direct status.
The frozen complete toolchain and a short owned TMPDIR prevent the prior environment failures.
After green source publication, exact source-set, immutable artifact, Website gate and live bytes
must cover all three changed public pages plus production provenance. Completion is recorded
only against those results, then every assigned unit/support root is retained and retired through
the managed lifecycle. No release, tag, version, installation, default switch or real deployment
is part of this wave.

## Opening and dispatch

The checked opening commit is `38cfefc07ae27ccc5e9b51ca2159b5015fab1f50`, with both
direct commit identities verified as the organization bot. Its formatting, action contract,
site build and diff checks each exited zero; the site lane took 14.815651829 seconds.
The complete raw commands, outputs and source inventory remain under
`target/review-boundaries-13/preparation/opening/checks`. These are opening checks, not the
integrated glossary gate.

The manager created `ess-glossary-boundaries` at that exact opening commit. Root created
`impl/review-glossary-boundaries`, started lease `ess-review-glossary-implementation` and dispatched
the existing implementor with the file `target/review-boundaries-13/glossary/brief.md` in its unit.
The accepted binding hash is
`271910e53562be0e018db7b39fa9d52f2aee7333201d6bf437f7793cd7a563cd`.
The brief corrects an earlier scratch draft's inaccurate “published opening” wording: the
opening is local; its parent `3464ea6` is published. Provisioning receipts and exact copied input
hashes remain under coordinator `target/review-boundaries-13/preparation/provision`.

The prepared full gate assigns the short external temporary root
`/home/timo/.cache/ess-w13-gate-attempt1-tmp`, to be created only when the reviewed integrated
source is ready. Its output will be `target/review-boundaries-13/gate-<commit-prefix>-attempt1`
within the coordinator. It reuses the complete frozen toolchain read-only, validates its entire
manifest around each lane and retains the direct exit status before later checks. Preparation
measured 16,990,343,168 free bytes; the 8,589,934,592-byte floor applies before every lane.

Public delivery support is assigned managed ID `ess-wave13-website-verification`, at
`/home/timo/.local/state/worktree/trees/b10x/website/ess-wave13-website-verification`, detached
at the current Atlas-controlled Website runtime `fc4571534765c098ed861bc326da4d3da0d1df63`.
Build and scratch remain under that tree's `target/review-boundaries-13`; npm dependencies
and ordinary Website build outputs are owned within the same tree. The delivery runner assigns
`/home/timo/.cache/ess-w13-website-tmp` for its browser processes. No Website source edit is
selected. Source-set and artifact verification start only after green ESS source publication.
The corresponding Atlas fence records belong under
`/home/timo/.local/state/worktree/trees/b10x/atlas/wt-53892beacf0d/target/ess-review-wave13-delivery-fence`.
They must report any unrelated organization-wide baseline failures separately from the exact
source-set, Website gate and live Pages results.

## Reviewed source and integration

The clean implementation commit is `c99776eb14173b467143525947cd0933b983b459` on
`impl/review-glossary-boundaries`, based on the exact opening `38cfefc`. Both author and
committer were verified as `b10x-bot[bot]`. It changes only the three assigned public pages:
123 insertions and 10 deletions. Root read the complete implementor report and diff, then
verified the 71-entry native scratch inventory, 36 before and 47 final input snapshots,
12 real command receipts and 20 local link checks. The report is SHA256
`7a3d758cf5e47267ec0545e5ce3c5020c7f690e75945f1ea6df52ac958dd9dbb`.

The separate source document attack returned pass 1 with an empty findings block. Its complete
report is recorded as `review-result:glossary-source-pass1`, SHA256
`035d6d1a4fcfe077dc9d03dd929be245e16358c36f4806583638d2c40198db59`.
After the adversary relinquished all writes, root independently checked all 255 scratch census
entries, 252 payload entries, 50 exact inputs, 80 command receipts and two final HEAD/status
receipts. File bytes and native metadata match, excluding observational access times. The
three-dot diff from the opening to the reviewed commit equals the recorded review diff,
SHA256 `5a55232eaa95d53bece7bdd792d25113331b8b2954bbe932c6caa22e7784d892`.
Readback evidence remains under
`target/review-boundaries-13/preparation/source-review-pass1-readback` in the coordinator.
The attack covers all 13 original preferred-name rows and six example families. Its 20 link
checks establish exact local source targets; rendered and live publication remain separate gates.

The unchanged Markdown-only site-output claim is existing F16 work already owned by
`story:review-public-support-claims`. It is outside this glossary diff and creates no new story.
The previously inferred binding reservation is now cited: the accepted binding exists at the
opening commit. No review findings require outcomes. Root's changelog and this execution record
accompany the immutable review addition before integration. The subsequent full integration
gate must bind the exact integrated commit and the three reviewed public source hashes.
