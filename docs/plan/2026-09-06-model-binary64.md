# Binary64 normalization wave — 2026-09-06

Skill version0.8.0. Interactive continuation of the original authorized ESS gap
closure; no new human approval or answer to an optional question is invented.
The operator has explicitly requested commit/push of owned results, subsequent
managed cleanup and a personal status DM. This unit remains within the original
scope; release work follows its separately authorized final procedure and gates.

## Selected unit and resource plan

One story: story:model-binary64-fields, serving vision:O2. Raw JSON capture is now
implemented and published at6c78676. The type/normalization unit overlaps other
normalization work, so TypeScript and positional decoding remain later units.
Remaining review-remediation drafts belong to a separate ongoing workstream.
No missing credential or external mutation is required for this implementation.

The standalone primitive crosses domain/compiler/generation/conformance packages;
that scope is necessary to avoid successful unsupported consumers, and is larger
than the skill's preferred one-package first wave. The cited/inferred entries and
binding were refreshed against the complete current source before dispatch.
The binding is docs/design/model-binary64.md. Its authored predicate Number policy
is distinct from strict nominal assignment and normalization5 Binary64 equality.

Managed unitwt-9d0ef745088a, branch impl/normalization-binary64, starts at
6c78676c35193423fe326b9dde21b8fc21681b8a. It owns its own target/ build directory.
Integration remainswt-bf45625a6a50, branch impl/normalization-base64-resume.
Each exact path is obtained from the manager record; scratch is the assigned
recovery root's ess-binary64-design folder. The prior cross-tree cache reuse is
not continued. The new unit sets CARGO_INCREMENTAL=0 and development/test debug
information to0 to bound disposable build size, with four build jobs and serial
Cargo/native lanes. It must stop new allocations below the8GiB filesystem reserve.

Preflight at2026-09-06T09:16:36.234020+00:00: available bytes24586117120; new unit target is absent. Existing coordinator target is24,382,360KiB. New target planning allowance6GiB; actual size and free space must be measured during builds.

The dispatch loads aep-drive:implementor and then aep-drive:adversary charters
explicitly in generic harness agents because a typed subagent_type is unavailable.
The coordinator alone writes the planning store. Two adversarial passes maximum;
record the exact report before routing any correction. Token/cost totals are unknown.

The existing authorization covers this unit commit, integration merge, opening
and closing planning/evidence commits, and merge/publication to main after gates.
The final release procedure remains separate. No consumer deployment is implied.

## Stage

The source unit bf16e504ccad68b2ee67607ba39606aadf07f627 and five independent
adversarial tests79f062f9a17ca3d97ec8cd052cf69550b2f42ff9 are integrated. The first
review reported no product finding and retained all926 tracked source files.
Published count-writer work87d9945 was combined at12fb11a, with a separate bounded
integration review retaining all957 tracked files and adding two test files.
That review also reported no product finding. The exact immutable bodies and
review outcomes are in the planning store.

The full gate exposed one introduced test setup assumption: the new unconditional
Go test required an optional compiler override. Root recorded the gate finding
before fixing it to honor the override or use Go from PATH. Both positive controls
and all native assertions remain. The corrected isolated case and literal full
gate passed with the override absent. Final executable/test content is33215d2;
metadata-only count closurebe0eefd was merged asd3401f1 without changing it.

`task check` passed1,945 tests with zero failed/ignored; `task site-build` passed.
The metadata-merge path check passed5 cases. Full source/provenance, initial gate
and harness errors, actual exits and boundaries are recorded in
verification-report:normalization-binary64-integrated. The story is implemented;
this closing checkpoint is ready for publication to main. It creates no release
tag. Subsequent native-codec/positional/TypeScript units remain separately bound.
All compiler/native processes are terminal. Token/cost telemetry is unavailable.
Managed unit and integration trees remain tracked for the continuing authorized
sequence and final cleanup; no unrelated worktree is a cleanup candidate.

## Computed candidate sets, collisions and unassessed ids

The [complete original computation](../reviews/2026-09-06-model-binary64-opening-waves.md) preserves every wave, collision and unassessed entry unchanged. It is a dated planning observation: two inferred test filenames did not materialize. The implemented normalization tests live in `crates/generate/schema-contract/tests/normalization_binary64.rs`; domain qualification is in the existing domain test suite. The current governed story records the final scope.
