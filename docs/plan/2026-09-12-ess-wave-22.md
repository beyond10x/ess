# ESS wave 22 — three units, one gate

Coordinator: an ordinary session, running `aep-drive:wave` **skill version 0.9.1**.
Base branch `main` at `c8023067` (ESS 0.23.0). Integration branch `wave/ess-wave-22`.
Selection path: **`aep plan artifact waves --kind story --status draft --format json`**, `aep` protocol 0.55.0
— a computed selection, not a pairwise reading.

Approval: the operator pre-approved (*"get yourself the next wave and work on it"*), so the stage-1
stop was waived. The page is written anyway, because approval never removed the page.

**Commits this approval authorises, and nothing else:** three unit commits (one per branch below),
the three merges into `wave/ess-wave-22`, one closing store commit, and the merge of
`wave/ess-wave-22` into `main`. Not a push, not a tag, not a release, not the next wave.

`subagent_type` for every dispatch: `aep-drive:implementor`, then `aep-drive:adversary`.

## The units

| # | Story | Surface | Branch | Worktree | Build dir | Scratch | Stage |
|---|---|---|---|---|---|---|---|
| 1 | `story:literal-representation-walk-exhaustion` | `crates/specify/ess-domain` | `impl/literal-representation-walk-exhaustion` | `wt-8fb8a43f6a81` | `…/wt-8fb8a43f6a81/target` (in-worktree, cargo default) | `/var/tmp/ess-wave-22/scratch/unit1` | green, under adversary |
| 2 | `story:fixtures-carry-workstation-paths` | `crates/edge/ess-cli/tests/fixtures/coverage-producers/` + a new test in `crates/edge/ess-xtask/tests/` | `impl/fixtures-carry-workstation-paths` | `wt-5677d3fc5e9b` | `…/wt-5677d3fc5e9b/target` (in-worktree, cargo default) | `/var/tmp/ess-wave-22/scratch/unit2` | dispatched |
| 3 | `story:primitive-canonical-serialization` | `crates/specify/ess-primitives`, `crates/generate/ess-gen`, `crates/generate/ess-synth`, `crates/verify/ess-conformance` | `impl/primitive-canonical-serialization` | `wt-07c719139ac2` | `…/wt-07c719139ac2/target` (in-worktree, cargo default) | `/var/tmp/ess-wave-22/scratch/unit3` | **left the wave** — green half merged |

Integration checkout: `wt-7e1bf0dba3fd`, coordinator lease session `ess-wave-22-coordinator`.

Objective each unit serves: units 1 and 3 carry `serves vision:O2` already; unit 2 carries
`serves vision:O2`. No edge had to be added for this wave.

Every unit's scope is **cited** except unit 3's, which is 10 cited paths and 0 inferred, and unit 1's
two paths, both cited. Unit 2's two fixture paths are cited; its gate-lane location is `inferred` and
is constrained below.

## The verb's three lists, verbatim

**Waves** (`aep plan artifact waves --kind story --status draft`), after this session wrote the scope
entries four `story-scoper` runs returned:

```
wave 1: architecture-review-and-outlook, browser-fixture-startup-deadline,
        component-declares-its-settings, create-only-command-cannot-refuse,
        crosswalk-verb-external-names-held-to-declarations, fixtures-carry-workstation-paths,
        literal-representation-walk-exhaustion, native-realization-ci,
        primitive-canonical-serialization, release-status-publication-state
wave 2: enum-variant-in-an-entity-invariant, integrate-source-driven-realizations,
        java-conformance-target, normalization-equality-eligibility,
        schema-unique-items-signed-zero
wave 3: outcome-decided-by-environment, the-generated-go-runtime-is-gofmt-clean
```

**Collisions it excluded** — 12:

```
create-only-command-cannot-refuse x enum-variant-in-an-entity-invariant  crates/specify/ess-domain (cited)
create-only-command-cannot-refuse x java-conformance-target              crates/verify/ess-conformance (cited)
create-only-command-cannot-refuse x normalization-equality-eligibility   docs/design (inferred)
create-only-command-cannot-refuse x outcome-decided-by-environment       docs/design (inferred)
create-only-command-cannot-refuse x the-generated-go-runtime-is-gofmt-clean  crates/verify/ess-conformance (cited)
crosswalk-verb-external-names... x integrate-source-driven-realizations  crates/edge/ess-cli/src/main.rs (inferred)
crosswalk-verb-external-names... x schema-unique-items-signed-zero       Cargo.toml (inferred)
java-conformance-target x the-generated-go-runtime-is-gofmt-clean        crates/verify/ess-conformance (cited)
literal-representation-walk-exhaustion x outcome-decided-by-environment  crates/specify/ess-domain/src/command.rs (inferred)
native-realization-ci x schema-unique-items-signed-zero                  crates/generate/schema-contract/Cargo.toml (inferred)
native-realization-ci x the-generated-go-runtime-is-gofmt-clean          Taskfile.yml (cited)
normalization-equality-eligibility x outcome-decided-by-environment      docs/design (cited)
```

**Unassessed**: none. Before this session it was
`story:crosswalk-verb-external-names-held-to-declarations` and
`story:outcome-decided-by-environment`; both were scoped by `aep-drive:story-scoper` and written
back with `aep plan artifact scope`, so the list is now empty.

**Cycles**: none.

## What was deliberately left out of this wave, and why

| Story | Reason |
|---|---|
| `release-status-publication-state` | its own `## Scope` says: *"this imported draft belongs to the other session's release follow-up and is excluded from the coordinator's next implementation wave"* — cited |
| `browser-fixture-startup-deadline` | `geckodriver` is absent on this machine, so no red-then-green run is possible here. Property 1 |
| `native-realization-ci` | acceptance is *"a CI run on an exact candidate"* — a GitHub Actions lane, unverifiable in this tree. Property 1 |
| `integrate-source-driven-realizations` | acceptance requires the documentation build, which is `npm ci` against a pinned public revision — network. Property 1 |
| `java-conformance-target` | a whole new synthesis target; `mvn`/`gradle` would fetch dependencies. Property 1 |
| `crosswalk-verb-external-names-held-to-declarations` | a new crate, 8 of 9 scope paths inferred, and its `proto` adapter has no existing support to land on — `git grep prost\|protobuf` over `crates/` returns nothing. Confidence `medium`; acceptance requires a design page first |
| `architecture-review-and-outlook` | prose review of the whole tree, no `serves` edge, not mechanically verifiable |
| `create-only-command-cannot-refuse` | 7 packages, and collides with unit 1 on `crates/specify/ess-domain` (cited) |
| `component-declares-its-settings` | 4 packages including `schemas/generated/ess.schema.json` and its reviewed digests; collides with unit 1 on `ess-domain` |
| `outcome-decided-by-environment`, `normalization-equality-eligibility` | both design-only, both land in `docs/design`, and they collide with each other there (cited). Good candidates for a later wave, one at a time |
| `enum-variant-in-an-entity-invariant`, `schema-unique-items-signed-zero`, `the-generated-go-runtime-is-gofmt-clean` | verb waves 2 and 3 — next turns of the loop |

**Unit 3 is a deliberate deviation from property 3** (blast radius of one package): it touches four.
It is taken anyway because it is the only remaining candidate that collides with neither of the
other two units, and all ten of its scope paths are cited.

## Pre-flight, with the numbers

| Check | Result |
|---|---|
| base branch | `main` at `c8023067` |
| working tree clean | **no — 20 paths.** Operator-sanctioned override; see below |
| previous waves' trees | **21 linked worktrees were already present**, from waves that skipped cleanup. Not this wave's, and not repaired here |
| free disk | 15 G at first reading — **below any workable floor**. `wt-2e2efd694095/target`, a 47 G build cache on a clean tree whose branch `wave/model-driven-interpretation-1` is unchanged, was removed. **61 G free** at dispatch |
| other build directories still on disk | `ess/target` 41 G, `ess-classify-delivery-single-attempt-20260910/target` 3.8 G |
| one measured build | the full `task check` on this tree grew `target/` by ~21 G and exited 0 with 289 suites |
| compiler cache | `sccache` is installed at the host's `sccache` and `RUSTC_WRAPPER` is unset; each unit brief sets it |
| model budget | not stated by the operator, so N defaults to 4; N is 3 here because only three candidates passed all three properties |
| `AGENTS.md` | read. Gate is `task check`; anything executable is Rust; ordered collections in persisted data; a format version is required when meaning, identity, references, canonicalization, names or the persisted envelope change |

### The dirty tree, pasted as the override requires

```
MM .engineering/planning/journal.jsonl
A  .engineering/planning/story/a-binding-may-be-triggered-by-a-period.md
A  .engineering/planning/story/a-synthesized-suite-has-a-compact-form.md
A  .engineering/planning/story/a-timestamp-names-the-clock-it-was-read-from.md
A  .engineering/planning/story/an-authored-scenario-can-arrange-a-view-row.md
M  .engineering/planning/story/binding-delivery-at-most-once.md
AM .engineering/planning/story/crosswalk-verb-external-names-held-to-declarations.md
A  .engineering/planning/story/exhaustiveness-and-reachability-disagree.md
M  .engineering/planning/story/normalization-equality-eligibility.md
M  .engineering/planning/story/outcome-decided-by-environment.md
A  .engineering/planning/story/outcome-selected-by-the-subjects-state.md
M  Taskfile.yml
?? crates/edge/ess-xtask/tests/model_enums.rs
?? docs/plan/2026-09-12-essv2-first-half-gap-analysis.md
A  models/toolchain-scenarios/a-declaration-is-readable-by-name.yaml
AM models/toolchain/README.md
A  models/toolchain/components.yaml
A  models/toolchain/domains/{generate,infra,release,specify,verify}.yaml
A  models/toolchain/system.yaml
```

These are this session's own retrofit work and are **left exactly where they are** — not stashed,
not committed, not branched away.

**Which of them a unit's surface touches:** `Taskfile.yml` and
`crates/edge/ess-xtask/tests/model_enums.rs`. Unit 2's acceptance needs a gate lane, and its
recorded scope for that is `inferred`. Its brief therefore **forbids editing `Taskfile.yml`** and
requires the check to be a **new** file under `crates/edge/ess-xtask/tests/` — which `task check`
already runs via `cargo test --package ess-xtask`, so the acceptance is met without touching either
dirty path. Units 1 and 3 touch none of them.

## Stage log

- Integration branch created from `main` at `c8023067`; coordinator lease acquired.
- Three worktrees created, each branched, each with its own build directory and scratch root.
- Briefs written to `/var/tmp/ess-wave-22/unit{1,2,3}-brief.md`.
- Unit 1 returned **green**: 411 → 418 executed cases, 5 red first. `binding::representation` now
  returns `Resolution::{Established, Undeclared, Cyclic}` and walks with a visited-name set instead
  of a 32-iteration bound. Adversary dispatched against `wt-8fb8a43f6a81`.
- **Unit 1 handed back two files outside its assignment, and they are the coordinator's:**
  - `crates/generate/ess-gen/tests/docs.rs` — `literal_docs_match_the_checked_representation_depth_boundary`
    pins `checked = newtypes + optionals < 32`, which asserts the defect unit 1 removes. It is red
    against the change. Replacement patch measured by the implementor (40 passed, 0 failed) and
    restored byte-exactly: `/var/tmp/ess-wave-22/scratch/unit1/ess-gen-docs-literal-boundary.patch`.
  - `crates/specify/ess-compiler/src/ir.rs` — stale doc comments:
    `/var/tmp/ess-wave-22/scratch/unit1/ess-compiler-ir-literal-docs.patch`.
  - `tests/docs.rs` is inside **unit 3's** package. Both patches are applied on the integration
    branch after unit 1 and unit 3 have merged, not in either unit's tree.

- Unit 3 returned **blocked**, and the story **leaves the wave**. Its `Integer` half is green
  (881 → 884 executed, red 0, all four packages exit 0) and is merged as `3807cb68` →
  `f3ada66f`: a 110-line additive test file and no tracked file changed. Its `Decimal` half moves
  persisted bytes and needs a reader in another repository, recorded as
  `decision-blocker:decimal-canonical-serialization-crosses-repositories`, which `blocks` the story.
  The story stays `active`; the unlanded patches are named in the blocker.
- **No adversary was dispatched against unit 3.** Its whole change is new test cases and no
  implementation, so there is nothing for an adversary to break. Stated rather than skipped quietly.
- Unit 3 also reported one correction to its own story's `## Scope`: `ess-conformance/src/report.rs`
  is attributed to "cited by stage one", and `story:review-primitive-semantics`'s scope list does
  not contain it. The path is right and the attribution is wrong. Carried in the blocker.
- Local `git commit` is refused in this repository by `b10x-gates`
  (*"local commits require the exact bot author and committer"*); every wave commit goes through
  `b10x-gates bot --repository beyond10x/ess -- commit`.
- **Unit 1, adversary pass 1** — `review-result:adversary-wave22-unit1-pass-1`. Verdict
  NEEDS-CHANGE, cases 418 → 423, red 3, origin `introduced 3 / pre-existing 0 / undecided 1`.
  Five findings; the routing, one row each:

  | # | Finding | Origin | Routed |
  |---|---|---|---|
  | 1 | three unresolved doc links in `binding.rs` — `doc-check` exits 101 while the package lanes stay green | introduced | back to the same implementor |
  | 2 | `ess-compiler/src/ir.rs:761`, `:1050` still document literal checking as bounded by `WRAPPER_LIMIT` | introduced | **coordinator's** — outside the unit's assignment. Its own patch applies clean to the integration branch and is **held until unit 1 merges**, so the docs do not describe behaviour the branch does not yet have |
  | 3 | the `Cyclic` arm double-reports `Ring = newtype of Ring`, which `check_inhabitation` already refuses | introduced | back to the same implementor |
  | 4 | the new `Resolution` enum mints four unclassified consumer-coverage entries; `consumer-check` bails | introduced | back to the same implementor, with `entry-classifications.json` added to its assignment for this round only |
  | 5 | the `ess-gen` docs renderer under-claims past 32 wrappers | **undecided** | **coordinator's, and taken: no-op.** It is an under-claim, the adversary could not show any document in the repository with a chain that long (*nothing found*), and widening the renderer moves byte-pinned generated output |

  Correction round 1 sent to **the same implementor**, which still holds its context — no case has
  failed twice, so the fresh-implementor rule does not fire. Brief:
  `/var/tmp/ess-wave-22/unit1-correction.md`, which carries only what changed.

- **One store record is out of order and is corrected by appending, not editing.** A
  `review_outcome` of `no-op` was written against pass 1 before the routing had resolved; the
  accurate outcome for findings 1, 3 and 4 is `fixed` and is appended when the correction lands.
  `aep plan artifact evidence` is append-only by design, so the premature record stays and this line
  is the reason it is there.

- **Correction round 1 returned green**, and corrected the coordinator. Four lanes exit 0 —
  `test`, `clippy`, `fmt` and `doc` — with `literal_representation_adversary` at
  `test result: ok. 5 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`, so the adversary's
  five ran rather than being deselected. Package total 418 → 426.

  The correction: `representation` gained a **fourth** answer. `Uninhabited` is silent and left to
  `check_inhabitation`; `Cyclic` is refused and is now only the ring that *has* a base case. The
  rule counts base cases crossed and compares against the count recorded when a name was first
  seen, so an `Optional` crossed on the way to a ring does not give the ring one. Applied to both
  consumers — pass 1 only measured the binding surface, and the implementor added the payload twin.
  Five classification rows, not the adversary's four: the fourth variant would have been left
  unclassified by a list read off the finding instead of off the minting rule at
  `consumer_coverage/consumer.rs:262-273`.

- **The coordinator conflated two separate `ess-gen` items in the pass-1 routing, and the
  implementor caught it.** Finding 5 — the docs renderer under-claiming past 32 wrappers — is the
  no-op. `/var/tmp/ess-wave-22/scratch/unit1/ess-gen-docs-literal-boundary.patch` is **not** that:
  it replaces `literal_docs_match_the_checked_representation_depth_boundary`, which asserts
  `checked = newtypes + optionals < 32` — the defect itself — and goes red against this change.
  Without it `cargo test -p ess-gen --test docs` is red on the integration branch. `git apply
  --check` against the integration branch: exit 0. Both patches are applied after unit 1 merges.

- Adversary pass 2 dispatched. A correction re-enters adversarial verification; green does not
  route to merge.
