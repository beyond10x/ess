diff --git a/crates/generate/schema-contract/Cargo.toml b/crates/generate/schema-contract/Cargo.toml
index d9f59bf..7b9335e 100644
--- a/crates/generate/schema-contract/Cargo.toml
+++ b/crates/generate/schema-contract/Cargo.toml
@@ -8,6 +8,14 @@ license.workspace = true
 repository.workspace = true
 authors.workspace = true
 
+[features]
+# Explicit external-tool lane; selecting it requires ESS_TYPESCRIPT_COMPILER.
+typescript-typecheck = []
+
+[[test]]
+name = "typescript_typecheck"
+required-features = ["typescript-typecheck"]
+
 [dependencies]
 serde_json.workspace = true
 thiserror.workspace = true
diff --git a/crates/generate/schema-contract/src/typescript.rs b/crates/generate/schema-contract/src/typescript.rs
index 74ea575..55848d0 100644
--- a/crates/generate/schema-contract/src/typescript.rs
+++ b/crates/generate/schema-contract/src/typescript.rs
@@ -63,7 +63,7 @@ pub enum ProjectionError {
 /// The output describes structural types only. JSON Schema remains authoritative for refinements
 /// such as patterns, formats, numeric bounds and uniqueness, which TypeScript cannot enforce.
 pub fn project(schema: &Value, root_name: &str) -> Result<String, ProjectionError> {
-    if !typescript_identifier(root_name) {
+    if !typescript_type_identifier(root_name) {
         return Err(ProjectionError::InvalidRootName(root_name.to_owned()));
     }
     let identifier = schema
@@ -74,7 +74,7 @@ pub fn project(schema: &Value, root_name: &str) -> Result<String, ProjectionErro
         .ok_or(ProjectionError::MissingSchemaId)?;
     let root = object(schema, "")?;
     let definitions = definitions(root)?;
-    let names = definition_names(definitions)?;
+    let names = definition_names(definitions, root_name)?;
 
     let mut output = format!(
         "// @generated by ESS schema-contract projection; do not edit.\n\
@@ -83,11 +83,11 @@ pub fn project(schema: &Value, root_name: &str) -> Result<String, ProjectionErro
     );
     for (key, definition) in definitions {
         let name = &names[key];
-        let rendered = render_type(definition, &pointer("/$defs", key), 0, &names)?;
+        let rendered = render_type(definition, &pointer("/$defs", key), 0, &names, root_name)?;
         writeln!(output, "export type {name} = {rendered};\n")
             .expect("writing to a String cannot fail");
     }
-    let rendered = render_type(schema, "", 0, &names)?;
+    let rendered = render_type(schema, "", 0, &names, root_name)?;
     writeln!(output, "export type {root_name} = {rendered};")
         .expect("writing to a String cannot fail");
     Ok(output)
@@ -98,6 +98,7 @@ fn render_type(
     path: &str,
     depth: usize,
     names: &BTreeMap<String, String>,
+    root_name: &str,
 ) -> Result<String, ProjectionError> {
     let object = object(schema, path)?;
     check_keywords(object, path)?;
@@ -134,7 +135,7 @@ fn render_type(
                 value
                     .as_str()
                     .ok_or_else(|| invalid(&pointer(path, "type"), "type entries must be strings"))
-                    .and_then(|kind| render_kind(kind, object, path, depth, names))
+                    .and_then(|kind| render_kind(kind, object, path, depth, names, root_name))
             })
             .collect::<Result<Vec<_>, _>>()?;
         variants.sort();
@@ -147,7 +148,7 @@ fn render_type(
             "type must be a string or string array",
         )
     })?;
-    render_kind(kind, object, path, depth, names)
+    render_kind(kind, object, path, depth, names, root_name)
 }
 
 fn render_kind(
@@ -156,6 +157,7 @@ fn render_kind(
     path: &str,
     depth: usize,
     names: &BTreeMap<String, String>,
+    root_name: &str,
 ) -> Result<String, ProjectionError> {
     match kind {
         "string" => Ok("string".to_owned()),
@@ -163,15 +165,23 @@ fn render_kind(
         "boolean" => Ok("boolean".to_owned()),
         "null" => Ok("null".to_owned()),
         "array" => {
+            // Reserve the global helper only where it is actually emitted. An alias named
+            // Array remains valid in projections that do not use the helper.
+            if root_name == "Array" || names.values().any(|name| name == "Array") {
+                return Err(invalid(
+                    path,
+                    "a declaration shadows the required TypeScript `Array` helper",
+                ));
+            }
             let items = schema
                 .get("items")
                 .ok_or_else(|| invalid(path, "an array must declare `items`"))?;
             Ok(format!(
                 "Array<{}>",
-                render_type(items, &pointer(path, "items"), depth, names)?
+                render_type(items, &pointer(path, "items"), depth, names, root_name)?
             ))
         }
-        "object" => render_object(schema, path, depth, names),
+        "object" => render_object(schema, path, depth, names, root_name),
         other => Err(invalid(
             &pointer(path, "type"),
             format!("unsupported JSON Schema type `{other}`"),
@@ -184,6 +194,7 @@ fn render_object(
     path: &str,
     depth: usize,
     names: &BTreeMap<String, String>,
+    root_name: &str,
 ) -> Result<String, ProjectionError> {
     if schema.get("additionalProperties") != Some(&Value::Bool(false)) {
         return Err(invalid(
@@ -209,6 +220,7 @@ fn render_object(
             &pointer(&pointer(path, "properties"), name),
             depth + 1,
             names,
+            root_name,
         )?;
         let property_name = if typescript_identifier(name) {
             name.clone()
@@ -261,6 +273,7 @@ fn definitions(root: &Map<String, Value>) -> Result<&Map<String, Value>, Project
 
 fn definition_names(
     definitions: &Map<String, Value>,
+    root_name: &str,
 ) -> Result<BTreeMap<String, String>, ProjectionError> {
     let mut names = BTreeMap::new();
     let mut reverse = BTreeMap::<String, String>::new();
@@ -271,6 +284,12 @@ fn definition_names(
                 "definition name cannot become a TypeScript identifier",
             )
         })?;
+        if projected == root_name {
+            return Err(invalid(
+                &pointer("/$defs", key),
+                format!("definition and root both project to `{root_name}`"),
+            ));
+        }
         if let Some(first) = reverse.insert(projected.clone(), key.clone()) {
             return Err(ProjectionError::NameCollision {
                 first,
@@ -398,6 +417,71 @@ fn typescript_identifier(value: &str) -> bool {
         && bytes.all(|byte| matches!(byte, b'a'..=b'z' | b'A'..=b'Z' | b'0'..=b'9' | b'_' | b'$'))
 }
 
+fn typescript_type_identifier(value: &str) -> bool {
+    // Binding restrictions differ from property spelling: keywords are valid object keys.
+    // These words cannot name an exported type alias in the supported TypeScript target.
+    const RESERVED: &[&str] = &[
+        "break",
+        "case",
+        "catch",
+        "class",
+        "const",
+        "continue",
+        "debugger",
+        "default",
+        "delete",
+        "do",
+        "else",
+        "enum",
+        "export",
+        "extends",
+        "false",
+        "finally",
+        "for",
+        "function",
+        "if",
+        "import",
+        "in",
+        "instanceof",
+        "new",
+        "null",
+        "return",
+        "super",
+        "switch",
+        "this",
+        "throw",
+        "true",
+        "try",
+        "typeof",
+        "var",
+        "void",
+        "while",
+        "with",
+        "any",
+        "unknown",
+        "never",
+        "number",
+        "bigint",
+        "boolean",
+        "string",
+        "symbol",
+        "object",
+        "undefined",
+        "as",
+        "implements",
+        "interface",
+        "let",
+        "package",
+        "private",
+        "protected",
+        "public",
+        "static",
+        "yield",
+        "await",
+    ];
+    typescript_identifier(value) && !RESERVED.contains(&value)
+}
+
 fn pascal_case(value: &str) -> Option<String> {
     let mut output = String::new();
     for word in value.split(|character: char| !character.is_ascii_alphanumeric()) {
@@ -521,4 +605,170 @@ mod tests {
         let error = project(&external, "Registry").expect_err("external refs are not projected");
         assert!(error.to_string().contains("only local"), "{error}");
     }
+
+    #[test]
+    fn root_and_normalized_definitions_cannot_claim_the_same_binding() {
+        for (key, root) in [
+            ("item", "Item"),
+            ("foo-bar", "FooBar"),
+            ("foo_bar", "FooBar"),
+            ("foo/bar", "FooBar"),
+        ] {
+            let reference = format!("#/$defs/{}", key.replace('/', "~1"));
+            let source = json!({
+                "$id": "urn:example:collision:1", "$ref": reference,
+                "$defs": {key: {"type": "string"}}
+            });
+            let before = source.clone();
+            let first = project(&source, root).expect_err("root/definition collision must refuse");
+            assert_eq!(project(&source, root).expect_err("repeat refusal"), first);
+            assert_eq!(
+                source, before,
+                "target refusal must not rewrite schema authority"
+            );
+        }
+    }
+
+    #[test]
+    fn normalized_definition_collisions_keep_the_existing_deterministic_refusal() {
+        let source = json!({
+            "$id": "urn:example:collision:1", "$ref": "#/$defs/foo-bar",
+            "$defs": {"foo_bar": {"type": "number"}, "foo-bar": {"type": "string"}}
+        });
+        assert_eq!(
+            project(&source, "Root"),
+            Err(ProjectionError::NameCollision {
+                first: "foo-bar".to_owned(),
+                second: "foo_bar".to_owned(),
+                projected: "FooBar".to_owned()
+            })
+        );
+    }
+
+    #[test]
+    fn reserved_words_and_primitive_type_aliases_are_refused() {
+        let source = json!({"$id": "urn:example:names:1", "type": "string"});
+        for name in [
+            "break",
+            "case",
+            "catch",
+            "class",
+            "const",
+            "continue",
+            "debugger",
+            "default",
+            "delete",
+            "do",
+            "else",
+            "enum",
+            "export",
+            "extends",
+            "false",
+            "finally",
+            "for",
+            "function",
+            "if",
+            "import",
+            "in",
+            "instanceof",
+            "new",
+            "null",
+            "return",
+            "super",
+            "switch",
+            "this",
+            "throw",
+            "true",
+            "try",
+            "typeof",
+            "var",
+            "void",
+            "while",
+            "with",
+            "any",
+            "unknown",
+            "never",
+            "number",
+            "bigint",
+            "boolean",
+            "string",
+            "symbol",
+            "object",
+            "undefined",
+        ] {
+            assert_eq!(
+                project(&source, name),
+                Err(ProjectionError::InvalidRootName(name.to_owned())),
+                "{name}"
+            );
+        }
+    }
+
+    #[test]
+    fn emitted_array_helper_cannot_be_shadowed_by_root_or_definition() {
+        for source in [
+            json!({"$id": "urn:example:array:1", "type": "array", "items": {"type": "string"}}),
+            json!({"$id": "urn:example:array:1", "type": "object", "additionalProperties": false,
+                "properties": {"nested": {"type": "array", "items": {"type": "string"}}}}),
+        ] {
+            project(&source, "Array").expect_err("root must not shadow emitted Array helper");
+            let mut with_definition = source;
+            with_definition["$defs"] = json!({"array": {"type": "string"}});
+            project(&with_definition, "Root")
+                .expect_err("definition must not shadow emitted Array helper");
+        }
+    }
+
+    #[test]
+    fn module_reserved_aliases_are_refused() {
+        let source = json!({"$id": "urn:example:names:1", "type": "string"});
+        for name in [
+            "as",
+            "implements",
+            "interface",
+            "let",
+            "package",
+            "private",
+            "protected",
+            "public",
+            "static",
+            "yield",
+            "await",
+        ] {
+            assert_eq!(
+                project(&source, name),
+                Err(ProjectionError::InvalidRootName(name.to_owned())),
+                "{name}"
+            );
+        }
+    }
+
+    #[test]
+    fn unused_array_name_and_keyword_properties_keep_their_valid_bytes() {
+        let scalar = json!({"$id": "urn:example:names:1", "type": "string"});
+        assert!(project(&scalar, "Array")
+            .expect("unused helper name is valid")
+            .ends_with("export type Array = string;\n"));
+        let source = json!({"$id": "urn:example:names:1", "$ref": "#/$defs/array",
+            "$defs": {"array": {"type": "string"}}});
+        assert!(project(&source, "Root")
+            .expect("Array definition without helper use")
+            .ends_with("export type Root = Array;\n"));
+        let source = json!({"$id": "urn:example:names:1", "type": "object", "additionalProperties": false,
+            "properties": {"class": {"type": "string"}, "string": {"type": "string"},
+                "Array": {"type": "string"}, "wire-key": {"type": "string"}},
+            "examples": [{"type": "array"}]
+        });
+        let output =
+            project(&source, "Array").expect("property and annotation words are not helper uses");
+        assert!(output.ends_with("export type Array = {\n  Array?: string;\n  class?: string;\n  string?: string;\n  \"wire-key\"?: string;\n};\n"), "{output}");
+    }
+
+    #[test]
+    fn noncolliding_projection_retains_the_complete_baseline_bytes() {
+        assert_eq!(
+            project(&schema(), "Registry").expect("noncollision control"),
+            include_str!("../tests/fixtures/registry.ts")
+        );
+    }
 }
diff --git a/crates/generate/schema-contract/tests/fixtures/registry.ts b/crates/generate/schema-contract/tests/fixtures/registry.ts
new file mode 100644
index 0000000..8f88e95
--- /dev/null
+++ b/crates/generate/schema-contract/tests/fixtures/registry.ts
@@ -0,0 +1,15 @@
+// @generated by ESS schema-contract projection; do not edit.
+// Source: urn:example:registry:1
+// JSON Schema remains authoritative for runtime validation and refinements.
+
+export type Item = {
+  enabled?: boolean;
+  id: number;
+  state: "active" | "draft";
+};
+
+export type Registry = {
+  items: Array<Item>;
+  "optional-label"?: null | string;
+  schema: "urn:example:registry:1";
+};
diff --git a/crates/generate/schema-contract/tests/typescript_typecheck.rs b/crates/generate/schema-contract/tests/typescript_typecheck.rs
new file mode 100644
index 0000000..0cb1c7f
--- /dev/null
+++ b/crates/generate/schema-contract/tests/typescript_typecheck.rs
@@ -0,0 +1,247 @@
+//! Explicit compiler-backed projection checks, enabled by `typescript-typecheck`.
+
+use std::path::{Path, PathBuf};
+use std::process::{Command, Output};
+use std::sync::atomic::{AtomicUsize, Ordering};
+use std::sync::OnceLock;
+
+use schema_contract::typescript::project;
+use serde_json::{json, Value};
+
+fn compiler() -> &'static Path {
+    static COMPILER: OnceLock<PathBuf> = OnceLock::new();
+    COMPILER.get_or_init(|| {
+        let path = PathBuf::from(
+            std::env::var_os("ESS_TYPESCRIPT_COMPILER")
+                .expect("the selected typescript-typecheck lane requires ESS_TYPESCRIPT_COMPILER"),
+        );
+        assert!(
+            path.is_file(),
+            "ESS_TYPESCRIPT_COMPILER must name an installed tsc.js: {}",
+            path.display()
+        );
+        let version = Command::new("node")
+            .arg(&path)
+            .arg("--version")
+            .output()
+            .expect("run configured TypeScript compiler");
+        assert!(
+            version.status.success(),
+            "compiler version query failed: {version:?}"
+        );
+        assert_eq!(
+            String::from_utf8_lossy(&version.stdout).trim(),
+            "Version 6.0.3",
+            "the target lane uses the repository's pinned compiler"
+        );
+        path
+    })
+}
+
+fn typecheck(sources: &[String]) -> Output {
+    static NEXT: AtomicUsize = AtomicUsize::new(0);
+    let compiler = compiler();
+    let root = Path::new(env!("CARGO_TARGET_TMPDIR")).join(format!(
+        "typescript-{}-{}",
+        std::process::id(),
+        NEXT.fetch_add(1, Ordering::Relaxed)
+    ));
+    std::fs::create_dir_all(&root).expect("worktree-local compiler fixtures");
+    let mut files = Vec::new();
+    for (index, source) in sources.iter().enumerate() {
+        let filename = format!("case-{index}.ts");
+        std::fs::write(root.join(&filename), source).expect("generated declaration");
+        files.push(filename);
+    }
+    let config = root.join("tsconfig.json");
+    std::fs::write(&config, serde_json::to_vec_pretty(&json!({
+        "compilerOptions": {"strict": true, "noEmit": true, "types": [], "target": "ES2022", "module": "ESNext"},
+        "files": files
+    })).expect("compiler configuration")).expect("write isolated compiler configuration");
+    Command::new("node")
+        .arg(compiler)
+        .args(["--pretty", "false", "--project"])
+        .arg(config)
+        .output()
+        .expect("typecheck generated declarations")
+}
+
+fn accepted(source: &Value, root: &str, output: &mut Vec<String>) {
+    if let Ok(generated) = project(source, root) {
+        output.push(generated);
+    }
+}
+
+// The pinned compiler's keyword vocabulary, plus ordinary, helper and contextual controls.
+const ROOT_NAME_CORPUS: &[&str] = &[
+    "Root",
+    "$Root",
+    "_Root",
+    "Array",
+    "any",
+    "unknown",
+    "never",
+    "number",
+    "bigint",
+    "boolean",
+    "string",
+    "symbol",
+    "void",
+    "object",
+    "undefined",
+    "break",
+    "case",
+    "catch",
+    "class",
+    "const",
+    "continue",
+    "debugger",
+    "default",
+    "delete",
+    "do",
+    "else",
+    "enum",
+    "export",
+    "extends",
+    "false",
+    "finally",
+    "for",
+    "function",
+    "if",
+    "import",
+    "in",
+    "instanceof",
+    "new",
+    "null",
+    "return",
+    "super",
+    "switch",
+    "this",
+    "throw",
+    "true",
+    "try",
+    "typeof",
+    "var",
+    "while",
+    "with",
+    "implements",
+    "interface",
+    "let",
+    "package",
+    "private",
+    "protected",
+    "public",
+    "static",
+    "yield",
+    "abstract",
+    "accessor",
+    "as",
+    "asserts",
+    "assert",
+    "async",
+    "await",
+    "constructor",
+    "declare",
+    "from",
+    "get",
+    "global",
+    "infer",
+    "intrinsic",
+    "is",
+    "keyof",
+    "module",
+    "namespace",
+    "of",
+    "out",
+    "override",
+    "readonly",
+    "require",
+    "satisfies",
+    "set",
+    "type",
+    "unique",
+    "using",
+    "defer",
+    "__esModule",
+    "eval",
+    "arguments",
+];
+
+#[test]
+fn accepted_binding_collision_and_keyword_corpus_typechecks() {
+    let mut sources = Vec::new();
+    let scalar = json!({"$id": "urn:example:bindings:1", "type": "string"});
+    for root in ROOT_NAME_CORPUS {
+        accepted(&scalar, root, &mut sources);
+    }
+    for key in ["item", "Item", "item-name", "item_name", "array", "Array"] {
+        let source = json!({"$id": "urn:example:bindings:1", "$ref": format!("#/$defs/{key}"), "$defs": {key: {"type": "string"}}});
+        for root in ["Root", "Item", "ItemName", "Array"] {
+            accepted(&source, root, &mut sources);
+        }
+    }
+    for root in ["Array", "Root"] {
+        for definition in ["array", "item"] {
+            let source = json!({"$id": "urn:example:bindings:1", "type": "array", "items": {"$ref": format!("#/$defs/{definition}")}, "$defs": {definition: {"type": "string"}}});
+            accepted(&source, root, &mut sources);
+        }
+    }
+    assert!(
+        sources.len() >= 10,
+        "the corpus must contain accepted controls"
+    );
+    let output = typecheck(&sources);
+    assert!(
+        output.status.success(),
+        "accepted bindings failed TypeScript:\n{}\n{}",
+        String::from_utf8_lossy(&output.stdout),
+        String::from_utf8_lossy(&output.stderr)
+    );
+}
+
+#[test]
+fn keyword_properties_and_valid_contextual_aliases_typecheck_without_rewriting() {
+    let source = json!({"$id": "urn:example:properties:1", "type": "object", "additionalProperties": false,
+        "properties": {"class": {"type": "string"}, "string": {"type": "string"}, "Array": {"type": "string"}, "wire-key": {"type": "string"}},
+        "examples": [{"type": "array"}]
+    });
+    let mut sources = Vec::new();
+    for root in [
+        "Array",
+        "type",
+        "namespace",
+        "readonly",
+        "abstract",
+        "$Type",
+        "_Type",
+    ] {
+        let generated = project(&source, root).expect("valid alias and property control");
+        assert!(
+            generated.contains("  class?: string;\n  string?: string;\n  \"wire-key\"?: string;")
+        );
+        sources.push(generated);
+    }
+    sources.push(include_str!("fixtures/registry.ts").to_owned());
+    let output = typecheck(&sources);
+    assert!(
+        output.status.success(),
+        "valid controls failed TypeScript:\n{}\n{}",
+        String::from_utf8_lossy(&output.stdout),
+        String::from_utf8_lossy(&output.stderr)
+    );
+}
+
+#[test]
+fn compiler_rejects_a_known_duplicate_binding() {
+    let output = typecheck(&[
+        "export type Duplicate = string;\nexport type Duplicate = number;\n".to_owned(),
+    ]);
+    assert!(
+        !output.status.success(),
+        "the real compiler must reject duplicate aliases"
+    );
+    assert!(
+        String::from_utf8_lossy(&output.stdout).contains("TS2300"),
+        "expected duplicate-identifier diagnostics: {output:?}"
+    );
+}
