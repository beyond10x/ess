diff --git a/docs/design/review-conformance-coverage.md b/docs/design/review-conformance-coverage.md
index be48b5a..ca1bd67 100644
--- a/docs/design/review-conformance-coverage.md
+++ b/docs/design/review-conformance-coverage.md
@@ -6,7 +6,7 @@ Decision date: 2026-09-05. Owner: ESS. Planning authority: story:review-conforma
 
 ## Decision
 
-Freeze the bytes and meanings of standalone report **ess-conformance-report/1** and suite **ess-conformance/1–4**. Introduce **ess-conformance-report/2** for explicit, producer-specific outcome partitions and exact-suite binding, and **ess-conformance/5** for durable coverage and selection. New reports can describe admitted legacy suites, but their coverage remains unknown and cannot qualify as complete. A report's execution verdict remains the producer's verdict; coverage qualification is a separate, explicit result.
+Freeze the bytes and meanings of standalone report **ess-conformance-report/1** and suite **ess-conformance/1–4**. Introduce **ess-conformance-report/2** for explicit, producer-specific outcome partitions and exact-suite binding, and **ess-conformance/5** for durable coverage and selection. Report/1 remains restricted to suite majors 1–4 even when execution readers add suite/5; suite/5 requires report/2. New reports can describe admitted legacy suites, but their coverage remains unknown and cannot qualify as complete. A report's execution verdict remains the producer's verdict; coverage qualification is a separate, explicit result.
 
 Move readers and consumers before writers. Do not enable a new default until the Atlas migration ADR, every relying reader, generated-runtime regeneration, cross-language fixtures, and the shipped-version record are complete. Increasing a version string does not make today's suite readers refuse it.
 
@@ -20,15 +20,15 @@ AEP citations are Git-object reads at **00c742e4179593738a2e8aa69e2ecc07d3c89402
 
 | Ref | Inspected owner and source | Existing behavior and required disposition |
 | --- | --- | --- |
-| S1 | [evidence.rs](../../crates/verify/ess-conformance/src/evidence.rs), lines 12–164 | Rust standalone v1 writer counts every non-pass in scenarios_failed and writes status-prefixed strings. The current validated Serde reader is closed, admits report/1 and supported suite majors 1–4, and checks counts/list length and aggregate status. Preserve its valid v1 bytes and meanings. Add a separate v2 type/admission path. |
+| S1 | [evidence.rs](../../crates/verify/ess-conformance/src/evidence.rs), lines 12–164 | Rust standalone v1 writer counts every non-pass in scenarios_failed and writes status-prefixed strings. The current validated Serde reader is closed, admits report/1 and supported suite majors 1–4, and checks counts/list length and aggregate status. Preserve its valid v1 bytes and meanings. Freeze v1's suite-major allowlist at 1–4 independently of the growing execution allowlist; add a separate v2 type/admission path. |
 | S2 | [report.rs](../../crates/verify/ess-conformance/src/report.rs), lines 40–80 and 535–611 | Rust scenario precedence and whole-run aggregation differ; detailed ConformanceReport is a separate serializable structure. Preserve both execution rules and the legacy detailed CLI surface. |
 | S3 | [scenario.rs](../../crates/verify/ess-conformance/src/scenario.rs), lines 101–208, 236–309 and 365–427 | Suites have provenance and an ordered scenario map. component is omitted when absent. CURRENT is 4, supported majors are 1–4, but SuiteFormat deserialization checks syntax rather than membership. from_json does not add admission. Add supported-version/vocabulary checks at every execution entry, including generic Serde routes. |
 | S4 | [runner.rs](../../crates/verify/ess-conformance/src/runner.rs), lines 286–310 | Runner::run accepts a typed suite without checking its version, records every scenario, and aggregates their final statuses. Add an admitted-suite boundary before target identity or scenario callbacks. |
 | S5 | [Go emitter](../../crates/verify/ess-conformance/src/go/mod.rs), lines 49–77; [runtime.go](../../crates/verify/ess-conformance/src/go/runtime.go), lines 383–406, 499–639, 686–704, 759, 1082 and 1344–1356 | Emitter embeds exact suite.json. Runtime decodes a reduced struct, omitting scenario dependency and other metadata, and never checks SuiteVersion. It uses passed/failed/skipped, ordinary errors fail, unsupported observations or unknown step vocabulary skip, and teardown can turn a skip into failed. Regenerate adopter runtimes, add pre-execution admission, and hash retained complete input bytes. |
 | S6 | [main.rs](../../crates/edge/ess-cli/src/main.rs), lines 2007–2013, 2435–2488, 2493–2558, 2602–2625 and 2640 onward | impact reads an optional suite; conform run --suite reads one; conform run --path discards synthesis refusals by taking .suite before merging authored scenarios. synthesize prints refusals/outside separately. author/web form authored-only suites. --report-out writes standalone JSON; --format json/yaml serializes the detailed report. These are separate contracts and each needs explicit dispatch. |
-| S7 | [synthesize.rs](../../crates/verify/ess-conformance/src/synthesize.rs), lines 197–246 and 1024–1030; [authored.rs](../../crates/verify/ess-conformance/src/authored.rs), lines 715–750 | Synthesis holds suite/refusals/outside; Authoring holds scenarios/refusals. A refusal may have no scenario ID. Outside means the scenario exists but needs another component. Persist these distinctions; a refusal-record count is not a failed-scenario count. |
+| S7 | [synthesize.rs](../../crates/verify/ess-conformance/src/synthesize.rs), lines 197–246, 997–1030, 2068–2109, 2697–2699, 2815–2846 and 2910–2976; [authored.rs](../../crates/verify/ess-conformance/src/authored.rs), lines 715–750 and 1416–1483 | Synthesis holds suite/refusals/outside; Authoring holds scenarios/refusals. RefusalUndeclared, ViewUndecidable, OrderUnwitnessed and some InvariantUnobservable results retain a runnable scenario beside an omitted check. Duplicate candidates/sources retain the first accepted scenario beside a refusal naming the same ID; authored Duplicate also names the rejected origin and accepted first source. Component synthesis keeps whole-system refusals while moving scenarios outside. Persist each distinction and source identity; refusal records are not execution results. |
 | S8 | [web.rs](../../crates/verify/ess-conformance/src/web.rs), lines 54–65; [player.js](../../crates/verify/ess-conformance/assets/player.js), lines 28–31, 96 and 273 | Web emits suite.json and the player fetches parsed JSON directly. There is no version/digest admission. Player is a replay UI, not an implementation report producer. Update model/suite checks and selection display; regenerate the player and suite as one set. |
-| S9 | [impact.rs](../../crates/verify/ess-diff/src/impact.rs), lines 775–851 and 893–930 | Impact compares system/model/contract provenance and intersects scenario dependencies. It does not verify an execution report or establish full coverage. Preserve existing impact/1 bytes; admit v5 suites through an internal view, carry selection in diagnostics, and conservatively refuse evidence reuse when the exact selected suite is unavailable. |
+| S9 | [impact.rs](../../crates/verify/ess-diff/src/impact.rs), lines 86–96, 775–851 and 893–930 | Impact compares system/model/contract provenance and intersects scenario dependencies. It does not verify an execution report or establish full coverage. Preserve existing ess-impact/2 bytes; admit v5 suites through an internal view, carry selection in diagnostics, and conservatively refuse evidence reuse when the exact selected suite is unavailable. |
 | S10 | [ess-realization/lib.rs](../../crates/specify/ess-realization/src/lib.rs), lines 375–390 | ConformanceEvidence contains suite_digest/report_digest artifact references and a status. No parser verifying the referenced suite/report bytes was established. These remain existing artifact identities; do not reinterpret them as the new digest profile or call them a migrated report reader. A future byte-verifying bridge needs its own design/version and owner. |
 | A1 | [AEP optional adapter](https://github.com/beyond10x/aep/blob/00c742e4179593738a2e8aa69e2ecc07d3c89402/crates/observe/aep-ess-evidence/src/lib.rs#L15), lines 15–32 and 138–167 | aep-ess-evidence transcribes a closed report/1, checks count consistency, and transfers scenarios_failed unchanged into AEP's existing result. Versioned v2 parsing and domain mapping are separately required. |
 | A2 | [AEP planning reader](https://github.com/beyond10x/aep/blob/00c742e4179593738a2e8aa69e2ecc07d3c89402/crates/edge/aep-cli/src/planning.rs#L5907), lines 5907–5985 | recorded_from_report independently reads JSON Value, requires report/1, refuses zero total, and incorporates scenarios_failed into recorded prose. It does not call A1 or preserve all execution facts. It must gain its own strict version dispatch and truthful category/coverage wording; updating A1 alone does not migrate it. |
@@ -58,7 +58,7 @@ Rust check aggregation within one scenario remains **failed > error > unsupporte
 
 For either producer, zero terminal scenarios currently aggregates to passed. Freeze that execution fact in v1; do not infer complete conformance from it. Rust detailed status error maps to standalone inconclusive. Rust CLI currently exits 0/1/3 for passed/failed/error. Go's test process may exit zero when all selected subtests skip while its standalone report says inconclusive; Go test success is not conformance qualification. [S2, S5, S6]
 
-The current validated v1 reader accepts both non-pass vocabularies because v1 carries no producer profile. It does not establish exact-suite binding, complete coverage, or uniqueness of opaque listed scenario IDs. Do not strengthen those unknown historical claims by upgrading a report. Keep the existing v1 shape and admitted meanings separate from v2's stronger invariants. [S1]
+The current validated v1 reader accepts both non-pass vocabularies because v1 carries no producer profile. It does not establish exact-suite binding, complete coverage, or uniqueness of opaque listed scenario IDs. Do not strengthen those unknown historical claims by upgrading a report. Keep the existing v1 shape and admitted meanings separate from v2's stronger invariants. Its suite_version admits exactly ess-conformance/1, /2, /3 and /4 forever under report/1. The future v1 reader must use that frozen allowlist, not the general SuiteFormat::is_supported helper after it grows to include /5. A v1 document naming suite/5 is refused by Rust and every migrated reader; no v1 writer may create it. [S1]
 
 ## New suite: ess-conformance/5
 
@@ -74,7 +74,7 @@ coverage has exactly the following fields. Every field is emitted, including emp
 | authored | Sorted distinct selected authored ScenarioIds |
 | outside | Sorted list of Outside records |
 | refused | Sorted list of Refusal records |
-| authored_sources | Map from repository/input-root-relative source identity to sha256: plus 64 lowercase hex digits, hashing each exact authored file's bytes |
+| authored_sources | Map from repository/input-root-relative source identity to {digest, scenario, disposition}; digest hashes the exact file bytes as sha256: plus 64 lowercase hex digits, scenario is its known ScenarioId or null, and disposition is accepted or refused |
 | counts | Object with generated, authored, outside, refused; unsigned counts equal the corresponding list lengths |
 
 Selection is exactly:
@@ -83,21 +83,42 @@ Selection is exactly:
 - origins: one of generated, authored, generated_and_authored.
 - filter: {kind: all}, or {kind: explicit, ids: [ScenarioId], parent: SuiteReference}. Explicit IDs are sorted/distinct; parent is the exact admitted parent suite, not merely its model digest.
 
-For generated or combined selections, the inventory includes every generated obligation within the stated component/system boundary. For authored or combined selections, authored_sources includes every requested source file, including files whose compilation was refused. Selecting zero authored files is explicit; it does not claim the repository has no authored requirements. The builder must record the actual resolved source set instead of relying on a later process's working directory.
+For generated or combined selections, the inventory includes every generated obligation within the stated component/system boundary. For authored or combined selections, authored_sources includes every requested source file, including files whose compilation was refused. Selecting zero authored files is explicit; it does not claim the repository has no authored requirements. The builder must record the actual resolved source set instead of relying on a later process's working directory. Each accepted authored source has a nonnull ID identifying exactly one selected or outside authored scenario; exactly one accepted source owns each such ID. Refused sources retain the ID when known, even when another source with that ID was accepted, and have at least one refusal naming that source. Disposition is the final inventory result after merging, not merely compile_one success: a successfully compiled candidate rejected by a later merge is refused. Duplicate source identities with conflicting bytes refuse admission. No accepted source, refused source or known ID is erased to make the lists disjoint.
 
-An Outside record is exactly {scenario, origin, reason, needs}: scenario is a ScenarioId; origin is generated or authored; reason is other_component or selection_filter; needs is a sorted/distinct list of existing EssSemanticRefs, empty only for selection_filter. It represents a known executable candidate intentionally outside the selection, not an adapter failure. Explicit filtering records omitted parent IDs as selection_filter and retains the parent's existing outside/refusal inventory. A filter must not hide an in-scope refusal or turn unknown parent knowledge into complete_inventory.
+An Outside record is exactly {scenario, origin, reason, needs}: scenario is a ScenarioId; origin is generated or authored; reason is other_component, origin_selection or selection_filter; needs is a sorted/distinct list of existing EssSemanticRefs, nonempty for other_component and empty otherwise. origin_selection records a known candidate whose origin is excluded by a newly built origin selection. It represents a known executable candidate intentionally outside the selection, not an adapter failure. It can have a refusal beside it; the two records answer different questions.
 
-A Refusal record is exactly {origin, scenario, subject, source, code, message}. origin is generated or authored; scenario is ScenarioId or null; subject is EssSemanticRef or null; source is an authored-source identity or null; code is the closed code vocabulary of the corresponding existing synthesis/authored diagnostic producer at the baseline; message is nonempty diagnostic text. Generated refusals require subject and null source; authored refusals require source and may have null subject. Persist the existing diagnostic code spelling, not a newly guessed cause. The implementation must exhaustively map every variant of the baseline RefusalCause/Cause code-producing match; a later unrecognized code requires a new supported vocabulary/format, not silent omission. This is a typed diagnostic summary, with no arbitrary details bag. It deliberately does not pretend all refusal records have one scenario identity. [S7]
+A Refusal record is exactly **{origin, scenario, subject, source, code, message, effect, retained, scope, needs}**:
+
+| Field | Closed type and meaning |
+| --- | --- |
+| origin, scenario, subject, source | Generated or authored origin; known ScenarioId or null; existing EssSemanticRef or null; authored-source identity or null. Preserve every known identity. Generated refusals require subject and null source; authored refusals require the rejected source identity and may have null subject. |
+| code, message | Existing producer diagnostic code spelling and nonempty diagnostic text, including the original cause text; code is from the closed baseline RefusalCause/Cause vocabulary. |
+| effect | check_not_emitted or candidate_not_emitted. This describes what was omitted, independently of whether a runnable scenario with the same ID survived. |
+| retained | null if no selected/outside scenario has that ID; otherwise exactly {origin, source}, naming the surviving scenario's origin and accepted authored source identity, or null source for a generated survivor. It references the same scenario ID; it does not invent an execution result for the rejected candidate or claim that the survivor checks the omitted obligation. |
+| scope | in_scope, outside_component or outside_origin, under the rules below; this is coverage applicability, not execution status. |
+| needs | Sorted/distinct existing EssSemanticRefs establishing other-component requirements when scope is outside_component; empty otherwise. |
+
+The baseline cause-to-effect mapping is exhaustive, not a code-name heuristic. **ViewUndecidable, OrderUnwitnessed, InvariantUnobservable and RefusalUndeclared** map to check_not_emitted. **NoWitness, GuardUnevaluable, GuardUnsatisfiable, InstanceRequired, NotSynthesisedYet, BindingUnobservable, ValueInvariantUnwitnessed, DuplicateScenario, StrategyWithoutGuard and WitnessRejected** map to candidate_not_emitted. Every authored Cause variant maps to candidate_not_emitted, because authored compilation does not admit a partial file. InvariantUnobservable may have retained null when every invariant was unobservable: holds_after then emits no scenario, while still preserving each missing-check refusal. The effect must not be inferred solely from final ID membership. [S7; synthesize.rs:502–526, 2910–2976; authored.rs:1049–1090, 1416–1483]
+
+DuplicateScenario keeps the first generated scenario. Authored Duplicate keeps the first accepted file in deterministic source order; the refusal's source is the later rejected file and retained.source is the existing Cause::Duplicate.first identity. Both source digests stay in authored_sources. The same representation covers a non-duplicate authored compile failure followed by a successful different file with the same ID: the rejected file contributes a candidate_not_emitted refusal and the successful file is retained. Never synthesize an absent-scenario claim merely because compilation refused one candidate. Generated/authored merge collisions likewise retain the established scenario and attach a rejected-candidate diagnostic with both known origins. Future mapping must preserve the baseline code and its cause text, exhaustively cover the producing matches, and fail when a variant lacks a mapping; it cannot erase the ID, omit a refusal, discard the useful scenario or relabel a code to fit the schema.
+
+Scope is assigned against the declared scope/origins **before explicit filtering**:
+
+1. A refusal whose origin is excluded is outside_origin. It remains in a retained parent inventory; it cannot qualify a broader origin selection.
+2. Within a component selection, a refusal is outside_component only when the builder establishes that its whole refused candidate/obligation requires another component and records the nonempty needs. A retained scenario's matching other_component entry can establish this for an omitted check known to belong to that surviving candidate. A rejected or absent candidate requires its own resolved dependency information: an outside survivor with the same ID does not prove that a duplicate candidate requires the same component. A subject name alone, an unknown authored source or missing dependency information cannot establish exclusion: use in_scope conservatively. Every known outside-component refusal remains visible.
+3. All other refusals are in_scope, including omitted checks beside selected scenarios, rejected duplicate candidates for selected IDs, and unlocatable source/construct refusals. A system selection never uses outside_component.
+4. An explicit filter may only narrow its admitted parent's IDs, not change its scope or origins. It copies all parent refusals, their effects, retained identities, scope and needs unchanged; copies the full authored_sources map; retains previous outside entries; and adds omitted selected IDs as selection_filter. Thus a scenario moved outside by a filter can still have an in_scope refusal beside it. Filtering its ID out cannot turn the parent's incomplete claim into a complete one. Component/origin changes require a newly built selection with justified scope assignments, not a filter rewrite.
 
 The following invariants are binding:
 
-1. generated and authored are disjoint and their union is exactly the scenario-map keys. Every selected scenario has one origin; adding an authored scenario under a generated ID is a refusal, not replacement.
-2. IDs in outside are unique and absent from selected IDs. A refused scenario ID, when present, is absent from selected/outside IDs. Multiple distinct refusal records can concern the same refused ID; their count measures records. Exact duplicate refusal records are refused. Sort outside by scenario then origin/reason; sort refused by origin, source, subject, scenario, code, message, with null before text.
-3. counts.generated + counts.authored equals the suite's scenario count. outside/refused do **not** contribute to execution total. Do not sum refusal-record counts into a supposed scenario universe: one construct may have no single ID or several diagnostic records.
-4. Complete coverage of the declared selection requires complete_inventory and zero refused records within that selection. Intentional outside entries do not make a correctly declared component/explicit selection incomplete.
-5. Generated-only, authored-only, component, and filtered selections never become whole-system combined coverage merely because their own inventory is complete.
-6. An imported legacy suite cannot acquire complete_inventory from its nonzero scenario count or its provenance. Keep its legacy suite version and use unknown coverage in a diagnostic v2 report; do not manufacture v5 generated/authored origins by conversion. Rebuilding from the source/model inventory creates a new v5 suite and digest. An external v5 producer that knows scenario origins but cannot establish the full inventory must use unknown.
-7. Unknown component, duplicate scenario IDs or JSON object keys, inconsistent inventory/counts, unrecognized vocabulary, and an explicit filter whose parent/IDs cannot be validated refuse suite admission. Coverage claims are builder claims; bytes alone cannot prove that a builder enumerated all semantic obligations. Qualification additionally requires the caller's approved exact suite/selection.
+1. generated and authored are disjoint and their union is exactly the scenario-map keys. Every selected scenario has one origin; adding an authored candidate under an already retained ID is a refusal, not replacement.
+2. IDs in outside are unique and absent from selected IDs. **Refusal scenario IDs need not be disjoint from either set.** For every nonnull refusal ID, retained is nonnull exactly when the ID is selected or outside, and identifies that surviving scenario and its accepted source. Otherwise retained is null. Null scenario requires null retained. A check_not_emitted with no survivor is valid, including the all-invariants-unobservable case.
+3. Multiple distinct refusal records may concern one ID or source; their count measures records. Exact duplicate records are refused. Sort outside by scenario then origin/reason; sort refused by origin, source, subject, scenario, code, effect, retained.origin, retained.source, scope, needs, message, with null before text and needs compared lexicographically.
+4. counts.generated + counts.authored equals the selected scenario-map size. A retained selected scenario contributes exactly one terminal execution result regardless of how many refusals name it; a retained outside scenario contributes none. Rejected candidates and missing checks contribute no execution result or outcome category. counts.refused includes all refusal records, including outside_component/outside_origin; it is never an additional scenario or failure count.
+5. Complete coverage requires complete_inventory and zero refusals with scope in_scope. An all-pass selected scenario with an in_scope omitted check or rejected candidate leaves conformance_status inconclusive. Outside scenarios and correctly justified outside-component/origin refusals do not make a complete named selection incomplete; they still prevent its result from satisfying a broader expectation.
+6. Generated-only, authored-only, component, and filtered selections never become whole-system combined coverage merely because their own inventory is complete.
+7. An imported legacy suite cannot acquire complete_inventory from its nonzero scenario count or its provenance. Keep its legacy suite version and use unknown coverage in a diagnostic v2 report; do not manufacture v5 generated/authored origins by conversion. Rebuilding from the source/model inventory creates a new v5 suite and digest. An external v5 producer that knows scenario origins but cannot establish the full inventory must use unknown.
+8. Unknown component, duplicate scenario IDs or JSON object keys, inconsistent inventory/counts/source ownership/retained identity/scope, unrecognized vocabulary, and an explicit filter whose parent/IDs or retained inventory cannot be validated refuse suite admission. Legitimate scenario/refusal coexistence is admitted for diagnostic execution, not refused as malformed. Coverage claims are builder claims; bytes alone cannot prove that a builder enumerated all semantic obligations or justified component exclusion. Qualification additionally requires the caller's approved exact suite/selection.
 
 These obligations are owned by story:review-conformance-coverage. Fresh synthesis, authored-only compilation, component filtering, CLI --path merging, Go emission and web emission must each preserve the same inventory. Legacy diagnostic execution retains the legacy suite and makes no inventory claim.
 
@@ -140,7 +161,7 @@ The closed object has the following fields; all are required. Unknown fields, un
 | execution_status | passed, failed or inconclusive, derived solely by the selected producer profile |
 | counts | Exactly total, passed, failed, error, unsupported, skipped; nonnegative u64 |
 | outcomes | Exactly passed, failed, error, unsupported, skipped; each is a sorted/distinct array of plain ScenarioIds, with no status prefix |
-| coverage | {knowledge: unknown} for legacy suites, or {knowledge: complete_inventory or unknown, selection, counts, refused} copied exactly from the admitted v5 suite; refused is the full refusal list |
+| coverage | {knowledge: unknown} for legacy suites, or {knowledge: complete_inventory or unknown, selection, counts, refused} copied exactly from the admitted v5 suite; refused is the full list including effect, retained identity, scope and needs, so outside refusals remain visible |
 | conformance_status | passed, failed or inconclusive under complete-selection/1 below |
 | policy | Exactly complete-selection/1 |
 | completed_at | Existing signed epoch-millisecond timestamp meaning |
@@ -155,7 +176,7 @@ complete-selection/1 determines conformance_status in this order:
 
 1. execution_status failed yields failed, even if coverage is incomplete.
 2. Otherwise execution_status inconclusive yields inconclusive.
-3. Otherwise unknown inventory, any in-scope refusal, or total = 0 yields inconclusive.
+3. Otherwise unknown inventory, any refusal with scope in_scope (including beside a selected or filtered-out scenario), or total = 0 yields inconclusive.
 4. Otherwise it yields passed **for the declared selection only**.
 
 An evidence consumer must additionally match its expected model digest, exact SuiteReference and selection. An authored-only passed result cannot satisfy a consumer asking for system generated_and_authored; a component passed result cannot satisfy a whole-system requirement. A report with unknown coverage is useful diagnostic history, never proof of complete coverage. Paired report admission recomputes counts/outcome-ID membership and v5 coverage equality from the exact suite; structural report parsing alone must expose an unverified state and must not yield a qualifying evidence value.
@@ -164,15 +185,39 @@ No missing-suite or missing-coverage inference is allowed. Report v2 over legacy
 
 ## CLI surfaces, strict mode and version admission
 
-The implementation will add explicit opt-ins **--suite-format 5** on suite-producing verbs and **--report-format 2** on conform run. Generated Go report selection uses **ESS_REPORT_FORMAT=2**. These select versions, not JSON versus YAML. Until the final rollout step, defaults remain suite/4 and report/1.
+Suite-producing verbs and conform run --path gain **--suite-format 4|5**; conform run gains **--report-format 1|2**. Generated Go selects the report with **ESS_REPORT_FORMAT=1|2**. These select versions, not JSON versus YAML. Loading a suite with --suite or embedding it in a Go package preserves its actual version; a suite-format option cannot relabel or downcast it. The frozen fresh legacy writer emits suite/4; existing suite/1–3 documents remain admitted byte-for-byte, not recreated by an invented old-major encoder.
+
+Version and strictness selection is **independent, followed by a mandatory pairing check**. There is no automatic promotion to report/2 when a v5 suite or --strict is selected, and no automatic disabling of strict mode when report/1 is selected. Resolve explicit controls and defaults first, then apply the complete table below before target construction, identity lookup, callbacks or report writes. Rust and generated Go must implement the same check, even when no report destination was requested. A refusal explains the required explicit report/2 or diagnostic override; it emits no conformance report.
+
+| Release phase of the CLI/generated runtime | Default fresh suite | Unset report selection | Unset strictness selection |
+| --- | --- | --- | --- |
+| Reader/opt-in writer releases, before default movement | suite/4 | report/1 | Diagnostic |
+| Coordinated default release and runtimes generated from it | suite/5 | report/2 | Strict |
+
+The runtime's generated release determines its defaults; an already generated Go package does not acquire new defaults by upgrading a separate ESS binary. Its embedded suite's version is still checked against the report it actually selects.
+
+| Pair | Actual suite | Resolved report | Resolved mode | Required outcome in Rust and generated Go |
+| --- | --- | --- | --- | --- |
+| P1 | 1–4 | 1 | Diagnostic | Execute with frozen legacy output/count/exit behavior; no coverage qualification. |
+| P2 | 1–4 | 1 | Strict | Refuse before execution: strict requires report/2; select report/2 or an explicit diagnostic override. |
+| P3 | 1–4 | 2 | Diagnostic | Execute paired exact bytes; v2 categories with unknown coverage; conformance_status cannot pass. |
+| P4 | 1–4 | 2 | Strict | Execute paired exact bytes; unknown coverage prevents success: failed execution yields failed, otherwise inconclusive; strict process exits nonzero. |
+| P5 | 5 | 1 | Either | Refuse before execution, whether report/1 was explicit or default; require report/2. Neither allow-incomplete nor a missing report destination permits a v5-to-v1 downcast. |
+| P6 | 5 | 2 | Diagnostic | Execute admitted exact selection; truthful v2 qualification, with producer execution exit behavior. |
+| P7 | 5 | 2 | Strict | Execute admitted exact selection; process succeeds only for conformance_status passed and the expected selection. |
+| P8 | Unsupported suite major or vocabulary | Any | Either | Refuse admission before execution, even if report selection itself is supported. |
+| P9 | Supported suite | Unsupported report major | Either | Refuse report selection before execution. |
+| P10 | Any | Any | Conflicting strict/allow-incomplete controls | Refuse invocation before execution. |
+
+Thus during opt-in rollout a Rust run of suite/5 requires --report-format 2, and an upgraded Go runtime embedding suite/5 requires ESS_REPORT_FORMAT=2; leaving the report option unset selects report/1 and P5 refuses. Selecting --strict or ESS_CONFORMANCE_STRICT=1 during that phase also requires explicit report/2; it never silently changes the report format. These are configuration refusals rather than failed, skipped or zero-case conformance results.
 
 The current conform run --format json/yaml output is the detailed S2 report, not S1 standalone output. Preserve that legacy structure and output bytes under report-format 1. For report-format 2, detailed output is a closed **ess-conformance-run/2** object with exactly format, summary, started_at, scenarios. summary is the complete v2 standalone report; scenarios is the existing ordered detailed scenario-result sequence with the chosen producer vocabulary, whose IDs/statuses must agree with summary.outcomes. JSON/YAML is presentation of that object; --report-out always writes the standalone JSON summary. No consumer may identify the surface solely from --format json. Update help, fixtures and downstream detailed-output consumers before switching defaults. Go produces only standalone output; browser replay produces neither form.
 
-Strict mode is **--strict** for the CLI and **ESS_CONFORMANCE_STRICT=1** for generated Go. It requires report v2 and a paired exact suite. It succeeds only for conformance_status passed and the caller's expected selection. In strict Rust mode, exits are 0 for passed, 1 for failed, 3 for inconclusive; admission/refused input exits nonzero before target execution. In strict Go mode, an inconclusive report must fail the enclosing test run even if all subtests were skipped. Existing report-format 1 behavior remains unchanged.
+Strict mode is **--strict** for the CLI and **ESS_CONFORMANCE_STRICT=1** for generated Go. It requires report v2 and a paired exact suite. It succeeds only for conformance_status passed and the caller's expected selection. In strict Rust mode, exits are 0 for passed, 1 for failed, 3 for inconclusive; admission/refused input exits nonzero before target execution. In strict Go mode, an inconclusive report must fail the enclosing test run even if all subtests were skipped. Report-format 1 retains its historical execution/output behavior only in an admitted diagnostic pairing (P1); a strict/v1 configuration is refused before execution (P2/P5).
 
-Opt-in v2 without strict mode may execute incomplete/legacy selections diagnostically and return the producer execution exit behavior; its conformance_status remains truthful. Once the governed default moves to suite/5 and report/2, strict mode becomes the default; **--allow-incomplete** or **ESS_CONFORMANCE_ALLOW_INCOMPLETE=1** is an explicit diagnostic override. Conflicting strict/allow-incomplete choices refuse. This override never changes a report's qualification fields.
+Opt-in v2 without strict mode may execute incomplete/legacy selections diagnostically and return the producer execution exit behavior; its conformance_status remains truthful. **--allow-incomplete** or **ESS_CONFORMANCE_ALLOW_INCOMPLETE=1** explicitly chooses diagnostic mode in either release phase. Once suite/5, report/2 and strict become defaults, selecting legacy report/1 alone still resolves strict and refuses. The complete legacy opt-out is an actual suite/1–4 plus --report-format 1 --allow-incomplete, or the corresponding Go package with ESS_REPORT_FORMAT=1 and ESS_CONFORMANCE_ALLOW_INCOMPLETE=1; fresh legacy production also selects --suite-format 4. An actual v5 suite remains incompatible with v1 under every override. A v2 report over a legacy suite may be run in default strict mode (P4) but cannot succeed; --allow-incomplete selects its diagnostic P3 behavior. Explicit strict and allow-incomplete controls together refuse. Go's strictness controls accept only unset or 1; other values refuse as invalid configuration. The diagnostic override never changes qualification fields or bypasses version admission.
 
-Before any target callback, every new reader must check exact supported membership, vocabulary appropriate to that suite major, closed envelopes, duplicate keys/IDs, semantic invariants, and expected byte digest where supplied. The supported set is 1, 2, 3, 4, 5; a syntactically valid 6 or 99 is refused. Known newer steps under a falsely older major are refused. Legacy fixture admission remains version-specific and keeps the earlier meanings/omissions; legacy Node payload maps remain open data. Unknown typed fields on legacy input are refused by the new admitted-execution path, although historical readers sometimes ignored them.
+Before any target callback, every new reader must check exact supported membership, vocabulary appropriate to that suite major, closed envelopes, duplicate keys/IDs, semantic invariants, and expected byte digest where supplied. The admitted-execution supported set is 1, 2, 3, 4, 5; a syntactically valid 6 or 99 is refused. This set does not enlarge the frozen report/1 suite allowlist, which remains 1–4. Known newer steps under a falsely older major are refused. Legacy fixture admission remains version-specific and keeps the earlier meanings/omissions; legacy Node payload maps remain open data. Unknown typed fields on legacy input are refused by the new admitted-execution path, although historical readers sometimes ignored them.
 
 ## Validation matrix and implementation owners
 
@@ -192,7 +237,7 @@ These are specified expected outcomes, **not executed compatibility results**. R
 | M10 | Counts do not sum, overflow, duplicate IDs, list/count disagreement, or mixed profile vocabulary | Refuse v2 report | R/A |
 | M11 | Paired v2 with an omitted or extra scenario result | Refuse complete report, including results fabricated for filtered Go subtests | R/C |
 | M12 | Valid old suite/1–4 with its own supported vocabulary | New readers admit legacy execution; v2 exact report remains unknown coverage | C |
-| M13 | New suite/5 with complete inventory and every selected scenario passed | Paired v2 conformance_status passed for that selection | C/R |
+| M13 | New nonempty suite/5 with complete inventory, no in_scope refusal and every selected scenario passed | Paired v2 conformance_status passed for that selection | C/R |
 | M14 | Future suite/6 or /99 using only familiar steps | New readers refuse before any target callback or browser replay | C |
 | M15 | New vocabulary mislabeled with an earlier suite major | New readers refuse before execution | C |
 | M16 | Unknown typed suite field/step/coverage code or duplicate JSON key | New readers refuse before execution; payload-map keys remain data | C |
@@ -200,7 +245,7 @@ These are specified expected outcomes, **not executed compatibility results**. R
 | M18 | Today's browser player receives new suite metadata | Do not claim coverage/digest verification; publish the updated player with the matching suite first | C/O browser fixture |
 | M19 | Empty suite and zero outcomes | execution_status passed may remain; conformance_status inconclusive; strict refuses qualification | R/C/A |
 | M20 | Whole-system generated_and_authored, complete inventory, no refusals | Nonempty all-pass execution qualifies for that exact selection | C/R |
-| M21 | Component selection, complete in-scope inventory, known outside scenarios | Can qualify for the named component; cannot satisfy a system expectation | C/A |
+| M21 | Component selection, complete in-scope inventory, known outside scenarios and no in_scope refusal | Can qualify for the named component; cannot satisfy a system expectation | C/A |
 | M22 | Authored-only or generated-only selection | Can qualify only for the same declared origin selection; combined requirement refuses it | C/A |
 | M23 | Explicit subset with admitted parent, requested IDs and accounted omissions | Can qualify only for that explicit selection; broader requirement refuses it | C/A |
 | M24 | Explicit subset hides a refusal or loses parent/ID binding | Refuse suite admission; cannot claim complete inventory | C |
@@ -212,10 +257,32 @@ These are specified expected outcomes, **not executed compatibility results**. R
 | M30 | Go reduced execution struct is marshaled to calculate digest | Fixture must fail; only retained complete suiteJSON may be hashed | C/R |
 | M31 | Numeric precision edge, signed zero, combining Unicode, escaped controls | Golden canonical bytes and raw-byte hash match the specified profile; no silent normalization | C/R |
 | M32 | Detailed CLI JSON/YAML treated as standalone report | Format/surface admission refuses the mismatch | R/A |
-| M33 | Impact on a v5 component/subset suite with matching model provenance | Compute scenario invalidation within that selection; do not claim whole-system execution coverage | C |
+| M33 | Impact on a v5 component/subset suite with matching model provenance | Compute invalidation within the named selection and preserve the current ess-impact/2 envelope; do not claim whole-system execution coverage | C |
 | M34 | Existing realization digest reference without referenced bytes/profile bridge | Remain an artifact reference, not verified exact-suite conformance | C/O |
 | M35 | New report sent through old AEP domain result/predicates | Refuse v2 adaptation until the new domain/policy route exists; do not narrow the old failed fact | A |
-| M36 | Report/1 and suite/1–4 explicitly requested after default movement | Preserve documented legacy behavior/bytes and mark coverage unavailable | R/C/O |
+| M36 | Suite/1–4 plus explicit report/1 and explicit allow-incomplete after default movement | P1: preserve legacy output/count/exit behavior and bytes; coverage unavailable | R/C/O |
+| M37 | RefusalUndeclared beside a selected all-pass generated scenario | Admit check_not_emitted with that ID and retained generated identity; count one execution result plus one refusal; conformance_status inconclusive | C/R, inventory/report writers |
+| M38 | ViewUndecidable and OrderUnwitnessed beside the same selected all-pass scenario | Preserve both missing-check records; count one execution result and two refusals; conformance_status inconclusive | C/R, inventory/report writers |
+| M39 | InvariantUnobservable with some observable invariants and an all-pass survivor | Retain the useful scenario and each missing check; conformance_status inconclusive | C/R, inventory/report writers |
+| M40 | InvariantUnobservable with every invariant unobservable | Preserve check_not_emitted records and known ID with retained null; no result for that ID; selected all-pass or empty run remains inconclusive | C/R, inventory/report writers |
+| M41 | DuplicateScenario with first generated scenario selected and passing | Retain first scenario and candidate_not_emitted refusal at same ID; one execution result; conformance_status inconclusive | C/R, inventory/report writers |
+| M42 | Duplicate authored source after an accepted source, same selected ID, all-pass execution | Preserve both source digests, rejected source and accepted retained.source from Duplicate.first; one execution result; conformance_status inconclusive | C/R, authored inventory/report writers |
+| M43 | Authored non-duplicate compile refusal followed by a successful file with the same ID | Preserve refused/accepted source dispositions and known shared ID; retained points to accepted source; one execution result; in_scope refusal keeps conformance_status inconclusive | C/R, authored inventory/report writers |
+| M44 | Generated/authored merge candidate collides with an already retained ID | Preserve survivor origin, candidate origin and known source identities beside the refusal; no replacement or extra execution result | C, merge writer/admission |
+| M45 | Partial-check or duplicate-candidate refusal beside a known other-component outside scenario, with justified needs and complete_inventory; selected nonempty scenarios all pass | Retain scenario in outside and refusal with retained identity and outside_component scope; no result for outside ID; named component can qualify if no other in_scope gap exists | C/R, component inventory/report writers |
+| M46 | Refusal without sufficient dependency/source information to prove component exclusion | Retain as in_scope; selected all-pass execution is inconclusive | C/R, component inventory/report writers |
+| M47 | Explicit filter omits a parent scenario with an in_scope missing-check or candidate refusal | Move ID to selection_filter outside and retain refusal/identity/scope and source map unchanged; even all-pass retained execution is inconclusive | C/R, filter writer/admission |
+| M48 | Explicit filter removes a refusal, reclassifies its scope, or drops the accepted/rejected source binding | Refuse child admission against its exact parent before execution | C, filter admission |
+| M49 | Retained parent refusal has an origin excluded by an explicitly built origin selection | Keep visible as outside_origin; it does not block that origin-only selection but cannot satisfy a combined expectation | C/A, inventory/adaptation |
+| M50 | Suite/5 with explicit or unset report/1 during opt-in phase, including missing report destination | P5: refuse before any Rust target/Go newTarget or report write; require explicit report/2 | R/C, CLI/Go pairing gate |
+| M51 | Suite/5 with report/1 and allow-incomplete in either phase | P5: refuse before execution; override cannot discard v5 coverage | R/C, CLI/Go pairing gate |
+| M52 | Suite/1–4 with explicit/default report/1 and strict resolved explicitly in either phase or by the new default | P2: refuse before execution; no automatic report promotion | R/C, CLI/Go pairing gate |
+| M53 | Suite/1–4 with report/2 under default/explicit strict after movement | P4: execute with unknown coverage; failed execution yields failed, otherwise inconclusive; strict exits nonzero | R/C, CLI/Go pairing gate |
+| M54 | Suite/1–4 with report/2 and allow-incomplete in either phase | P3: diagnostic execution exit behavior; coverage remains unknown and cannot qualify | R/C, CLI/Go pairing gate |
+| M55 | v1 report names suite/5 after general execution support grows to include /5 | Frozen v1 reader refuses; v1 writer pairing prevents creation | R/A, report readers/writers |
+| M56 | Both strict and allow-incomplete are explicit, or Go strictness variable is neither unset nor 1 | Refuse invalid invocation before execution, regardless of otherwise valid pairing | R/C, CLI/Go pairing gate |
+| M57 | Upgraded runtime generated during the opt-in phase retains its old defaults after the standalone ESS binary upgrades | Resolve that runtime's actual defaults; embedded v5 with unset report stays refused under P5 until explicitly configured or regenerated | C/O, retained-runtime rollout fixture |
+| M58 | Current persisted impact result versus a proposed exact-suite impact envelope | Preserve ess-impact/2 format and existing fields/meanings; future exact-suite fields require a separately versioned impact envelope | C/O, impact migration |
 
 M17/M18 have a deployment outcome, not a invented statement that old code refuses. Their retained-runtime tests must demonstrate actual old behavior; a green test of a freshly regenerated runtime cannot stand in for that test.
 
@@ -225,22 +292,22 @@ AEP owns a new versioned evidence result and fact namespace for v2. Its old EssC
 
 Both aep-ess-evidence::adapt_json and aep-cli::recorded_from_report require version dispatch. The planning route must report the correct category counts and coverage scope, preserve exact references, and state when it records only descriptive evidence rather than qualifying facts. Updating prose must not claim that a lifecycle records count now evaluates conformance predicates; that is independently owned AEP behavior.
 
-Impact continues using semantic dependencies for invalidation. A v5 admitted-suite view supplies the same dependency graph plus its selection; missing coverage cannot be interpreted as an empty dependency set. No new field is added silently to impact/1 or its serialized SuiteProvenance. To issue a future persisted impact result that binds exact suites, the impact owner must version that envelope separately; until then callers pair the original suite externally and cannot reuse a report solely from model/contract provenance. [S9]
+Impact continues using semantic dependencies for invalidation. A v5 admitted-suite view supplies the same dependency graph plus its selection; missing coverage cannot be interpreted as an empty dependency set. The current persisted format is **ess-impact/2** (IMPACT_FORMAT at impact.rs:96); /2 introduced artifacts and optional suite/invalidation. Historical ess-impact/1 is not the current producer contract. No new field is added silently to ess-impact/2 or its serialized SuiteProvenance, and its existing bytes/meanings remain frozen. To issue a future persisted impact result that binds exact suites, the impact owner must version that envelope separately; until then callers pair the original suite externally and cannot reuse a report solely from model/contract provenance. [S9]
 
 Before new default bytes cross repositories, Atlas requires an ADR here in its own repository: **"ESS conformance report v2 and exact-suite coverage migration"**. The coordinator must allocate its ADR number. It must name R/C/A and the actual adopter repositories, generated Go runtimes, browser bundles, AEP detailed-output automation, and any discovered digest consumers; record the contract versions, fixtures, deployed/pinned versions and rollback path. This ADR and shipped log are required by [Atlas AGENTS.md at 6035d6e](https://github.com/beyond10x/atlas/blob/6035d6e1209686ca474a3f43975fde7d8621ba48/AGENTS.md#L297). No ADR approval or downstream completion is claimed here.
 
 The mandatory movement order is:
 
 1. **O: govern and inventory.** Record the Atlas ADR, relying parties, pinned old suites/reports/runtimes and exact upgrade dependencies. Keep legacy goldens and establish the matrix fixtures.
-2. **R/C: release readers first.** Ship strict version-specific admission and opt-in v2/v5 parsing with defaults still report/1 and suite/4. Update Rust entry points, Go template, browser, CLI detailed output, and impact's internal view; prove pre-execution refusal.
+2. **R/C: release readers first.** Ship strict version-specific admission and opt-in v2/v5 parsing with defaults still report/1 and suite/4. Update Rust entry points, Go template, browser, CLI detailed output, and impact's internal view; prove pre-execution refusal. Keep report/1 admission fixed at suite majors 1–4 independently of new suite execution support, and implement the P1–P10 pairing gate before writer opt-ins.
 3. **A: migrate both readers and policy.** Land the new AEP domain evidence contract/facts and predicates, then optional adapter and planning route, with legacy behavior retained. Consumer-side fixtures must cover skipped/error/unsupported, incomplete and empty coverage, and exact digest mismatch. Do not enable v2 adaptation before its domain representation is available.
 4. **C/O: regenerate and deploy consumers.** Regenerate adopter Go packages and web player/model/suite sets from the admitted-reader release; update automation using detailed CLI outputs. Inventory exact deployed versions. Old embedded runtimes remain on legacy suites until their own regeneration is verified.
-5. **R/C: enable opt-in writers.** Count migration implements the v2 outcome/profile contract; coverage migration implements v5 inventory and exact byte binding. Counts-only v2 output against legacy suites is diagnostic/unknown coverage. No default changes yet. Execute all matrix rows, legacy byte checks, paired cross-language vectors and required repository gates.
-6. **O/A/R/C: move defaults last.** After every mandatory relying party is ready and the ADR/shipped log records actual versions, release suite/5, report/2 and strict default behavior together. Publish migration instructions and explicit legacy-format opt-outs. Keep old bytes and format meanings frozen.
+5. **R/C: enable opt-in writers.** Count migration implements the v2 outcome/profile contract; coverage migration implements v5 inventory and exact byte binding. Counts-only v2 output against legacy suites is diagnostic/unknown coverage. Preserve partial-check and rejected-candidate inventories through every builder/filter, including retained source identities and explicit refusal scope. Suite/5 with an unset or explicit report/1 must refuse before execution; opt-in instructions require report/2 explicitly for both Rust and generated Go. No default changes yet. Execute all matrix rows, legacy byte checks, paired cross-language vectors and required repository gates.
+6. **O/A/R/C: move defaults last.** After every mandatory relying party is ready and the ADR/shipped log records actual versions, release suite/5, report/2 and strict default behavior together. Publish migration instructions and the complete legacy opt-out (actual suite/1–4, explicit report/1, explicit allow-incomplete); report/1 alone is not an opt-out from the strict default. Record regenerated Go runtime defaults as well as the standalone CLI version. Keep old bytes and format meanings frozen.
 7. **O: observe and retain rollback.** Roll back writer defaults, not contract meanings or previously issued documents, if a relying reader is missed. Never relabel new data as v1 or feed v5 to unupgraded generated code to make a gate green.
 
 The existing planning stories own implementation, not this document. Missing Atlas/AEP/adopter work must become explicit coordinator-owned dependencies before step 6. This design closes the choice of ESS contracts and migration policy; it does not discharge those obligations.
 
 ## Validation status
 
-This unit reviewed the source inventory and all 36 matrix rows for explicit outcomes and implementation owners. No compatibility fixture, canonicalization test, package test, formatter, Clippy run, browser check, AEP adaptation or downstream release was executed for this document. The sole repository check required here is git diff --check; its actual result is retained in the implementation report. The integration coordinator owns full gates and any required site build. Implementation acceptance requires the matrix to become executed checks rather than treating this design review as that evidence.
+This unit reviewed the source inventory, all 58 matrix rows and the P1–P10 pairing table for explicit outcomes and implementation owners. No compatibility fixture, canonicalization test, package test, formatter, Clippy run, browser check, AEP adaptation or downstream release was executed for this document. The sole repository check required here is git diff --check; its original result is retained in the implementation report and the correction result in correction-report.md. The integration coordinator owns full gates and any required site build. Implementation acceptance requires the matrix to become executed checks rather than treating this design review as that evidence.
