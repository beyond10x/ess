diff --git a/docs/design/review-conformance-coverage.md b/docs/design/review-conformance-coverage.md
index ca1bd67..3e410e6 100644
--- a/docs/design/review-conformance-coverage.md
+++ b/docs/design/review-conformance-coverage.md
@@ -21,7 +21,7 @@ AEP citations are Git-object reads at **00c742e4179593738a2e8aa69e2ecc07d3c89402
 | Ref | Inspected owner and source | Existing behavior and required disposition |
 | --- | --- | --- |
 | S1 | [evidence.rs](../../crates/verify/ess-conformance/src/evidence.rs), lines 12–164 | Rust standalone v1 writer counts every non-pass in scenarios_failed and writes status-prefixed strings. The current validated Serde reader is closed, admits report/1 and supported suite majors 1–4, and checks counts/list length and aggregate status. Preserve its valid v1 bytes and meanings. Freeze v1's suite-major allowlist at 1–4 independently of the growing execution allowlist; add a separate v2 type/admission path. |
-| S2 | [report.rs](../../crates/verify/ess-conformance/src/report.rs), lines 40–80 and 535–611 | Rust scenario precedence and whole-run aggregation differ; detailed ConformanceReport is a separate serializable structure. Preserve both execution rules and the legacy detailed CLI surface. |
+| S2 | [report.rs](../../crates/verify/ess-conformance/src/report.rs), lines 40–80 and 535–611 | Rust scenario precedence and whole-run aggregation differ; detailed ConformanceReport is a separate serializable structure. Preserve both execution rules and the legacy detailed CLI surface. Its started_at/completed_at use the existing unsigned Timestamp; see S11 for the producer-specific timestamp boundary. |
 | S3 | [scenario.rs](../../crates/verify/ess-conformance/src/scenario.rs), lines 101–208, 236–309 and 365–427 | Suites have provenance and an ordered scenario map. component is omitted when absent. CURRENT is 4, supported majors are 1–4, but SuiteFormat deserialization checks syntax rather than membership. from_json does not add admission. Add supported-version/vocabulary checks at every execution entry, including generic Serde routes. |
 | S4 | [runner.rs](../../crates/verify/ess-conformance/src/runner.rs), lines 286–310 | Runner::run accepts a typed suite without checking its version, records every scenario, and aggregates their final statuses. Add an admitted-suite boundary before target identity or scenario callbacks. |
 | S5 | [Go emitter](../../crates/verify/ess-conformance/src/go/mod.rs), lines 49–77; [runtime.go](../../crates/verify/ess-conformance/src/go/runtime.go), lines 383–406, 499–639, 686–704, 759, 1082 and 1344–1356 | Emitter embeds exact suite.json. Runtime decodes a reduced struct, omitting scenario dependency and other metadata, and never checks SuiteVersion. It uses passed/failed/skipped, ordinary errors fail, unsupported observations or unknown step vocabulary skip, and teardown can turn a skip into failed. Regenerate adopter runtimes, add pre-execution admission, and hash retained complete input bytes. |
@@ -33,6 +33,7 @@ AEP citations are Git-object reads at **00c742e4179593738a2e8aa69e2ecc07d3c89402
 | A1 | [AEP optional adapter](https://github.com/beyond10x/aep/blob/00c742e4179593738a2e8aa69e2ecc07d3c89402/crates/observe/aep-ess-evidence/src/lib.rs#L15), lines 15–32 and 138–167 | aep-ess-evidence transcribes a closed report/1, checks count consistency, and transfers scenarios_failed unchanged into AEP's existing result. Versioned v2 parsing and domain mapping are separately required. |
 | A2 | [AEP planning reader](https://github.com/beyond10x/aep/blob/00c742e4179593738a2e8aa69e2ecc07d3c89402/crates/edge/aep-cli/src/planning.rs#L5907), lines 5907–5985 | recorded_from_report independently reads JSON Value, requires report/1, refuses zero total, and incorporates scenarios_failed into recorded prose. It does not call A1 or preserve all execution facts. It must gain its own strict version dispatch and truthful category/coverage wording; updating A1 alone does not migrate it. |
 | A3 | [AEP domain evidence](https://github.com/beyond10x/aep/blob/00c742e4179593738a2e8aa69e2ecc07d3c89402/crates/govern/aep-domain/src/evidence.rs#L1006), lines 1006–1051 and 1862–1889 | EssConformanceResult is closed. Existing facts define ess_conformance.passed from status.is_pass() && scenarios_failed == 0 and expose scenarios.failed. Preserve those historical meanings. A new domain result/fact namespace and predicate migration are required; wire parsing alone cannot change this boundary. |
+| S11 | [time.rs](../../crates/specify/ess-primitives/src/time.rs), lines 25–58; [runner.rs](../../crates/verify/ess-conformance/src/runner.rs), lines 116–155; [runtime.go](../../crates/verify/ess-conformance/src/go/runtime.go), lines 581–637 | Legacy Rust Timestamp is a transparent u64, and AdvancingClock accepts the full u64 start range. Legacy Go CompletedAt is int64 from UnixMilli. Preserve their distinct v1 writer/reader domains; the shared exact unsigned timestamp contract below applies only to new report/2 and run/2. |
 | T1 | [CLI Go tests](../../crates/edge/ess-cli/tests/go_conformance.rs), lines 109–112; [conformance tests](../../crates/verify/ess-conformance/tests/); [impact tests](../../crates/verify/ess-diff/tests/impact.rs) | Go reports are read through S1; suite serialization, execution, fault matrices, impact, generated examples and browser bundles rely on the existing shapes. Keep legacy golden fixtures and add paired new fixtures. No separate production report parser was found in repository workflow/xtask sources. |
 
 An external adopter may retain an older generated Go package indefinitely. That relying party is not upgraded by releasing ESS. The Atlas ADR must enumerate the actual repositories and deployed consumers discovered at rollout, including their pinned generated packages and automation reading detailed CLI JSON/YAML; this source inventory is the known minimum, not an assertion that every external adopter has been discovered.
@@ -60,6 +61,8 @@ For either producer, zero terminal scenarios currently aggregates to passed. Fre
 
 The current validated v1 reader accepts both non-pass vocabularies because v1 carries no producer profile. It does not establish exact-suite binding, complete coverage, or uniqueness of opaque listed scenario IDs. Do not strengthen those unknown historical claims by upgrading a report. Keep the existing v1 shape and admitted meanings separate from v2's stronger invariants. Its suite_version admits exactly ess-conformance/1, /2, /3 and /4 forever under report/1. The future v1 reader must use that frozen allowlist, not the general SuiteFormat::is_supported helper after it grows to include /5. A v1 document naming suite/5 is refused by Rust and every migrated reader; no v1 writer may create it. [S1]
 
+Legacy timestamps do not have one shared signed domain: Rust's standalone completed_at and detailed started_at/completed_at use Timestamp(u64), while Go's standalone completed_at uses int64 from time.Now().UnixMilli(). Freeze those v1 writers and readers, including Rust's refusal of negative timestamp input and preservation of values above i64::MAX. Do not widen a legacy reader, narrow a valid Rust timestamp or reinterpret negative Go output to create retrospective cross-producer agreement. [S1, S2, S5, S11]
+
 ## New suite: ess-conformance/5
 
 The closed top-level object has exactly **provenance**, **scenarios**, and **coverage**. provenance has the existing fields in S3, with suite_version fixed to ess-conformance/5. scenarios retains the v4 scenario/step/dependency vocabulary and its ID-keyed map. Version 5 adds no execution step semantics. Unknown fields are refused throughout typed envelopes and tagged variants; arbitrary keys inside an existing declared payload Node remain payload data, not unknown envelope fields.
@@ -73,7 +76,7 @@ coverage has exactly the following fields. Every field is emitted, including emp
 | generated | Sorted distinct selected generated ScenarioIds |
 | authored | Sorted distinct selected authored ScenarioIds |
 | outside | Sorted list of Outside records |
-| refused | Sorted list of Refusal records |
+| refused | Sorted list of Refusal occurrences, preserving multiplicity even when records are identical |
 | authored_sources | Map from repository/input-root-relative source identity to {digest, scenario, disposition}; digest hashes the exact file bytes as sha256: plus 64 lowercase hex digits, scenario is its known ScenarioId or null, and disposition is accepted or refused |
 | counts | Object with generated, authored, outside, refused; unsigned counts equal the corresponding list lengths |
 
@@ -100,25 +103,27 @@ A Refusal record is exactly **{origin, scenario, subject, source, code, message,
 
 The baseline cause-to-effect mapping is exhaustive, not a code-name heuristic. **ViewUndecidable, OrderUnwitnessed, InvariantUnobservable and RefusalUndeclared** map to check_not_emitted. **NoWitness, GuardUnevaluable, GuardUnsatisfiable, InstanceRequired, NotSynthesisedYet, BindingUnobservable, ValueInvariantUnwitnessed, DuplicateScenario, StrategyWithoutGuard and WitnessRejected** map to candidate_not_emitted. Every authored Cause variant maps to candidate_not_emitted, because authored compilation does not admit a partial file. InvariantUnobservable may have retained null when every invariant was unobservable: holds_after then emits no scenario, while still preserving each missing-check refusal. The effect must not be inferred solely from final ID membership. [S7; synthesize.rs:502–526, 2910–2976; authored.rs:1049–1090, 1416–1483]
 
-DuplicateScenario keeps the first generated scenario. Authored Duplicate keeps the first accepted file in deterministic source order; the refusal's source is the later rejected file and retained.source is the existing Cause::Duplicate.first identity. Both source digests stay in authored_sources. The same representation covers a non-duplicate authored compile failure followed by a successful different file with the same ID: the rejected file contributes a candidate_not_emitted refusal and the successful file is retained. Never synthesize an absent-scenario claim merely because compilation refused one candidate. Generated/authored merge collisions likewise retain the established scenario and attach a rejected-candidate diagnostic with both known origins. Future mapping must preserve the baseline code and its cause text, exhaustively cover the producing matches, and fail when a variant lacks a mapping; it cannot erase the ID, omit a refusal, discard the useful scenario or relabel a code to fit the schema.
+DuplicateScenario keeps the first generated scenario. Authored Duplicate keeps the first accepted file in deterministic source order; the refusal's source is the later rejected file and retained.source is the existing Cause::Duplicate.first identity. Both source digests stay in authored_sources. The same representation covers a non-duplicate authored compile failure followed by a successful different file with the same ID: the rejected file contributes a candidate_not_emitted refusal and the successful file is retained. Never synthesize an absent-scenario claim merely because compilation refused one candidate. Generated/authored merge collisions likewise retain the established scenario and attach a rejected-candidate diagnostic with both known origins. Future mapping must preserve every producer occurrence, the baseline code and its cause text, exhaustively cover the producing matches, and fail when a variant lacks a mapping; it cannot erase the ID, omit a refusal, discard the useful scenario or relabel a code to fit the schema.
 
 Scope is assigned against the declared scope/origins **before explicit filtering**:
 
 1. A refusal whose origin is excluded is outside_origin. It remains in a retained parent inventory; it cannot qualify a broader origin selection.
 2. Within a component selection, a refusal is outside_component only when the builder establishes that its whole refused candidate/obligation requires another component and records the nonempty needs. A retained scenario's matching other_component entry can establish this for an omitted check known to belong to that surviving candidate. A rejected or absent candidate requires its own resolved dependency information: an outside survivor with the same ID does not prove that a duplicate candidate requires the same component. A subject name alone, an unknown authored source or missing dependency information cannot establish exclusion: use in_scope conservatively. Every known outside-component refusal remains visible.
 3. All other refusals are in_scope, including omitted checks beside selected scenarios, rejected duplicate candidates for selected IDs, and unlocatable source/construct refusals. A system selection never uses outside_component.
-4. An explicit filter may only narrow its admitted parent's IDs, not change its scope or origins. It copies all parent refusals, their effects, retained identities, scope and needs unchanged; copies the full authored_sources map; retains previous outside entries; and adds omitted selected IDs as selection_filter. Thus a scenario moved outside by a filter can still have an in_scope refusal beside it. Filtering its ID out cannot turn the parent's incomplete claim into a complete one. Component/origin changes require a newly built selection with justified scope assignments, not a filter rewrite.
+4. An explicit filter may only narrow its admitted parent's IDs, not change its scope or origins. It copies all parent refusal occurrences with their full multiplicity, effects, retained identities, scope and needs unchanged; copies the full authored_sources map; retains previous outside entries; and adds omitted selected IDs as selection_filter. Thus a scenario moved outside by a filter can still have an in_scope refusal beside it. Filtering its ID out cannot turn the parent's incomplete claim into a complete one. Component/origin changes require a newly built selection with justified scope assignments, not a filter rewrite.
 
 The following invariants are binding:
 
 1. generated and authored are disjoint and their union is exactly the scenario-map keys. Every selected scenario has one origin; adding an authored candidate under an already retained ID is a refusal, not replacement.
 2. IDs in outside are unique and absent from selected IDs. **Refusal scenario IDs need not be disjoint from either set.** For every nonnull refusal ID, retained is nonnull exactly when the ID is selected or outside, and identifies that surviving scenario and its accepted source. Otherwise retained is null. Null scenario requires null retained. A check_not_emitted with no survivor is valid, including the all-invariants-unobservable case.
-3. Multiple distinct refusal records may concern one ID or source; their count measures records. Exact duplicate records are refused. Sort outside by scenario then origin/reason; sort refused by origin, source, subject, scenario, code, effect, retained.origin, retained.source, scope, needs, message, with null before text and needs compared lexicographically.
-4. counts.generated + counts.authored equals the selected scenario-map size. A retained selected scenario contributes exactly one terminal execution result regardless of how many refusals name it; a retained outside scenario contributes none. Rejected candidates and missing checks contribute no execution result or outcome category. counts.refused includes all refusal records, including outside_component/outside_origin; it is never an additional scenario or failure count.
-5. Complete coverage requires complete_inventory and zero refusals with scope in_scope. An all-pass selected scenario with an in_scope omitted check or rejected candidate leaves conformance_status inconclusive. Outside scenarios and correctly justified outside-component/origin refusals do not make a complete named selection incomplete; they still prevent its result from satisfying a broader expectation.
+3. refused is a **sorted list with multiplicity, not a set**: emit one array element per actual producer occurrence, including identical records concerning the same ID or source. Do not deduplicate, add an occurrence identity, or fabricate ordinal message suffixes. Sort outside by scenario then origin/reason; sort refused by origin, source, subject, scenario, code, effect, retained.origin, retained.source, scope, needs, message, with null before text and needs compared lexicographically. Equal complete sort keys give identical record bytes, so their relative order cannot change canonical bytes. Preserve full multiplicity whenever an inventory is retained through component/origin selection, explicit filtering, report coverage or parent equality checks. Selected/outside scenario IDs, authored-source map keys and report outcome IDs retain their existing uniqueness rules.
+4. counts.generated + counts.authored equals the selected scenario-map size. A retained selected scenario contributes exactly one terminal execution result regardless of how many refusals name it; a retained outside scenario contributes none. Rejected candidates and missing checks contribute no execution result or outcome category. counts.refused equals the array's occurrence count, including identical records and outside_component/outside_origin occurrences; it is never an additional scenario or failure count.
+5. Complete coverage requires complete_inventory and no refusal occurrence with scope in_scope; the predicate depends on existence, not the number of distinct records. An all-pass selected scenario with an in_scope omitted check or rejected candidate leaves conformance_status inconclusive. Outside scenarios and correctly justified outside-component/origin refusals do not make a complete named selection incomplete; they still prevent its result from satisfying a broader expectation.
 6. Generated-only, authored-only, component, and filtered selections never become whole-system combined coverage merely because their own inventory is complete.
 7. An imported legacy suite cannot acquire complete_inventory from its nonzero scenario count or its provenance. Keep its legacy suite version and use unknown coverage in a diagnostic v2 report; do not manufacture v5 generated/authored origins by conversion. Rebuilding from the source/model inventory creates a new v5 suite and digest. An external v5 producer that knows scenario origins but cannot establish the full inventory must use unknown.
-8. Unknown component, duplicate scenario IDs or JSON object keys, inconsistent inventory/counts/source ownership/retained identity/scope, unrecognized vocabulary, and an explicit filter whose parent/IDs or retained inventory cannot be validated refuse suite admission. Legitimate scenario/refusal coexistence is admitted for diagnostic execution, not refused as malformed. Coverage claims are builder claims; bytes alone cannot prove that a builder enumerated all semantic obligations or justified component exclusion. Qualification additionally requires the caller's approved exact suite/selection.
+8. Unknown component, duplicate scenario IDs or JSON object keys, inconsistent inventory/counts/source ownership/retained identity/scope, unrecognized vocabulary, and an explicit filter whose parent/IDs or retained inventory cannot be validated refuse suite admission. Legitimate scenario/refusal coexistence is admitted for diagnostic execution, not refused as malformed. Coverage claims are builder claims; bytes alone cannot prove that a builder enumerated all semantic obligations, preserved every original producer occurrence or justified component exclusion. Qualification additionally requires the caller's approved exact suite/selection.
+
+Repeated source constructs are a concrete multiplicity case: entity conversion retains invariant entries and holds_after pushes one refusal per unobservable invariant; authored Compiler::run visits each assertion and Compiler::refuse appends each refusal. Two identical unobservable invariants or undeclared-view assertions therefore remain two refusal elements with the same record bytes. The known scenario/source identity remains intact and neither occurrence adds an execution result. [ess-domain/src/entity.rs:1566; synthesize.rs:2945–2959; authored.rs:1543–1548, 1600–1602, 1893–1898]
 
 These obligations are owned by story:review-conformance-coverage. Fresh synthesis, authored-only compilation, component filtering, CLI --path merging, Go emission and web emission must each preserve the same inventory. Legacy diagnostic execution retains the legacy suite and makes no inventory claim.
 
@@ -137,16 +142,16 @@ Canonical generation is a writer requirement, distinct from byte identity:
 | Object fields | Existing typed field order and BTreeMap order; no rewrite | All object keys, including typed objects and payload maps, sorted by UTF-8 byte order |
 | Layout | Existing serde_json pretty form plus trailing LF | Two-space indentation, colon followed by one space, LF line endings, nonempty arrays/objects on multiple lines, empty arrays/objects as []/{}, exactly one trailing LF, no BOM |
 | Omission | Existing optional/default omissions, including absent provenance.component | Existing nested scenario/provenance omission rules remain; every newly defined coverage field is present and nullable fields use null |
-| Counts and integer fields | Existing valid spellings preserved | Unsigned decimal integers without exponent, sign or leading zero; bounded to u64 and checked before arithmetic |
+| Counts and integer fields | Existing valid spellings and producer-specific timestamp domains preserved | Unsigned decimal integers without exponent, sign or leading zero; bounded to u64 and checked before arithmetic. The new report/run timestamps use this same exact integer rule; payload numbers below remain a separate domain |
 | Payload numbers | Existing admitted finite Number semantics preserved | Finite binary64 values only for existing Node numbers; retain the numeric rendering behavior of serde_json 1.0.151 and its locked formatter at this baseline, including signed zero and integral-float spelling; do not coerce to integer or string |
 | Strings | Existing bytes preserved | UTF-8, no Unicode normalization; quote/backslash/control escapes as the baseline serde_json JSON encoder, printable Unicode literal, slash and HTML punctuation unescaped |
-| Ordered arrays | Existing order | Scenario steps retain execution order; sets/coverage lists use the orders stated here; no sorting of semantic sequences |
+| Ordered arrays | Existing order | Scenario steps retain execution order; sets/coverage lists use the orders stated here; refused is sorted with full multiplicity; no sorting of semantic sequences |
 
 The v5 numeric/string spelling is tied to the inspected encoder behavior, not to a vague "whatever JSON library is current": Cargo.lock pins serde_json 1.0.151 and zmij 1.0.23. A dependency change that changes output fails byte fixtures and requires an intentional format decision. The implementation must pin golden vectors for 1.0, -0.0, fractional/exponent boundaries, Unicode combining sequences and escaped control characters before releasing a writer. This document does not claim those vectors have run. [S3; crates/specify/ess-primitives/src/node.rs:35–57 and facts.rs:22–61]
 
 Readers admit valid noncanonical JSON spellings only after complete version/schema/semantic checks and duplicate-key detection; their exact digest remains the digest of the admitted original bytes. Thus a reader need not implement another language's canonical formatter to verify identity. For in-memory Rust suites, serialize once into an immutable byte buffer, admit/hash that buffer, and execute the corresponding admitted value. For Go, hash the complete embedded suiteJSON bytes before decoding into execution views; retain/validate the full envelope and coverage/dependency fields even if execution does not consume them. Never marshal the existing reduced Suite struct to calculate identity. Browser code retains response bytes before JSON parsing and verifies the selected suite/model before replay. [S4, S5, S8]
 
-New report and detailed-report writers use the same v5 layout/scalar rules for their own bytes, but their hashes, if any, are report-artifact hashes and never substitutes for SuiteReference.digest. Existing realization artifact digests retain their original profile. [S10]
+New report and detailed-report writers use the same v5 layout/scalar rules, including exact u64 timestamps as defined below, for their own bytes, but their hashes, if any, are report-artifact hashes and never substitutes for SuiteReference.digest. Existing realization artifact digests retain their original profile. [S10]
 
 ## New standalone report: ess-conformance-report/2
 
@@ -161,10 +166,18 @@ The closed object has the following fields; all are required. Unknown fields, un
 | execution_status | passed, failed or inconclusive, derived solely by the selected producer profile |
 | counts | Exactly total, passed, failed, error, unsupported, skipped; nonnegative u64 |
 | outcomes | Exactly passed, failed, error, unsupported, skipped; each is a sorted/distinct array of plain ScenarioIds, with no status prefix |
-| coverage | {knowledge: unknown} for legacy suites, or {knowledge: complete_inventory or unknown, selection, counts, refused} copied exactly from the admitted v5 suite; refused is the full list including effect, retained identity, scope and needs, so outside refusals remain visible |
+| coverage | {knowledge: unknown} for legacy suites, or {knowledge: complete_inventory or unknown, selection, counts, refused} copied exactly from the admitted v5 suite; refused is the full list with multiplicity, including effect, retained identity, scope and needs, so repeated and outside refusals remain visible |
 | conformance_status | passed, failed or inconclusive under complete-selection/1 below |
 | policy | Exactly complete-selection/1 |
-| completed_at | Existing signed epoch-millisecond timestamp meaning |
+| completed_at | Exact nonnegative epoch-millisecond u64, 0 through 18446744073709551615 inclusive, under the new timestamp rules below |
+
+**New report/run timestamp contract.** Every timestamp in report/2 and run/2 is a nonnegative epoch-millisecond integer in **0..=18446744073709551615**. This covers standalone completed_at, detailed started_at and summary.completed_at; any timestamp inside a detailed result follows the same rule. On the JSON wire use an unsigned decimal integer token: 0 or a nonzero digit followed by decimal digits, with no sign, leading zero, fraction or exponent. Do not encode it as a quoted string or apply the binary64 payload-number profile. YAML presentation preserves the same exact integer and range.
+
+Rust v2 writers retain the full Timestamp(u64) range. Go v2 wire fields use **uint64**, with a checked nonnegative conversion from the existing UnixMilli/int64 clock result; a negative clock value refuses v2 report production, emits diagnostics and exits nonzero without a complete report. It never wraps, clamps or silently switches to report/1. Clock values from 0 through i64::MAX convert exactly; the Go clock API's narrower production range does not narrow v2 reader admission of valid larger u64 values. This is a new shared contract, not a claim that the legacy producers already agree. [S11]
+
+New readers, including browser consumers and AEP adaptation, parse timestamp integer tokens exactly before range checks. Their wire reader must preserve 0, 9223372036854775807 (i64::MAX), 9223372036854775808 (i64::MAX + 1) and 18446744073709551615 (u64::MAX) without passing through binary64 or silently narrowing to i64. A browser reader uses an integer-preserving parser/representation; ordinary JSON.parse followed by Number-based conversion is insufficient. A pre-parsed value whose original integer precision cannot be established is not qualifying input: require the original bytes and exact parsing, or refuse that route. If a downstream date/time type or API has a narrower domain, perform an explicit checked conversion and refuse that adaptation when out of range; the original valid u64 bytes remain positive and valid under this wire contract.
+
+New reader and adaptation timestamp comparisons, subtraction, addition and duration conversions use exact checked integer arithmetic. Underflow, overflow or a narrowing conversion error refuses the affected report operation/adaptation instead of wrapping, truncating, saturating or qualifying a changed value. Negative values, values above u64::MAX and fractional/exponent/signed tokens are invalid v2 timestamp input, even when another numeric spelling would have the same mathematical value. The existing admission of noncanonical JSON layout does not relax timestamp token/range rules. None of these new rules changes v1 timestamp parsing, clocks, bytes or historical reader asymmetries.
 
 counts.total = passed + failed + error + unsupported + skipped. Each count equals the corresponding outcomes array length; the five arrays are pairwise disjoint. Their union must equal the exact suite's selected scenario IDs. Rust requires skipped = 0; Go requires error = unsupported = 0. An unknown/mixed producer profile or an outcome inadmissible for that profile is a malformed v2 report, not an inferred translation.
 
@@ -179,7 +192,7 @@ complete-selection/1 determines conformance_status in this order:
 3. Otherwise unknown inventory, any refusal with scope in_scope (including beside a selected or filtered-out scenario), or total = 0 yields inconclusive.
 4. Otherwise it yields passed **for the declared selection only**.
 
-An evidence consumer must additionally match its expected model digest, exact SuiteReference and selection. An authored-only passed result cannot satisfy a consumer asking for system generated_and_authored; a component passed result cannot satisfy a whole-system requirement. A report with unknown coverage is useful diagnostic history, never proof of complete coverage. Paired report admission recomputes counts/outcome-ID membership and v5 coverage equality from the exact suite; structural report parsing alone must expose an unverified state and must not yield a qualifying evidence value.
+An evidence consumer must additionally match its expected model digest, exact SuiteReference and selection. An authored-only passed result cannot satisfy a consumer asking for system generated_and_authored; a component passed result cannot satisfy a whole-system requirement. A report with unknown coverage is useful diagnostic history, never proof of complete coverage. Paired report admission recomputes counts/outcome-ID membership and v5 coverage equality, including the complete refusal occurrence list and its length, from the exact suite; structural report parsing alone must expose an unverified state and must not yield a qualifying evidence value.
 
 No missing-suite or missing-coverage inference is allowed. Report v2 over legacy suite majors 1–4 is permitted for diagnostic category accounting when paired with its exact bytes; coverage is unknown and conformance_status cannot be passed. Report v1 is never silently upconverted to a qualifying v2 report.
 
@@ -211,7 +224,7 @@ The runtime's generated release determines its defaults; an already generated Go
 
 Thus during opt-in rollout a Rust run of suite/5 requires --report-format 2, and an upgraded Go runtime embedding suite/5 requires ESS_REPORT_FORMAT=2; leaving the report option unset selects report/1 and P5 refuses. Selecting --strict or ESS_CONFORMANCE_STRICT=1 during that phase also requires explicit report/2; it never silently changes the report format. These are configuration refusals rather than failed, skipped or zero-case conformance results.
 
-The current conform run --format json/yaml output is the detailed S2 report, not S1 standalone output. Preserve that legacy structure and output bytes under report-format 1. For report-format 2, detailed output is a closed **ess-conformance-run/2** object with exactly format, summary, started_at, scenarios. summary is the complete v2 standalone report; scenarios is the existing ordered detailed scenario-result sequence with the chosen producer vocabulary, whose IDs/statuses must agree with summary.outcomes. JSON/YAML is presentation of that object; --report-out always writes the standalone JSON summary. No consumer may identify the surface solely from --format json. Update help, fixtures and downstream detailed-output consumers before switching defaults. Go produces only standalone output; browser replay produces neither form.
+The current conform run --format json/yaml output is the detailed S2 report, not S1 standalone output. Preserve that legacy structure and output bytes under report-format 1. For report-format 2, detailed output is a closed **ess-conformance-run/2** object with exactly format, summary, started_at, scenarios. summary is the complete v2 standalone report; started_at and summary.completed_at obey the exact u64 timestamp rules above. scenarios is the existing ordered detailed scenario-result sequence with the chosen producer vocabulary, whose IDs/statuses must agree with summary.outcomes. JSON/YAML is presentation of that object; --report-out always writes the standalone JSON summary. No consumer may identify the surface solely from --format json. Update help, fixtures and downstream detailed-output consumers before switching defaults. Go produces only standalone output; browser replay produces neither form.
 
 Strict mode is **--strict** for the CLI and **ESS_CONFORMANCE_STRICT=1** for generated Go. It requires report v2 and a paired exact suite. It succeeds only for conformance_status passed and the caller's expected selection. In strict Rust mode, exits are 0 for passed, 1 for failed, 3 for inconclusive; admission/refused input exits nonzero before target execution. In strict Go mode, an inconclusive report must fail the enclosing test run even if all subtests were skipped. Report-format 1 retains its historical execution/output behavior only in an admitted diagnostic pairing (P1); a strict/v1 configuration is refused before execution (P2/P5).
 
@@ -283,6 +296,23 @@ These are specified expected outcomes, **not executed compatibility results**. R
 | M56 | Both strict and allow-incomplete are explicit, or Go strictness variable is neither unset nor 1 | Refuse invalid invocation before execution, regardless of otherwise valid pairing | R/C, CLI/Go pairing gate |
 | M57 | Upgraded runtime generated during the opt-in phase retains its old defaults after the standalone ESS binary upgrades | Resolve that runtime's actual defaults; embedded v5 with unset report stays refused under P5 until explicitly configured or regenerated | C/O, retained-runtime rollout fixture |
 | M58 | Current persisted impact result versus a proposed exact-suite impact envelope | Preserve ess-impact/2 format and existing fields/meanings; future exact-suite fields require a separately versioned impact envelope | C/O, impact migration |
+| M59 | A generated candidate retains a useful check beside two identical unobservable invariant occurrences | Preserve two identical check_not_emitted array elements and counts.refused contribution 2; one selected execution result; in_scope occurrences keep all-pass conformance inconclusive | C/R, inventory/report writers |
+| M60 | One authored file repeats the same undeclared-view assertion twice, with an otherwise valid nonempty timeline | Preserve two identical candidate_not_emitted elements with the same source/ID and counts.refused contribution 2; one source-map entry and no result from that refused file | C/R, authored inventory/report writers |
+| M61 | Existing repeated refusal occurrences pass through component/origin selection or explicit filtering | Preserve every occurrence and retained identity; classify scope only under the existing selection rules; selected/outside/source/outcome uniqueness remains unchanged | C, inventory/filter writers |
+| M62 | A child suite or paired v2 report coalesces identical parent/suite refusal occurrences or invents ordinal messages | Refuse parent/coverage equality even if its deduplicated count is internally consistent; require exact full occurrence list | C/R/A, paired admission |
+| M63 | New report/2 or run/2 timestamp is 0 | Admit the exact epoch integer; Go nonnegative clock conversion and full-range readers retain 0 | R/A, timestamp readers/writers |
+| M64 | New report/2 or run/2 timestamp is 9223372036854775807 (i64::MAX) | Admit and preserve the exact positive integer; Go int64 clock conversion to uint64 is exact | R/A, timestamp readers/writers |
+| M65 | New report/2 or run/2 timestamp is 9223372036854775808 (i64::MAX + 1) | Full-range wire readers admit and preserve the positive u64; never reinterpret as a negative i64 | R/C/A, exact timestamp readers |
+| M66 | New report/2 or run/2 timestamp is 18446744073709551615 (u64::MAX) | Admit and preserve the exact maximum u64 through Rust writer and full-range readers | R/C/A, exact timestamp readers/writers |
+| M67 | Negative timestamp input to report/2 or run/2, including -1 | Refuse timestamp admission; no wrapping, clamping or retrospective change to v1 behavior | R/C/A, timestamp admission |
+| M68 | New timestamp exceeds u64::MAX, including 18446744073709551616 | Refuse before conversion or qualifying adaptation; do not round down to an admitted value | R/C/A, timestamp admission |
+| M69 | New timestamp token is fractional, exponent-form, signed or quoted, including 0.5, 1.0, 1e0, -0 or "1" | Refuse the timestamp token; payload-number or noncanonical-layout admission does not override integer typing | R/C/A, timestamp admission |
+| M70 | New timestamp 9007199254740993 or an upper-bound vector reaches a binary64 JSON Number path | Require re-reading original bytes with exact integer parsing; if unavailable, refuse that route without qualifying or emitting rounded evidence | R/C/A, exact parser/adaptation fixtures |
+| M71 | Go v2 clock returns a negative UnixMilli/int64 value | Refuse report production with a nonzero process result; emit no complete v2 report and do not wrap, clamp or switch to v1 | R, Go writer conversion |
+| M72 | Valid v2 u64 above a consumer's internal i64/date-time range | Admit the wire integer, then explicitly refuse the narrower adaptation; retain original positive bytes and no false qualification | A/C, consumer migration |
+| M73 | Detailed run/2 started_at or summary.completed_at exercises M63–M70 while the other timestamp is valid | Apply the same exact integer/range outcome to both fields; the detailed surface has no timestamp exception | R/A, detailed reader/writer fixtures |
+| M74 | Legacy Rust v1 upper-u64 timestamp or legacy Go v1 signed timestamp is read/written | Preserve each existing producer/reader's bytes and admission domain; no new shared range is imposed on v1 | R/A, retained legacy fixtures |
+| M75 | New reader/adaptation computes a timestamp difference, sum or duration outside its exact integer domain | Refuse that operation/adaptation on checked underflow/overflow/narrowing; never wrap, truncate, saturate or qualify a changed value | R/C/A, exact arithmetic fixtures |
 
 M17/M18 have a deployment outcome, not a invented statement that old code refuses. Their retained-runtime tests must demonstrate actual old behavior; a green test of a freshly regenerated runtime cannot stand in for that test.
 
@@ -300,9 +330,9 @@ The mandatory movement order is:
 
 1. **O: govern and inventory.** Record the Atlas ADR, relying parties, pinned old suites/reports/runtimes and exact upgrade dependencies. Keep legacy goldens and establish the matrix fixtures.
 2. **R/C: release readers first.** Ship strict version-specific admission and opt-in v2/v5 parsing with defaults still report/1 and suite/4. Update Rust entry points, Go template, browser, CLI detailed output, and impact's internal view; prove pre-execution refusal. Keep report/1 admission fixed at suite majors 1–4 independently of new suite execution support, and implement the P1–P10 pairing gate before writer opt-ins.
-3. **A: migrate both readers and policy.** Land the new AEP domain evidence contract/facts and predicates, then optional adapter and planning route, with legacy behavior retained. Consumer-side fixtures must cover skipped/error/unsupported, incomplete and empty coverage, and exact digest mismatch. Do not enable v2 adaptation before its domain representation is available.
+3. **A: migrate both readers and policy.** Land the new AEP domain evidence contract/facts and predicates, then optional adapter and planning route, with legacy behavior retained. New consumer readers/adaptation must also preserve refusal multiplicity and exact u64 timestamps, with checked refusal at narrower internal boundaries. Consumer-side fixtures must cover skipped/error/unsupported, incomplete and empty coverage, and exact digest mismatch. Do not enable v2 adaptation before its domain representation is available.
 4. **C/O: regenerate and deploy consumers.** Regenerate adopter Go packages and web player/model/suite sets from the admitted-reader release; update automation using detailed CLI outputs. Inventory exact deployed versions. Old embedded runtimes remain on legacy suites until their own regeneration is verified.
-5. **R/C: enable opt-in writers.** Count migration implements the v2 outcome/profile contract; coverage migration implements v5 inventory and exact byte binding. Counts-only v2 output against legacy suites is diagnostic/unknown coverage. Preserve partial-check and rejected-candidate inventories through every builder/filter, including retained source identities and explicit refusal scope. Suite/5 with an unset or explicit report/1 must refuse before execution; opt-in instructions require report/2 explicitly for both Rust and generated Go. No default changes yet. Execute all matrix rows, legacy byte checks, paired cross-language vectors and required repository gates.
+5. **R/C: enable opt-in writers.** Count migration implements the v2 outcome/profile contract; coverage migration implements v5 inventory and exact byte binding. Counts-only v2 output against legacy suites is diagnostic/unknown coverage. Preserve partial-check and rejected-candidate inventories through every builder/filter, including retained source identities, every refusal occurrence and explicit refusal scope. New report/run producers implement exact unsigned timestamp emission and checked Go clock conversion. Suite/5 with an unset or explicit report/1 must refuse before execution; opt-in instructions require report/2 explicitly for both Rust and generated Go. No default changes yet. Execute all matrix rows, legacy byte checks, paired cross-language vectors and required repository gates.
 6. **O/A/R/C: move defaults last.** After every mandatory relying party is ready and the ADR/shipped log records actual versions, release suite/5, report/2 and strict default behavior together. Publish migration instructions and the complete legacy opt-out (actual suite/1–4, explicit report/1, explicit allow-incomplete); report/1 alone is not an opt-out from the strict default. Record regenerated Go runtime defaults as well as the standalone CLI version. Keep old bytes and format meanings frozen.
 7. **O: observe and retain rollback.** Roll back writer defaults, not contract meanings or previously issued documents, if a relying reader is missed. Never relabel new data as v1 or feed v5 to unupgraded generated code to make a gate green.
 
@@ -310,4 +340,4 @@ The existing planning stories own implementation, not this document. Missing Atl
 
 ## Validation status
 
-This unit reviewed the source inventory, all 58 matrix rows and the P1–P10 pairing table for explicit outcomes and implementation owners. No compatibility fixture, canonicalization test, package test, formatter, Clippy run, browser check, AEP adaptation or downstream release was executed for this document. The sole repository check required here is git diff --check; its original result is retained in the implementation report and the correction result in correction-report.md. The integration coordinator owns full gates and any required site build. Implementation acceptance requires the matrix to become executed checks rather than treating this design review as that evidence.
+This unit reviewed the source inventory, all 75 matrix rows and the P1–P10 pairing table for explicit outcomes and implementation owners. No compatibility fixture, canonicalization test, package test, formatter, Clippy run, browser check, AEP adaptation or downstream release was executed for this document. The sole repository check required here is git diff --check; its original result is retained in the implementation report, the first correction in correction-report.md and the final bounded correction in correction-report-2.md. The integration coordinator owns full gates and any required site build. Implementation acceptance requires the matrix to become executed checks rather than treating this design review as that evidence.
