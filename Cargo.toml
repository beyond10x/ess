[workspace]
resolver = "2"
members = [
    # specify — an authored system becomes a validated, resolved IR.
    "crates/specify/ess-primitives",
    "crates/specify/ess-domain",
    "crates/specify/ess-compiler",
    "crates/specify/ess-composition",
    "crates/specify/ess-realization",
    # generate — that IR becomes artifacts; nothing here applies anything.
    "crates/generate/ess-gen",
    "crates/generate/ess-synth",
    "crates/generate/ess-openapi",
    "crates/generate/schema-contract",
    "crates/generate/ess-deployment",
    # verify — an implementation is measured against the specification.
    "crates/verify/ess-conformance",
    "crates/verify/ess-diff",
    # infra — the observed cluster: a separate bounded context.
    "crates/infra/infra-domain",
    "crates/infra/infra-compiler",
    "crates/infra/infra-analyze",
    "crates/infra/infra-spec",
    "crates/infra/infra-project",
    "crates/infra/ess-kubernetes",
    # edge — the binary an adopter runs, and this repository's own tooling.
    "crates/edge/ess-cli",
    "crates/edge/ess-xtask",
    "examples/billing-realization",
    "examples/gatepass-realization",
]
exclude = [
    "generated/rust/billing",
    "generated/rust/gatepass",
    "generated/web/billing",
    "examples/billing-web",
]

[workspace.package]
version = "0.13.0"
edition = "2021"
rust-version = "1.85"
license = "Apache-2.0"
repository = "https://github.com/beyond10x/ess"
authors = ["Beyond10x contributors"]
publish = false

[workspace.dependencies]
ess-primitives = { path = "crates/specify/ess-primitives", version = "0.13.0" }
ess-domain = { path = "crates/specify/ess-domain", version = "0.13.0" }
ess-compiler = { path = "crates/specify/ess-compiler", version = "0.13.0" }
ess-composition = { path = "crates/specify/ess-composition", version = "0.13.0" }
ess-realization = { path = "crates/specify/ess-realization", version = "0.13.0" }
ess-deployment = { path = "crates/generate/ess-deployment", version = "0.13.0" }
ess-gen = { path = "crates/generate/ess-gen", version = "0.13.0" }
ess-conformance = { path = "crates/verify/ess-conformance", version = "0.13.0" }
ess-diff = { path = "crates/verify/ess-diff", version = "0.13.0" }
ess-synth = { path = "crates/generate/ess-synth", version = "0.13.0" }
ess-kubernetes = { path = "crates/infra/ess-kubernetes", version = "0.13.0" }
ess-openapi = { path = "crates/generate/ess-openapi", version = "0.13.0" }
infra-domain = { path = "crates/infra/infra-domain", version = "0.13.0" }
infra-compiler = { path = "crates/infra/infra-compiler", version = "0.13.0" }
infra-analyze = { path = "crates/infra/infra-analyze", version = "0.13.0" }
infra-spec = { path = "crates/infra/infra-spec", version = "0.13.0" }
infra-project = { path = "crates/infra/infra-project", version = "0.13.0" }
schema-contract = { path = "crates/generate/schema-contract", version = "0.13.0" }
semver = { version = "1", features = ["serde"] }

serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
schemars = "0.8"
thiserror = "2"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
# CommonMark to the document IR, for content an adopter wrote: a `README.md` beside the model, a
# plan board another tool rendered. Parsing *input* into the IR is the opposite of the round trip
# this layer exists to remove — nothing here re-reads bytes this crate produced.
pulldown-cmark = { version = "0.13", default-features = false, features = ["html"] }

[workspace.lints.rust]
missing_docs = "warn"
unsafe_code = "forbid"

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
must_use_candidate = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"
