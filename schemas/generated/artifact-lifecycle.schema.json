{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ArtifactLifecycle",
  "description": "The legal statuses and status transitions for one artifact kind.\n\nLifecycles differ by kind — an ADR goes `proposed → accepted → superseded`, a design goes `draft → in_review → approved → implemented` — and validation rejects a status a kind does not have, which catches the copy-paste error that would otherwise make a requirement silently unsatisfiable.\n\nA document that names **no** kind is the tree's fallback: the lifecycle every kind with no nearer one is held to, and the only way to make a rule bind kinds nobody has enumerated. One tree may declare at most one. A lifecycle registered for the kind itself, or for a kind it specialises, always wins over it.",
  "type": "object",
  "required": [
    "initial"
  ],
  "properties": {
    "initial": {
      "description": "The status a new artifact starts in.",
      "allOf": [
        {
          "$ref": "#/definitions/ArtifactStatus"
        }
      ]
    },
    "kind": {
      "description": "The kind this lifecycle governs; absent for the fallback lifecycle.",
      "anyOf": [
        {
          "$ref": "#/definitions/ArtifactKind"
        },
        {
          "type": "null"
        }
      ]
    },
    "requires": {
      "description": "For each status, the evidence an artifact must carry to reach it.\n\nEmpty by default, and an empty map means exactly what it did before this field existed: the ladder governs *which* moves are legal and says nothing about whether one is earned. That is gap-register `:39` — \"a story's `implemented` is a claim nothing checks\" — and this is where a ladder gets to check it.\n\nA requirement is declared beside the rung it guards, rather than in the artifact-kind document or in a requirement document, because the refusal a person reads names a rung and the thing explaining a rung should sit next to it.",
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "$ref": "#/definitions/StatusRequirement"
        }
      }
    },
    "transitions": {
      "description": "For each status, the statuses it may move to.",
      "default": {},
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "$ref": "#/definitions/ArtifactStatus"
        },
        "uniqueItems": true
      }
    },
    "when": {
      "description": "For each status, a date that must have passed — or not yet — before it can be reached.\n\nGap-register `:73`: *time-based transitions of any kind, which today live in scripts `explain` cannot see*. A rung that opens on a date is now a line in this document rather than a cron job nobody can read.\n\nThe clock is **not** here. This names a key in the artifact's own frontmatter; the instant to compare it against is read at the edge and handed in, which is what keeps `aep-domain` clock-free and its banned-token scan passing.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/TimeGuard"
      }
    }
  },
  "additionalProperties": false,
  "definitions": {
    "ArtifactKind": {
      "description": "Artifact kind in kebab-case; the named vocabulary is listed in `examples`, and any other kebab-case name is accepted as an organisation-specific kind.",
      "examples": [
        "vision",
        "product-requirements",
        "initiative",
        "epic",
        "story",
        "task",
        "specification",
        "acceptance-criteria",
        "design",
        "feature-design",
        "component-design",
        "architecture-design",
        "api-design",
        "data-design",
        "architecture-decision-record",
        "test-plan",
        "evaluation-plan",
        "verification-report",
        "review-result",
        "approval-record",
        "release-plan",
        "migration-plan",
        "runbook",
        "incident-report",
        "postmortem",
        "executable-system-specification"
      ],
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "ArtifactStatus": {
      "description": "Artifact status. The named vocabulary is listed in `examples`; any other name a lifecycle document declares is carried verbatim, so a rung is a line in a YAML file rather than a release of the engine.",
      "examples": [
        "draft",
        "proposed",
        "in_review",
        "approved",
        "accepted",
        "rejected",
        "active",
        "implemented",
        "superseded",
        "archived"
      ],
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*$"
    },
    "EvidenceKind": {
      "description": "A kind of evidence. The canonical names come first; the rest are older spellings still accepted.",
      "type": "string",
      "enum": [
        "test_result",
        "static_analysis",
        "contract_result",
        "property_test_result",
        "deployment_result",
        "metric_observation",
        "health_observation",
        "approval",
        "diff",
        "artifact",
        "review",
        "verification",
        "specification",
        "ess_conformance",
        "trace_conformance",
        "test_execution",
        "source_diff",
        "artifact_evidence",
        "review_result",
        "verification_record",
        "specification_record",
        "ess_conformance_result"
      ]
    },
    "StatusRequirement": {
      "description": "What reaching a status costs: evidence of a kind, at least so many times.\n\nDeliberately **smaller** than [`EvidenceRequirement`](crate::requirement::EvidenceRequirement), which also carries `subject`, `verifier` and `independent`. Those are judgements the engine makes about a record — whether the producer was independent of the author, whether the record is about *this* artifact — and a ladder that declared them without the engine evaluating them would be a document making a promise nothing keeps. What a ladder can honestly say is *how many of what kind*, and a shell counts.",
      "type": "object",
      "required": [
        "evidence"
      ],
      "properties": {
        "at_least": {
          "description": "How many records of that kind are needed. One by default, because zero would be a requirement that requires nothing, written by somebody who believed it required something.",
          "default": 1,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "evidence": {
          "description": "The kind of evidence.",
          "allOf": [
            {
              "$ref": "#/definitions/EvidenceKind"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "TimeGuard": {
      "description": "When a status may be reached, relative to a date the artifact itself records.\n\nBoth fields name a **frontmatter key**, not a date: `after: expires_at` means *not until now is past whatever this artifact's `expires_at` says*. Putting a literal date here would make the ladder a document about one artifact.\n\nAn empty guard constrains nothing, which is what a `when:` entry someone started and did not finish should do — refusing every move because a document is half-written is a worse failure than permitting them.",
      "type": "object",
      "properties": {
        "after": {
          "description": "Reachable only once the supplied instant is **after** the date this key records.",
          "type": [
            "string",
            "null"
          ]
        },
        "before": {
          "description": "Reachable only while the supplied instant is **before** the date this key records.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    }
  }
}
