# Retained Go report/1 runtime

This is the unchanged generated runtime source captured from ESS
`bd6d82f0d551fcf1cc2ec2eab65aab2fe7539947` before the count writer edits.
It is a legacy control, not an upgraded runtime or a deployment claim.
The `count_retained_runtime_preserves_legacy_behavior_and_does_not_gain_version_checks`
case compiles and executes it, retaining the historical skip-as-nonpass behavior
and demonstrating that its missing suite-version check is still missing.

SHA-256: `434a77d4044ebb7775e68b97e36c57cc26ac3c066ae5b678fe948cfbca10a124`.
