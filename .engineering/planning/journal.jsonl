{"entity":"epic","version":1,"id":"entity-relations","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Epic: An entity declares its relations, and the model checks them\n\n## Outcome\n\nA modeller writes *an Account owns many CommercialClients* as one declared relation on the entity, `ess validate` refuses it when the target, the carrying field or the cardinality do not agree, and every projection (JSON Schema, OpenAPI, Rust) carries the relation instead of each reader re-deriving it from an id field.\n\n## Why Now\n\nThe second adopter of the planning plugins lost a plan on exactly this: the relationship between an account and a commercial-client object was inconsistent across passes (`#ai-general`, 2026-09-02 07:27 UTC). The review of that failure found that ESS is the only typed place in either stack to write the relation down, and that ESS today has no construct for it: `grep -rn 'cardinality\\|association\\|foreign' crates/ess-domain/src` returns nothing, `EntitySpec` holds `name`, `identity`, `fields`, `states`, `invariants`, `naming` and nothing relational (`crates/ess-domain/src/entity.rs:595-617`), and the only cross-member checks are member-to-domain ownership and command-to-lifecycle causes (`crates/ess-domain/src/spec.rs:302-309`, `:385`). A relation is therefore a typed id field on the child plus an invariant somebody remembers to write. The planning guardrail that now routes a new noun to ESS (`agentplugins` 0.4.0) routes it to a place that cannot hold the one fact that was missing.\n\n## Scope\n\nA `relations:` list on an entity; its vocabulary decided on a design page first; validation in `ess-domain`; the generated schema; the three projections; the billing example; and the `aep reverse openapi` mapping (filed in `aep`, `story:reverse-openapi-emits-relations`) and the plugin rule that cites it (filed in `agentplugins`, `story:ess-schema-cites-relations`).\n\n## Out of Scope\n\n- Deletion and cascade semantics at runtime. ESS describes; the entity runtime decides. A relation may *name* an `on_delete` policy only if the design page decides it belongs in the specification at all.\n- A graph query language over relations. `ess` answers `validate`, `compile`, `diff`; a traversal verb is earned by a reader that needs it.\n- Migrating existing adopters' models. There are no `relations:` entries to migrate; existing id fields keep validating exactly as today.\n\n## Risks\n\n- The vocabulary is decided by writing code instead of on the design page, and the first adopter's model then has to change. Mitigation: `story:relations-design-page` lands first, and the decision-blocker on the vocabulary is cleared by the operator, not by the implementor.\n- Projections disagree on how a relation renders (OpenAPI has no native ownership construct). Mitigation: one extension key, named on the design page, used by all three.\n\n## Ambiguities\n\n- `requires-stakeholder-input` — the relation vocabulary: `owns` vs `references`, whether cardinality is `one|many` or `min/max`, and whether `on_delete` is in scope. Decides: the ESS owner. Raised as `decision-blocker:relation-vocabulary`.\n- `inferable` — the identity type a relation is carried by is the target's `identity.type` (`crates/ess-domain/src/entity.rs:604`).\n- `inferable` — every declared reference must resolve, and the assembler already refuses one that does not (`crates/ess-domain/src/spec.rs:215`, `:265` at the reviewed revision).\n\n## Done When\n\n`ess validate` refuses a relation whose target does not exist, whose carrying field has the wrong type, or whose cardinality contradicts the field's shape; `ess compile` emits the relation in all three projections; the billing example carries one ownership relation and its projections are checked in `task check`.\n","owner":"ess","summary":"A relations: list on an entity, validated and projected, so an ownership relation is a declared fact rather than a typed id field plus an invariant somebody remembers.","tags":["domain","relations"],"title":"An entity declares its relations, and the model checks them"},"args":{"command":"create-entity","data":{"body":"# Epic: An entity declares its relations, and the model checks them\n\n## Outcome\n\nA modeller writes *an Account owns many CommercialClients* as one declared relation on the entity, `ess validate` refuses it when the target, the carrying field or the cardinality do not agree, and every projection (JSON Schema, OpenAPI, Rust) carries the relation instead of each reader re-deriving it from an id field.\n\n## Why Now\n\nThe second adopter of the planning plugins lost a plan on exactly this: the relationship between an account and a commercial-client object was inconsistent across passes (`#ai-general`, 2026-09-02 07:27 UTC). The review of that failure found that ESS is the only typed place in either stack to write the relation down, and that ESS today has no construct for it: `grep -rn 'cardinality\\|association\\|foreign' crates/ess-domain/src` returns nothing, `EntitySpec` holds `name`, `identity`, `fields`, `states`, `invariants`, `naming` and nothing relational (`crates/ess-domain/src/entity.rs:595-617`), and the only cross-member checks are member-to-domain ownership and command-to-lifecycle causes (`crates/ess-domain/src/spec.rs:302-309`, `:385`). A relation is therefore a typed id field on the child plus an invariant somebody remembers to write. The planning guardrail that now routes a new noun to ESS (`agentplugins` 0.4.0) routes it to a place that cannot hold the one fact that was missing.\n\n## Scope\n\nA `relations:` list on an entity; its vocabulary decided on a design page first; validation in `ess-domain`; the generated schema; the three projections; the billing example; and the `aep reverse openapi` mapping (filed in `aep`, `story:reverse-openapi-emits-relations`) and the plugin rule that cites it (filed in `agentplugins`, `story:ess-schema-cites-relations`).\n\n## Out of Scope\n\n- Deletion and cascade semantics at runtime. ESS describes; the entity runtime decides. A relation may *name* an `on_delete` policy only if the design page decides it belongs in the specification at all.\n- A graph query language over relations. `ess` answers `validate`, `compile`, `diff`; a traversal verb is earned by a reader that needs it.\n- Migrating existing adopters' models. There are no `relations:` entries to migrate; existing id fields keep validating exactly as today.\n\n## Risks\n\n- The vocabulary is decided by writing code instead of on the design page, and the first adopter's model then has to change. Mitigation: `story:relations-design-page` lands first, and the decision-blocker on the vocabulary is cleared by the operator, not by the implementor.\n- Projections disagree on how a relation renders (OpenAPI has no native ownership construct). Mitigation: one extension key, named on the design page, used by all three.\n\n## Ambiguities\n\n- `requires-stakeholder-input` — the relation vocabulary: `owns` vs `references`, whether cardinality is `one|many` or `min/max`, and whether `on_delete` is in scope. Decides: the ESS owner. Raised as `decision-blocker:relation-vocabulary`.\n- `inferable` — the identity type a relation is carried by is the target's `identity.type` (`crates/ess-domain/src/entity.rs:604`).\n- `inferable` — every declared reference must resolve, and the assembler already refuses one that does not (`crates/ess-domain/src/spec.rs:215`, `:265` at the reviewed revision).\n\n## Done When\n\n`ess validate` refuses a relation whose target does not exist, whose carrying field has the wrong type, or whose cardinality contradicts the field's shape; `ess compile` emits the relation in all three projections; the billing example carries one ownership relation and its projections are checked in `task check`.\n","owner":"ess","status":"draft","summary":"A relations: list on an entity, validated and projected, so an ownership relation is a declared fact rather than a typed id field plus an invariant somebody remembers.","tags":["domain","relations"],"title":"An entity declares its relations, and the model checks them"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/entity-relations"},"payload":{"actor":"human:timo","at":1788383080972,"causation":"cmd-new-epic-entity-relations","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:entity-relations@1#0~c4beb81d439b4eb4","executor":null,"recorded_at":"2026-09-02T21:04:40Z"}}
{"entity":"story","version":1,"id":"own-planning-store","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: ESS plans in a store of its own\n\n## Outcome\n\nAnybody working on ESS finds its plan in `.engineering/planning/`, mutated only through `aep artifact`, pinned to a protocol tree by commit — the same shape `aep` and `agentplugins` already use.\n\n## Context\n\nUntil 2026-09-02 ESS had no `.engineering/` directory (`ls -d .engineering` in the worktree at `2979e6e`: no such file). The epic that needs this store, `epic:entity-relations`, is the first cross-repository change since the 2026-09-01 split, and a plan that lives only in a chat is the thing that split was meant to end.\n\n## Acceptance\n\n- `.engineering/project.yaml` names the `aep` protocol tree by a 40-hex commit and `development.standard`.\n- `aep artifact validate` exits 0 in this repository.\n- `AGENTS.md` names the store and says mutations go through `aep artifact`.\n\n## Out of Scope\n\nMigrating `docs/plan/` pages into artifacts. They stay as design and plan documents; the store references them.\n\n## Ambiguities\n\n- `inferable` — the pin: `a054945cf55229861b7e7b9e83e94343278cbc02` is `aep` tag `0.42.0`.\n\n## Open Questions\n\nNone.\n","owner":"ess","summary":"The .engineering store, pinned to aep 0.42.0, so cross-repository work has a plan that is not a chat.","tags":["store"],"title":"ESS plans in a store of its own"},"args":{"command":"create-entity","data":{"body":"# Story: ESS plans in a store of its own\n\n## Outcome\n\nAnybody working on ESS finds its plan in `.engineering/planning/`, mutated only through `aep artifact`, pinned to a protocol tree by commit — the same shape `aep` and `agentplugins` already use.\n\n## Context\n\nUntil 2026-09-02 ESS had no `.engineering/` directory (`ls -d .engineering` in the worktree at `2979e6e`: no such file). The epic that needs this store, `epic:entity-relations`, is the first cross-repository change since the 2026-09-01 split, and a plan that lives only in a chat is the thing that split was meant to end.\n\n## Acceptance\n\n- `.engineering/project.yaml` names the `aep` protocol tree by a 40-hex commit and `development.standard`.\n- `aep artifact validate` exits 0 in this repository.\n- `AGENTS.md` names the store and says mutations go through `aep artifact`.\n\n## Out of Scope\n\nMigrating `docs/plan/` pages into artifacts. They stay as design and plan documents; the store references them.\n\n## Ambiguities\n\n- `inferable` — the pin: `a054945cf55229861b7e7b9e83e94343278cbc02` is `aep` tag `0.42.0`.\n\n## Open Questions\n\nNone.\n","owner":"ess","status":"draft","summary":"The .engineering store, pinned to aep 0.42.0, so cross-repository work has a plan that is not a chat.","tags":["store"],"title":"ESS plans in a store of its own"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/own-planning-store"},"payload":{"actor":"human:timo","at":1788383081115,"causation":"cmd-new-story-own-planning-store","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:own-planning-store@1#0~41f94a5e41d33be6","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"own-planning-store","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:entity-relations"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000004","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788383081122,"causation":"cmd-story-own-planning-store-decomposes-epic-entity-relations","change":{"change":"related","relation":"decomposes","target":"epic:entity-relations"},"correlation":"protocol-artifact-relate","event_id":"story:own-planning-store@1#0~3b2985be0bb70b96","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"relations-design-page","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The relation vocabulary is decided on a design page before code\n\n## Outcome\n\nA reader of `docs/design/ess-entity-relations-design-v0.1.md` knows what a relation is in ESS, what it refuses, how it renders in each projection, and which alternatives were rejected — before any parser accepts one.\n\n## Context\n\nThe repository's design documents live in `docs/design/` (six `*-design-v0.1.md` files at `2979e6e`). The vocabulary questions are the ones an implementor would otherwise settle by typing: ownership vs reference, cardinality form, direction, and whether a relation may name a deletion policy. `decision-blocker:relation-vocabulary` holds those questions and this page is where the answer is written.\n\n## Acceptance\n\n- The page defines the YAML shape with one example for each of `owns` and `references`, and one refused example for each validation rule.\n- It names the projection of a relation in JSON Schema, OpenAPI and Rust, with one extension key used by all three.\n- It lists the rejected alternatives (an invariant-only encoding; a separate `relations.yaml` document) with the reason each was rejected.\n- It is linked from `README.md` and from the epic.\n\n## Out of Scope\n\nCode. This story lands as a document and a cleared decision-blocker.\n\n## Ambiguities\n\n- `requires-stakeholder-input` — the vocabulary itself; see `decision-blocker:relation-vocabulary`, which names a default for silence.\n\n## Open Questions\n\nNone beyond the blocker.\n","owner":"ess","summary":"docs/design/ess-entity-relations-design-v0.1.md: shape, refusals, projections, rejected alternatives.","tags":["design","relations"],"title":"The relation vocabulary is decided on a design page before code"},"args":{"command":"create-entity","data":{"body":"# Story: The relation vocabulary is decided on a design page before code\n\n## Outcome\n\nA reader of `docs/design/ess-entity-relations-design-v0.1.md` knows what a relation is in ESS, what it refuses, how it renders in each projection, and which alternatives were rejected — before any parser accepts one.\n\n## Context\n\nThe repository's design documents live in `docs/design/` (six `*-design-v0.1.md` files at `2979e6e`). The vocabulary questions are the ones an implementor would otherwise settle by typing: ownership vs reference, cardinality form, direction, and whether a relation may name a deletion policy. `decision-blocker:relation-vocabulary` holds those questions and this page is where the answer is written.\n\n## Acceptance\n\n- The page defines the YAML shape with one example for each of `owns` and `references`, and one refused example for each validation rule.\n- It names the projection of a relation in JSON Schema, OpenAPI and Rust, with one extension key used by all three.\n- It lists the rejected alternatives (an invariant-only encoding; a separate `relations.yaml` document) with the reason each was rejected.\n- It is linked from `README.md` and from the epic.\n\n## Out of Scope\n\nCode. This story lands as a document and a cleared decision-blocker.\n\n## Ambiguities\n\n- `requires-stakeholder-input` — the vocabulary itself; see `decision-blocker:relation-vocabulary`, which names a default for silence.\n\n## Open Questions\n\nNone beyond the blocker.\n","owner":"ess","status":"draft","summary":"docs/design/ess-entity-relations-design-v0.1.md: shape, refusals, projections, rejected alternatives.","tags":["design","relations"],"title":"The relation vocabulary is decided on a design page before code"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/relations-design-page"},"payload":{"actor":"human:timo","at":1788383081254,"causation":"cmd-new-story-relations-design-page","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:relations-design-page@1#0~a0962afac37b8844","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"relations-design-page","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:entity-relations"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000009","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788383081265,"causation":"cmd-story-relations-design-page-decomposes-epic-entity-relations","change":{"change":"related","relation":"decomposes","target":"epic:entity-relations"},"correlation":"protocol-artifact-relate","event_id":"story:relations-design-page@1#0~5bc0b3c34d6edd43","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"relations-in-the-domain-model","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: An entity's relations are typed, resolved and refused like its fields\n\n## Outcome\n\nA modeller who writes a wrong relation is told which rule it broke, by `ess validate`, with the same hint style the field rules use.\n\n## Context\n\n`EntitySpec` has `identity`, `fields`, `states`, `invariants`, `naming` (`crates/ess-domain/src/entity.rs:595-617`). This story adds `relations: Vec<RelationSpec>` and the validation pass that reads it, placed beside `validate_lifecycle_causes` (`crates/ess-domain/src/spec.rs:305`) because it is, like that one, a relation between two members that neither can see alone. Shape and vocabulary come from `story:relations-design-page`.\n\n## Acceptance\n\n- `RelationSpec` parses from the shape on the design page and is rejected with a positioned error otherwise.\n- `ess validate` refuses: a target that is not a declared entity; a `via` field that does not exist on the source entity; a `via` field whose type is not the target's identity type (wrapped by `Optional<…>` or `List<…>` as the cardinality demands); an `owns` relation whose target is owned by more than one entity.\n- Every refusal is a `ValidationCode` with a hint, and each has a test that breaks it on purpose.\n- `schemas/generated/ess.schema.json` regenerates with the new key and `task check` is green.\n\n## Out of Scope\n\nProjections (`story:relations-projected`) and the example (`story:relations-in-the-billing-example`).\n\n## Ambiguities\n\n- `inferable` — the identity type to compare against: `EntitySpec.identity: Field` (`entity.rs:604`).\n- `requires-stakeholder-input` — whether an unowned child of an `owns` relation is refused or merely reported. Decides: ESS owner, inside `decision-blocker:relation-vocabulary`. Default: refused.\n\n## Open Questions\n\nNone.\n","owner":"ess","summary":"RelationSpec on EntitySpec, a validation pass beside validate_lifecycle_causes, four refusals each with a test that breaks it.","tags":["domain","relations"],"title":"An entity's relations are typed, resolved and refused like its fields"},"args":{"command":"create-entity","data":{"body":"# Story: An entity's relations are typed, resolved and refused like its fields\n\n## Outcome\n\nA modeller who writes a wrong relation is told which rule it broke, by `ess validate`, with the same hint style the field rules use.\n\n## Context\n\n`EntitySpec` has `identity`, `fields`, `states`, `invariants`, `naming` (`crates/ess-domain/src/entity.rs:595-617`). This story adds `relations: Vec<RelationSpec>` and the validation pass that reads it, placed beside `validate_lifecycle_causes` (`crates/ess-domain/src/spec.rs:305`) because it is, like that one, a relation between two members that neither can see alone. Shape and vocabulary come from `story:relations-design-page`.\n\n## Acceptance\n\n- `RelationSpec` parses from the shape on the design page and is rejected with a positioned error otherwise.\n- `ess validate` refuses: a target that is not a declared entity; a `via` field that does not exist on the source entity; a `via` field whose type is not the target's identity type (wrapped by `Optional<…>` or `List<…>` as the cardinality demands); an `owns` relation whose target is owned by more than one entity.\n- Every refusal is a `ValidationCode` with a hint, and each has a test that breaks it on purpose.\n- `schemas/generated/ess.schema.json` regenerates with the new key and `task check` is green.\n\n## Out of Scope\n\nProjections (`story:relations-projected`) and the example (`story:relations-in-the-billing-example`).\n\n## Ambiguities\n\n- `inferable` — the identity type to compare against: `EntitySpec.identity: Field` (`entity.rs:604`).\n- `requires-stakeholder-input` — whether an unowned child of an `owns` relation is refused or merely reported. Decides: ESS owner, inside `decision-blocker:relation-vocabulary`. Default: refused.\n\n## Open Questions\n\nNone.\n","owner":"ess","status":"draft","summary":"RelationSpec on EntitySpec, a validation pass beside validate_lifecycle_causes, four refusals each with a test that breaks it.","tags":["domain","relations"],"title":"An entity's relations are typed, resolved and refused like its fields"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/relations-in-the-domain-model"},"payload":{"actor":"human:timo","at":1788383081399,"causation":"cmd-new-story-relations-in-the-domain-model","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:relations-in-the-domain-model@1#0~8eca025e72cc2000","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"relations-in-the-domain-model","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:entity-relations"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000014","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788383081406,"causation":"cmd-story-relations-in-the-domain-model-decomposes-epic-entity-relations","change":{"change":"related","relation":"decomposes","target":"epic:entity-relations"},"correlation":"protocol-artifact-relate","event_id":"story:relations-in-the-domain-model@1#0~ab86b8454367a56d","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"relations-in-the-domain-model","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:entity-relations"},{"depends_on":"story:relations-design-page"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000014","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788383081414,"causation":"cmd-story-relations-in-the-domain-model-depends_on-story-relations-design-page","change":{"change":"related","relation":"depends_on","target":"story:relations-design-page"},"correlation":"protocol-artifact-relate","event_id":"story:relations-in-the-domain-model@1#0~21f409acfa909e7c","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"relations-projected","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: A relation survives into every projection\n\n## Outcome\n\nA reader of the generated OpenAPI document, the JSON Schema or the Rust types sees the relation the model declared, under one extension key, rather than an id field they have to interpret.\n\n## Context\n\nToday a relation is a typed id field and readers re-derive it. The design page names the extension key; this story applies it to the three projections `ess compile` produces.\n\n## Acceptance\n\n- JSON Schema: the carrying property carries the extension key with target and cardinality.\n- OpenAPI: the same key on the property, and the target schema is referenced by `$ref`.\n- Rust: a doc attribute or a typed newtype reference that names the target; the generated crate compiles.\n- A golden test per projection over the billing example diffs byte-exact.\n\n## Out of Scope\n\nGenerating navigation code (fetch the owner, list the children). Projections describe; runtimes navigate.\n\n## Ambiguities\n\n- `inferable` — the extension key name comes from the design page; if the page has not landed this story cannot start (`depends_on`).\n\n## Open Questions\n\nNone.\n","owner":"ess","summary":"One extension key carries target and cardinality into JSON Schema, OpenAPI and Rust; golden tests per projection.","tags":["projections","relations"],"title":"A relation survives into every projection"},"args":{"command":"create-entity","data":{"body":"# Story: A relation survives into every projection\n\n## Outcome\n\nA reader of the generated OpenAPI document, the JSON Schema or the Rust types sees the relation the model declared, under one extension key, rather than an id field they have to interpret.\n\n## Context\n\nToday a relation is a typed id field and readers re-derive it. The design page names the extension key; this story applies it to the three projections `ess compile` produces.\n\n## Acceptance\n\n- JSON Schema: the carrying property carries the extension key with target and cardinality.\n- OpenAPI: the same key on the property, and the target schema is referenced by `$ref`.\n- Rust: a doc attribute or a typed newtype reference that names the target; the generated crate compiles.\n- A golden test per projection over the billing example diffs byte-exact.\n\n## Out of Scope\n\nGenerating navigation code (fetch the owner, list the children). Projections describe; runtimes navigate.\n\n## Ambiguities\n\n- `inferable` — the extension key name comes from the design page; if the page has not landed this story cannot start (`depends_on`).\n\n## Open Questions\n\nNone.\n","owner":"ess","status":"draft","summary":"One extension key carries target and cardinality into JSON Schema, OpenAPI and Rust; golden tests per projection.","tags":["projections","relations"],"title":"A relation survives into every projection"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/relations-projected"},"payload":{"actor":"human:timo","at":1788383081549,"causation":"cmd-new-story-relations-projected","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:relations-projected@1#0~64211bf9404b7c01","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"relations-projected","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:entity-relations"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000021","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788383081556,"causation":"cmd-story-relations-projected-decomposes-epic-entity-relations","change":{"change":"related","relation":"decomposes","target":"epic:entity-relations"},"correlation":"protocol-artifact-relate","event_id":"story:relations-projected@1#0~b78015c0d16ce2b1","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"relations-projected","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:entity-relations"},{"depends_on":"story:relations-in-the-domain-model"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000021","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788383081562,"causation":"cmd-story-relations-projected-depends_on-story-relations-in-the-domain-model","change":{"change":"related","relation":"depends_on","target":"story:relations-in-the-domain-model"},"correlation":"protocol-artifact-relate","event_id":"story:relations-projected@1#0~5836066c53c31008","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"relations-in-the-billing-example","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The billing example shows an ownership relation end to end\n\n## Outcome\n\nSomebody learning ESS opens `examples/billing/` and sees one relation declared, validated and projected, and the `ess-schema` plugin skill can point at it as the pattern for *an Account owns many CommercialClients*.\n\n## Context\n\n`examples/billing/domains/invoice.yaml` declares `billing.invoice.Invoice` with an identity `invoice_id: billing.invoice.InvoiceId` and typed fields (`:62-82`) and no relation to any other entity. The adopter case that motivated the epic is an ownership relation; the example should carry one of the same shape.\n\n## Acceptance\n\n- One `owns`-kind relation is declared in the example (a customer or account entity owning invoices, or the equivalent the design page chooses).\n- `ess validate examples/billing` exits 0 and `ess compile` output for the example is checked into the golden fixtures.\n- The example README explains the relation in three sentences.\n\n## Out of Scope\n\nA second example repository.\n\n## Ambiguities\n\n- `inferable` — the example's structure and file names: `examples/billing/{system.yaml,domains/invoice.yaml,domains/email.yaml,components.yaml,topology.yaml}`.\n\n## Open Questions\n\nNone.\n","owner":"ess","summary":"examples/billing carries one owns relation, validated, compiled into the golden fixtures and explained in its README.","tags":["examples","relations"],"title":"The billing example shows an ownership relation end to end"},"args":{"command":"create-entity","data":{"body":"# Story: The billing example shows an ownership relation end to end\n\n## Outcome\n\nSomebody learning ESS opens `examples/billing/` and sees one relation declared, validated and projected, and the `ess-schema` plugin skill can point at it as the pattern for *an Account owns many CommercialClients*.\n\n## Context\n\n`examples/billing/domains/invoice.yaml` declares `billing.invoice.Invoice` with an identity `invoice_id: billing.invoice.InvoiceId` and typed fields (`:62-82`) and no relation to any other entity. The adopter case that motivated the epic is an ownership relation; the example should carry one of the same shape.\n\n## Acceptance\n\n- One `owns`-kind relation is declared in the example (a customer or account entity owning invoices, or the equivalent the design page chooses).\n- `ess validate examples/billing` exits 0 and `ess compile` output for the example is checked into the golden fixtures.\n- The example README explains the relation in three sentences.\n\n## Out of Scope\n\nA second example repository.\n\n## Ambiguities\n\n- `inferable` — the example's structure and file names: `examples/billing/{system.yaml,domains/invoice.yaml,domains/email.yaml,components.yaml,topology.yaml}`.\n\n## Open Questions\n\nNone.\n","owner":"ess","status":"draft","summary":"examples/billing carries one owns relation, validated, compiled into the golden fixtures and explained in its README.","tags":["examples","relations"],"title":"The billing example shows an ownership relation end to end"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/relations-in-the-billing-example"},"payload":{"actor":"human:timo","at":1788383081695,"causation":"cmd-new-story-relations-in-the-billing-example","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:relations-in-the-billing-example@1#0~8e79a932316bd8a9","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"relations-in-the-billing-example","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:entity-relations"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000028","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788383081702,"causation":"cmd-story-relations-in-the-billing-example-decomposes-epic-entity-relations","change":{"change":"related","relation":"decomposes","target":"epic:entity-relations"},"correlation":"protocol-artifact-relate","event_id":"story:relations-in-the-billing-example@1#0~f83818e1f9be3f88","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"story","version":1,"id":"relations-in-the-billing-example","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:entity-relations"},{"depends_on":"story:relations-projected"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000028","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788383081709,"causation":"cmd-story-relations-in-the-billing-example-depends_on-story-relations-projected","change":{"change":"related","relation":"depends_on","target":"story:relations-projected"},"correlation":"protocol-artifact-relate","event_id":"story:relations-in-the-billing-example@1#0~3b73e91854b1e5c0","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"decision-blocker","version":1,"id":"relation-vocabulary","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"open","changed":{"body":"# Decision blocker: Nobody has decided the relation vocabulary\n\n## What is undecided\n\n1. Kinds: `owns` and `references` only, or a single kind with an `ownership: true` flag.\n2. Cardinality: `one | many` on the target side only, or `min`/`max` on both sides.\n3. Direction: declared on the owner, on the child, or on both with a consistency check.\n4. Whether a relation may name `on_delete` (`cascade | restrict | detach`) at all, given that ESS describes and the runtime decides.\n\n## Who decides\n\nThe ESS owner, on `story:relations-design-page`.\n\n## Default if nobody answers\n\nTwo kinds (`owns`, `references`); cardinality `one | many` on the target side; declared on the source entity only, with `owns` targets checked for a single owner; no `on_delete` in v0.1.\n\n## What it withholds\n\n`story:relations-in-the-domain-model` cannot start without it: the parser's shape is the decision.\n","owner":"ess","summary":"Kinds, cardinality form, direction and on_delete are open; default for silence is written in the body.","title":"Nobody has decided the relation vocabulary"},"args":{"command":"create-entity","data":{"body":"# Decision blocker: Nobody has decided the relation vocabulary\n\n## What is undecided\n\n1. Kinds: `owns` and `references` only, or a single kind with an `ownership: true` flag.\n2. Cardinality: `one | many` on the target side only, or `min`/`max` on both sides.\n3. Direction: declared on the owner, on the child, or on both with a consistency check.\n4. Whether a relation may name `on_delete` (`cascade | restrict | detach`) at all, given that ESS describes and the runtime decides.\n\n## Who decides\n\nThe ESS owner, on `story:relations-design-page`.\n\n## Default if nobody answers\n\nTwo kinds (`owns`, `references`); cardinality `one | many` on the target side; declared on the source entity only, with `owns` targets checked for a single owner; no `on_delete` in v0.1.\n\n## What it withholds\n\n`story:relations-in-the-domain-model` cannot start without it: the parser's shape is the decision.\n","owner":"ess","status":"open","summary":"Kinds, cardinality form, direction and on_delete are open; default for silence is written in the body.","title":"Nobody has decided the relation vocabulary"},"entity_type":"aep.decision-blocker/v1","locator":"ep://planning/store/decision-blocker/relation-vocabulary"},"payload":{"actor":"human:timo","at":1788383081843,"causation":"cmd-new-decision-blocker-relation-vocabulary","change":{"change":"created","status":"open"},"correlation":"protocol-artifact-new","event_id":"decision-blocker:relation-vocabulary@1#0~8861d7f4dbcd89d7","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"decision-blocker","version":1,"id":"relation-vocabulary","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"blocks":"story:relations-in-the-domain-model"}]},"args":{"command":"create-relation","kind":"blocks","source":"01MEM0000000000000035","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788383081851,"causation":"cmd-decision-blocker-relation-vocabulary-blocks-story-relations-in-the-domain-model","change":{"change":"related","relation":"blocks","target":"story:relations-in-the-domain-model"},"correlation":"protocol-artifact-relate","event_id":"decision-blocker:relation-vocabulary@1#0~cceedf0a40f066bd","executor":null,"recorded_at":"2026-09-02T21:04:41Z"}}
{"entity":"decision-blocker","version":1,"id":"relation-vocabulary","revision":2,"type":"aep.entity.update/v1","from_state":"open","to_state":"open","changed":{"body":"# Decision blocker: Nobody has decided the relation vocabulary\n\n## What is undecided\n\n1. Kinds: `owns` and `references` only, or a single kind with an `ownership: true` flag.\n2. Cardinality: `one | many` on the target side only, or `min`/`max` on both sides.\n3. Direction: declared on the owner, on the child, or on both with a consistency check.\n4. Whether a relation may name `on_delete` (`cascade | restrict | detach`) at all, given that ESS describes and the runtime decides.\n\n## Who decides\n\nThe ESS owner, on `story:relations-design-page`.\n\n## Default if nobody answers\n\nTwo kinds (`owns`, `references`); cardinality `one | many` on the target side; declared on the source entity only, with `owns` targets checked for a single owner; no `on_delete` in v0.1.\n\n## What it withholds\n\n`story:relations-in-the-domain-model` cannot start without it: the parser's shape is the decision.\n\n## Decided\n\n2026-09-03: the operator accepted the default (\"yes, do it\" on the report that named it). Two kinds, `owns` and `references`; cardinality `one | many` on the target side; declared on the source entity only, with `owns` targets checked for a single owner; no `on_delete` in v0.1. The design page (`story:relations-design-page`) writes the shape down.\n"},"args":{"changes":{"body":"# Decision blocker: Nobody has decided the relation vocabulary\n\n## What is undecided\n\n1. Kinds: `owns` and `references` only, or a single kind with an `ownership: true` flag.\n2. Cardinality: `one | many` on the target side only, or `min`/`max` on both sides.\n3. Direction: declared on the owner, on the child, or on both with a consistency check.\n4. Whether a relation may name `on_delete` (`cascade | restrict | detach`) at all, given that ESS describes and the runtime decides.\n\n## Who decides\n\nThe ESS owner, on `story:relations-design-page`.\n\n## Default if nobody answers\n\nTwo kinds (`owns`, `references`); cardinality `one | many` on the target side; declared on the source entity only, with `owns` targets checked for a single owner; no `on_delete` in v0.1.\n\n## What it withholds\n\n`story:relations-in-the-domain-model` cannot start without it: the parser's shape is the decision.\n\n## Decided\n\n2026-09-03: the operator accepted the default (\"yes, do it\" on the report that named it). Two kinds, `owns` and `references`; cardinality `one | many` on the target side; declared on the source entity only, with `owns` targets checked for a single owner; no `on_delete` in v0.1. The design page (`story:relations-design-page`) writes the shape down.\n"},"command":"update-entity","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788386579021,"causation":"cmd-protocol-artifact-body-decision-blocker-relation-vocabulary-1788386579021","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"decision-blocker:relation-vocabulary@2#0~5ba1b7581ca004e4","executor":null,"recorded_at":"2026-09-02T22:02:59Z"}}
{"entity":"decision-blocker","version":1,"id":"relation-vocabulary","revision":3,"type":"aep.status.move/v1","from_state":"open","to_state":"cleared","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000001","to":"cleared"},"payload":{"actor":"human:timo","at":1788386579162,"causation":"cmd-move-decision-blocker-relation-vocabulary-1788386579162","change":{"change":"moved","decided_on":{},"from":"open","to":"cleared"},"correlation":"protocol-artifact-move","event_id":"decision-blocker:relation-vocabulary@3#0~ac38e4fe0da3a305","executor":null,"recorded_at":"2026-09-02T22:02:59Z"}}
{"entity":"story","version":1,"id":"own-planning-store","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788401400000,"causation":"cmd-evidence-story-own-planning-store-test_result-1788389685062","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check"},"correlation":"protocol-artifact-evidence","event_id":"story:own-planning-store@1#0~3ece28d8956c915f","executor":null,"recorded_at":"2026-09-03T02:10:00Z"}}
{"entity":"story","version":1,"id":"own-planning-store","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000007","to":"proposed"},"payload":{"actor":"human:timo","at":1788389685270,"causation":"cmd-move-story-own-planning-store-1788389685270","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:own-planning-store@2#0~a8606860e45f68ca","executor":null,"recorded_at":"2026-09-02T22:54:45Z"}}
{"entity":"story","version":1,"id":"own-planning-store","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000007","to":"active"},"payload":{"actor":"human:timo","at":1788389685508,"causation":"cmd-move-story-own-planning-store-1788389685508","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:own-planning-store@3#0~5eca84ffbe5168be","executor":null,"recorded_at":"2026-09-02T22:54:45Z"}}
{"entity":"story","version":1,"id":"own-planning-store","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000007","to":"implemented"},"payload":{"actor":"human:timo","at":1788389685738,"causation":"cmd-move-story-own-planning-store-1788389685738","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:own-planning-store@4#0~c7cacbbd7e642f56","executor":null,"recorded_at":"2026-09-02T22:54:45Z"}}
{"entity":"story","version":1,"id":"relations-design-page","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788401400000,"causation":"cmd-evidence-story-relations-design-page-test_result-1788389685857","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check"},"correlation":"protocol-artifact-evidence","event_id":"story:relations-design-page@1#0~5e54e5cf89af57a7","executor":null,"recorded_at":"2026-09-03T02:10:00Z"}}
{"entity":"story","version":1,"id":"relations-design-page","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000010","to":"proposed"},"payload":{"actor":"human:timo","at":1788389686026,"causation":"cmd-move-story-relations-design-page-1788389686026","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:relations-design-page@2#0~5e4eb49b5a0c7a66","executor":null,"recorded_at":"2026-09-02T22:54:46Z"}}
{"entity":"story","version":1,"id":"relations-design-page","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000010","to":"active"},"payload":{"actor":"human:timo","at":1788389686231,"causation":"cmd-move-story-relations-design-page-1788389686231","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:relations-design-page@3#0~8c2e7f67b8a83bb2","executor":null,"recorded_at":"2026-09-02T22:54:46Z"}}
{"entity":"story","version":1,"id":"relations-design-page","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000010","to":"implemented"},"payload":{"actor":"human:timo","at":1788389686421,"causation":"cmd-move-story-relations-design-page-1788389686421","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:relations-design-page@4#0~6d4e4482cba0f992","executor":null,"recorded_at":"2026-09-02T22:54:46Z"}}
{"entity":"story","version":1,"id":"relations-in-the-domain-model","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788401400000,"causation":"cmd-evidence-story-relations-in-the-domain-model-test_result-1788389686551","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check"},"correlation":"protocol-artifact-evidence","event_id":"story:relations-in-the-domain-model@1#0~2a69f3cf6c4b81d1","executor":null,"recorded_at":"2026-09-03T02:10:00Z"}}
{"entity":"story","version":1,"id":"relations-in-the-domain-model","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000016","to":"proposed"},"payload":{"actor":"human:timo","at":1788389686716,"causation":"cmd-move-story-relations-in-the-domain-model-1788389686716","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:relations-in-the-domain-model@2#0~1ddd9e8f6f65bfac","executor":null,"recorded_at":"2026-09-02T22:54:46Z"}}
{"entity":"story","version":1,"id":"relations-in-the-domain-model","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000016","to":"active"},"payload":{"actor":"human:timo","at":1788389686983,"causation":"cmd-move-story-relations-in-the-domain-model-1788389686983","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:relations-in-the-domain-model@3#0~16197e6bbe252640","executor":null,"recorded_at":"2026-09-02T22:54:46Z"}}
{"entity":"story","version":1,"id":"relations-in-the-domain-model","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000016","to":"implemented"},"payload":{"actor":"human:timo","at":1788389687205,"causation":"cmd-move-story-relations-in-the-domain-model-1788389687205","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:relations-in-the-domain-model@4#0~5eeef370d6c71538","executor":null,"recorded_at":"2026-09-02T22:54:47Z"}}
{"entity":"story","version":1,"id":"relations-projected","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788401400000,"causation":"cmd-evidence-story-relations-projected-test_result-1788389687466","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check"},"correlation":"protocol-artifact-evidence","event_id":"story:relations-projected@1#0~ac5756cfb5e20900","executor":null,"recorded_at":"2026-09-03T02:10:00Z"}}
{"entity":"story","version":1,"id":"relations-projected","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000019","to":"proposed"},"payload":{"actor":"human:timo","at":1788389687733,"causation":"cmd-move-story-relations-projected-1788389687733","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:relations-projected@2#0~f9a58fef9456570d","executor":null,"recorded_at":"2026-09-02T22:54:47Z"}}
{"entity":"story","version":1,"id":"relations-projected","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000019","to":"active"},"payload":{"actor":"human:timo","at":1788389688199,"causation":"cmd-move-story-relations-projected-1788389688199","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:relations-projected@3#0~2fcd169a7c16343d","executor":null,"recorded_at":"2026-09-02T22:54:48Z"}}
{"entity":"story","version":1,"id":"relations-projected","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000019","to":"implemented"},"payload":{"actor":"human:timo","at":1788389688714,"causation":"cmd-move-story-relations-projected-1788389688714","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:relations-projected@4#0~1b19f78d5effdf07","executor":null,"recorded_at":"2026-09-02T22:54:48Z"}}
{"entity":"story","version":1,"id":"relations-in-the-billing-example","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788401400000,"causation":"cmd-evidence-story-relations-in-the-billing-example-test_result-1788389688969","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check"},"correlation":"protocol-artifact-evidence","event_id":"story:relations-in-the-billing-example@1#0~55abc0cf84c7eb1e","executor":null,"recorded_at":"2026-09-03T02:10:00Z"}}
{"entity":"story","version":1,"id":"relations-in-the-billing-example","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000013","to":"proposed"},"payload":{"actor":"human:timo","at":1788389689225,"causation":"cmd-move-story-relations-in-the-billing-example-1788389689225","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:relations-in-the-billing-example@2#0~124e003a36b3f08f","executor":null,"recorded_at":"2026-09-02T22:54:49Z"}}
{"entity":"story","version":1,"id":"relations-in-the-billing-example","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000013","to":"active"},"payload":{"actor":"human:timo","at":1788389689768,"causation":"cmd-move-story-relations-in-the-billing-example-1788389689768","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:relations-in-the-billing-example@3#0~b2b815730b95babf","executor":null,"recorded_at":"2026-09-02T22:54:49Z"}}
{"entity":"story","version":1,"id":"relations-in-the-billing-example","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000013","to":"implemented"},"payload":{"actor":"human:timo","at":1788389690323,"causation":"cmd-move-story-relations-in-the-billing-example-1788389690323","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:relations-in-the-billing-example@4#0~7b149e8637f603a1","executor":null,"recorded_at":"2026-09-02T22:54:50Z"}}
{"entity":"epic","version":1,"id":"entity-relations","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788401400000,"causation":"cmd-evidence-epic-entity-relations-test_result-1788389690664","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/ess/commit/3a55e8a","source":"task check"},"correlation":"protocol-artifact-evidence","event_id":"epic:entity-relations@1#0~362503d8908524d6","executor":null,"recorded_at":"2026-09-03T02:10:00Z"}}
{"entity":"epic","version":1,"id":"entity-relations","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000004","to":"proposed"},"payload":{"actor":"human:timo","at":1788389690953,"causation":"cmd-move-epic-entity-relations-1788389690953","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:entity-relations@2#0~b850bcf7f4596533","executor":null,"recorded_at":"2026-09-02T22:54:50Z"}}
{"entity":"epic","version":1,"id":"entity-relations","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000004","to":"active"},"payload":{"actor":"human:timo","at":1788389691363,"causation":"cmd-move-epic-entity-relations-1788389691363","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:entity-relations@3#0~0a5bb0365687551b","executor":null,"recorded_at":"2026-09-02T22:54:51Z"}}
{"entity":"epic","version":1,"id":"entity-relations","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000004","to":"implemented"},"payload":{"actor":"human:timo","at":1788389691788,"causation":"cmd-move-epic-entity-relations-1788389691788","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"epic:entity-relations@4#0~e3e875b0e74f0bd5","executor":null,"recorded_at":"2026-09-02T22:54:51Z"}}
{"entity":"epic","version":1,"id":"entity-relations","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"# Epic: An entity declares its relations, and the model checks them\n\n## Outcome\n\nA modeller writes *an Account owns many CommercialClients* as one declared relation on the entity, `ess validate` refuses it when the target, the carrying field or the cardinality do not agree, and every projection (JSON Schema, OpenAPI, Rust) carries the relation instead of each reader re-deriving it from an id field.\n\n## Why Now\n\nThe second adopter of the planning plugins lost a plan on exactly this: the relationship between an account and a commercial-client object was inconsistent across passes. The review of that failure found that ESS is the only typed place in either stack to write the relation down, and that ESS today has no construct for it: `grep -rn 'cardinality\\|association\\|foreign' crates/ess-domain/src` returns nothing, `EntitySpec` holds `name`, `identity`, `fields`, `states`, `invariants`, `naming` and nothing relational (`crates/ess-domain/src/entity.rs:595-617`), and the only cross-member checks are member-to-domain ownership and command-to-lifecycle causes (`crates/ess-domain/src/spec.rs:302-309`, `:385`). A relation is therefore a typed id field on the child plus an invariant somebody remembers to write. The planning guardrail that now routes a new noun to ESS (`agentplugins` 0.4.0) routes it to a place that cannot hold the one fact that was missing.\n\n## Scope\n\nA `relations:` list on an entity; its vocabulary decided on a design page first; validation in `ess-domain`; the generated schema; the three projections; the billing example; and the `aep reverse openapi` mapping (filed in `aep`, `story:reverse-openapi-emits-relations`) and the plugin rule that cites it (filed in `agentplugins`, `story:ess-schema-cites-relations`).\n\n## Out of Scope\n\n- Deletion and cascade semantics at runtime. ESS describes; the entity runtime decides. A relation may *name* an `on_delete` policy only if the design page decides it belongs in the specification at all.\n- A graph query language over relations. `ess` answers `validate`, `compile`, `diff`; a traversal verb is earned by a reader that needs it.\n- Migrating existing adopters' models. There are no `relations:` entries to migrate; existing id fields keep validating exactly as today.\n\n## Risks\n\n- The vocabulary is decided by writing code instead of on the design page, and the first adopter's model then has to change. Mitigation: `story:relations-design-page` lands first, and the decision-blocker on the vocabulary is cleared by the operator, not by the implementor.\n- Projections disagree on how a relation renders (OpenAPI has no native ownership construct). Mitigation: one extension key, named on the design page, used by all three.\n\n## Ambiguities\n\n- `requires-stakeholder-input` — the relation vocabulary: `owns` vs `references`, whether cardinality is `one|many` or `min/max`, and whether `on_delete` is in scope. Decides: the ESS owner. Raised as `decision-blocker:relation-vocabulary`.\n- `inferable` — the identity type a relation is carried by is the target's `identity.type` (`crates/ess-domain/src/entity.rs:604`).\n- `inferable` — every declared reference must resolve, and the assembler already refuses one that does not (`crates/ess-domain/src/spec.rs:215`, `:265` at the reviewed revision).\n\n## Done When\n\n`ess validate` refuses a relation whose target does not exist, whose carrying field has the wrong type, or whose cardinality contradicts the field's shape; `ess compile` emits the relation in all three projections; the billing example carries one ownership relation and its projections are checked in `task check`.\n"},"args":{"changes":{"body":"# Epic: An entity declares its relations, and the model checks them\n\n## Outcome\n\nA modeller writes *an Account owns many CommercialClients* as one declared relation on the entity, `ess validate` refuses it when the target, the carrying field or the cardinality do not agree, and every projection (JSON Schema, OpenAPI, Rust) carries the relation instead of each reader re-deriving it from an id field.\n\n## Why Now\n\nThe second adopter of the planning plugins lost a plan on exactly this: the relationship between an account and a commercial-client object was inconsistent across passes. The review of that failure found that ESS is the only typed place in either stack to write the relation down, and that ESS today has no construct for it: `grep -rn 'cardinality\\|association\\|foreign' crates/ess-domain/src` returns nothing, `EntitySpec` holds `name`, `identity`, `fields`, `states`, `invariants`, `naming` and nothing relational (`crates/ess-domain/src/entity.rs:595-617`), and the only cross-member checks are member-to-domain ownership and command-to-lifecycle causes (`crates/ess-domain/src/spec.rs:302-309`, `:385`). A relation is therefore a typed id field on the child plus an invariant somebody remembers to write. The planning guardrail that now routes a new noun to ESS (`agentplugins` 0.4.0) routes it to a place that cannot hold the one fact that was missing.\n\n## Scope\n\nA `relations:` list on an entity; its vocabulary decided on a design page first; validation in `ess-domain`; the generated schema; the three projections; the billing example; and the `aep reverse openapi` mapping (filed in `aep`, `story:reverse-openapi-emits-relations`) and the plugin rule that cites it (filed in `agentplugins`, `story:ess-schema-cites-relations`).\n\n## Out of Scope\n\n- Deletion and cascade semantics at runtime. ESS describes; the entity runtime decides. A relation may *name* an `on_delete` policy only if the design page decides it belongs in the specification at all.\n- A graph query language over relations. `ess` answers `validate`, `compile`, `diff`; a traversal verb is earned by a reader that needs it.\n- Migrating existing adopters' models. There are no `relations:` entries to migrate; existing id fields keep validating exactly as today.\n\n## Risks\n\n- The vocabulary is decided by writing code instead of on the design page, and the first adopter's model then has to change. Mitigation: `story:relations-design-page` lands first, and the decision-blocker on the vocabulary is cleared by the operator, not by the implementor.\n- Projections disagree on how a relation renders (OpenAPI has no native ownership construct). Mitigation: one extension key, named on the design page, used by all three.\n\n## Ambiguities\n\n- `requires-stakeholder-input` — the relation vocabulary: `owns` vs `references`, whether cardinality is `one|many` or `min/max`, and whether `on_delete` is in scope. Decides: the ESS owner. Raised as `decision-blocker:relation-vocabulary`.\n- `inferable` — the identity type a relation is carried by is the target's `identity.type` (`crates/ess-domain/src/entity.rs:604`).\n- `inferable` — every declared reference must resolve, and the assembler already refuses one that does not (`crates/ess-domain/src/spec.rs:215`, `:265` at the reviewed revision).\n\n## Done When\n\n`ess validate` refuses a relation whose target does not exist, whose carrying field has the wrong type, or whose cardinality contradicts the field's shape; `ess compile` emits the relation in all three projections; the billing example carries one ownership relation and its projections are checked in `task check`.\n"},"command":"update-entity","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788397668680,"causation":"cmd-protocol-artifact-body-epic-entity-relations-1788397668680","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"epic:entity-relations@5#0~2d3ecec846df759c","executor":null,"recorded_at":"2026-09-03T01:07:48Z"}}
{"entity":"specification","version":1,"id":"static-server-safe-generated-doc-paths","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Specification: Static-server-safe generated documentation paths\n\n## Context\n\nThis record migrates `beyond10x/ess` GitHub issue 2, “ess generate --kind docs emits filenames a static server cannot serve.” The issue was opened by `timofriedlberlin` on 2026-09-02 at 14:12:51Z, remained open with no labels, assignees, milestone, or comments, and described dotted domain names becoming dotted path segments that static servers interpret as extension-bearing filenames.\n\nNo implementation was added during this migration. Current `main` already satisfies the issue through commit `6cd3706ee2530a1806e74f169851d23667b2bdb7`, released in ESS 0.4.0 and every later release through 0.9.2. The implementation explains and performs the transformation at `crates/ess-gen/src/docs.rs:2502-2511`; generated-document tests exercise hyphenated domain paths beginning at `crates/ess-gen/tests/docs.rs:308`.\n\n## Requirements\n\n1. A domain page generated by `ess generate --kind docs` uses a static-server-safe path stem.\n2. Every dot in the qualified domain name becomes a hyphen in the path stem, so `acd.routing` is emitted as `domains/acd-routing.md`.\n3. Links generated for that domain use the same transformed page identity.\n4. The page content and title retain the qualified domain name; only its route identity changes.\n\n## Constraints\n\n- The transformation is deterministic and does not depend on a particular static-site generator.\n- The generated Markdown extension remains `.md`; only dots within the route stem are transformed.\n- A consumer must not need a post-processing rename-and-rewrite pass.\n\n## Out of Scope\n\n- Choosing a documentation site theme or static-site framework.\n- Renaming the domain inside the ESS model.\n- The broader `--kind site` projection that landed in the same implementation commit.\n\n## Invariants\n\n- A qualified domain name maps to one stable page identity everywhere the generator writes or links it.\n- A route stem generated from a dotted domain contains no dot.\n- Repeated generation from the same ESS IR produces the same paths and links.\n\n## Acceptance Criteria\n\n- Generating documentation for `acd.routing` produces `domains/acd-routing.md`, never `domains/acd.routing.md`.\n- Every link targeting the generated page resolves to the hyphenated path.\n- `task check` passes at the migrated repository revision.\n\n## Resolution\n\nImplemented before migration by `6cd3706ee2530a1806e74f169851d23667b2bdb7` and included in the published 0.4.0 release. This migration records the already-delivered behavior; it does not build it again.\n\n## Ambiguities\n\n- `inferable` — the requested filename mapping is explicit in the issue and in `domain_page_id` at `crates/ess-gen/src/docs.rs:2509-2510`.\n- `inferable` — the behavior is released: `git tag --contains 6cd3706` includes 0.4.0 through 0.9.2.\n\n## Open Questions\n\nNone.\n\n## Original GitHub Issue\n\nSource: `https://github.com/beyond10x/ess/issues/2`\n\n> `ess generate --kind docs` writes one page per domain, named after the domain. A domain called\n> `acd.routing` becomes `domains/acd.routing.md`, and the site built from it serves\n> `domains/acd.routing/`.\n>\n> A static file server reads the dot as an extension boundary and returns 404 for that path. This is\n> not a Docusaurus quirk — GitLab Pages, GitHub Pages and `http-server` all behave this way, because\n> the segment looks like a filename.\n>\n> **Observed** on `sbf/acd`, whose ESS model declares `acd.routing` and `acd.backend`. The published\n> site 404s on both domain pages until the stems are renamed and every link to them rewritten. The\n> repository carries that rename as a post-processing pass in `scripts/build-docs.sh`, which is a\n> workaround every consumer of `--kind docs` with a dotted domain name will have to write.\n>\n> ### What would fix it\n>\n> Emit the dot as something a path segment can carry — `domains/acd-routing.md` — and write the links\n> to match. The page's title still says `acd.routing`; only the filename changes.\n\n## Provenance\n\nMigrated from GitHub issue `beyond10x/ess#2`.\n\n- Created 2026-09-02T14:12:51Z · last updated 2026-09-02T14:12:51Z\n- Original state: `OPEN`\n- Original author: `timofriedlberlin`\n- Original issue body: 21 lines, 15 nonblank\n- Original labels, assignees, milestone, comments, and issue relations: none\n- AEP reference: `github:beyond10x/ess#2`\n","owner":"ess","refs":[{"provider":"github","reference":"beyond10x/ess#2"}],"summary":"Generated domain documentation uses a stable path that static file servers can serve.","tags":["github-issue-migration"],"title":"Static-server-safe generated documentation paths"},"args":{"command":"create-entity","data":{"body":"# Specification: Static-server-safe generated documentation paths\n\n## Context\n\nThis record migrates `beyond10x/ess` GitHub issue 2, “ess generate --kind docs emits filenames a static server cannot serve.” The issue was opened by `timofriedlberlin` on 2026-09-02 at 14:12:51Z, remained open with no labels, assignees, milestone, or comments, and described dotted domain names becoming dotted path segments that static servers interpret as extension-bearing filenames.\n\nNo implementation was added during this migration. Current `main` already satisfies the issue through commit `6cd3706ee2530a1806e74f169851d23667b2bdb7`, released in ESS 0.4.0 and every later release through 0.9.2. The implementation explains and performs the transformation at `crates/ess-gen/src/docs.rs:2502-2511`; generated-document tests exercise hyphenated domain paths beginning at `crates/ess-gen/tests/docs.rs:308`.\n\n## Requirements\n\n1. A domain page generated by `ess generate --kind docs` uses a static-server-safe path stem.\n2. Every dot in the qualified domain name becomes a hyphen in the path stem, so `acd.routing` is emitted as `domains/acd-routing.md`.\n3. Links generated for that domain use the same transformed page identity.\n4. The page content and title retain the qualified domain name; only its route identity changes.\n\n## Constraints\n\n- The transformation is deterministic and does not depend on a particular static-site generator.\n- The generated Markdown extension remains `.md`; only dots within the route stem are transformed.\n- A consumer must not need a post-processing rename-and-rewrite pass.\n\n## Out of Scope\n\n- Choosing a documentation site theme or static-site framework.\n- Renaming the domain inside the ESS model.\n- The broader `--kind site` projection that landed in the same implementation commit.\n\n## Invariants\n\n- A qualified domain name maps to one stable page identity everywhere the generator writes or links it.\n- A route stem generated from a dotted domain contains no dot.\n- Repeated generation from the same ESS IR produces the same paths and links.\n\n## Acceptance Criteria\n\n- Generating documentation for `acd.routing` produces `domains/acd-routing.md`, never `domains/acd.routing.md`.\n- Every link targeting the generated page resolves to the hyphenated path.\n- `task check` passes at the migrated repository revision.\n\n## Resolution\n\nImplemented before migration by `6cd3706ee2530a1806e74f169851d23667b2bdb7` and included in the published 0.4.0 release. This migration records the already-delivered behavior; it does not build it again.\n\n## Ambiguities\n\n- `inferable` — the requested filename mapping is explicit in the issue and in `domain_page_id` at `crates/ess-gen/src/docs.rs:2509-2510`.\n- `inferable` — the behavior is released: `git tag --contains 6cd3706` includes 0.4.0 through 0.9.2.\n\n## Open Questions\n\nNone.\n\n## Original GitHub Issue\n\nSource: `https://github.com/beyond10x/ess/issues/2`\n\n> `ess generate --kind docs` writes one page per domain, named after the domain. A domain called\n> `acd.routing` becomes `domains/acd.routing.md`, and the site built from it serves\n> `domains/acd.routing/`.\n>\n> A static file server reads the dot as an extension boundary and returns 404 for that path. This is\n> not a Docusaurus quirk — GitLab Pages, GitHub Pages and `http-server` all behave this way, because\n> the segment looks like a filename.\n>\n> **Observed** on `sbf/acd`, whose ESS model declares `acd.routing` and `acd.backend`. The published\n> site 404s on both domain pages until the stems are renamed and every link to them rewritten. The\n> repository carries that rename as a post-processing pass in `scripts/build-docs.sh`, which is a\n> workaround every consumer of `--kind docs` with a dotted domain name will have to write.\n>\n> ### What would fix it\n>\n> Emit the dot as something a path segment can carry — `domains/acd-routing.md` — and write the links\n> to match. The page's title still says `acd.routing`; only the filename changes.\n\n## Provenance\n\nMigrated from GitHub issue `beyond10x/ess#2`.\n\n- Created 2026-09-02T14:12:51Z · last updated 2026-09-02T14:12:51Z\n- Original state: `OPEN`\n- Original author: `timofriedlberlin`\n- Original issue body: 21 lines, 15 nonblank\n- Original labels, assignees, milestone, comments, and issue relations: none\n- AEP reference: `github:beyond10x/ess#2`\n","owner":"ess","refs":[{"provider":"github","reference":"beyond10x/ess#2"}],"status":"draft","summary":"Generated domain documentation uses a stable path that static file servers can serve.","tags":["github-issue-migration"],"title":"Static-server-safe generated documentation paths"},"entity_type":"aep.specification/v1","locator":"ep://planning/store/specification/static-server-safe-generated-doc-paths"},"payload":{"actor":"human:timo","at":1788446633797,"causation":"cmd-new-specification-static-server-safe-generated-doc-paths","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"specification:static-server-safe-generated-doc-paths@1#0~0828bbf67627e07e","executor":null,"recorded_at":"2026-09-03T14:43:53Z"}}
{"entity":"specification","version":1,"id":"construct-provenance-references","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Specification: Construct provenance references\n\n## Context\n\nThis record migrates `beyond10x/ess` GitHub issue 3, “A command, event or binding has nowhere to record why it exists.” The issue was opened by `timofriedlberlin` on 2026-09-02 at 14:13:07Z, remained open with no labels, assignees, milestone, or comments, and proposed both free-form `because` prose and structured external references on commands, events, errors, and bindings.\n\nNo implementation was added during this migration. Commit `9f13631d66583b56b08ce9bdeff01042842e5ade`, released in ESS 0.4.0 and every later release through 0.9.2, resolved the underlying provenance problem with a deliberately narrower contract: structured `provider:key` references on commands, outcomes, bindings, and components. It explicitly rejected a general `because` field because prose beside the external record can go stale. `crates/ess-domain/src/refs.rs:1-31` owns that decision and spelling; the typed fields are visible at `crates/ess-domain/src/binding.rs:166-170`, `crates/ess-domain/src/command.rs:793-797`, `:922-926`, `:1739-1743`, and `:1810-1814`.\n\nThe original proposal is preserved below, including the broader event/error and `because` requests. Those parts are recorded as deliberately outside the shipped resolution, not silently reported as implemented.\n\n## Requirements\n\n1. A supported ESS construct can name the external record that explains why it exists using a typed `provider:key` reference.\n2. Commands, outcomes, bindings, and components accept an ordered `refs` collection.\n3. References survive validation and compilation into resolved IR and reach `ess generate --kind docs`.\n4. An empty reference collection emits no `refs` key.\n5. A URL is refused where a `provider:key` reference is required; URL expansion belongs to a project-level provider map.\n6. Strict readers continue to deny unknown fields.\n\n## Constraints\n\n- ESS has no AEP dependency; the two systems share only the `provider:key` spelling.\n- References are typed values rather than arbitrary JSON or a generic extension bag.\n- The persisted ESS format changes only through an explicit format decision when compatibility rules require one.\n\n## Out of Scope\n\n- A general free-form `because` field on commands, outcomes, events, errors, bindings, or components. Commit `9f13631` rejects this in favor of the external record as the durable explanation.\n- `refs` on `RawEventSpec` or `RawErrorSpec`; the released implementation does not claim those surfaces (`crates/ess-domain/src/command.rs:1817-1842`).\n- Embedding tracker URLs in each construct.\n- Resolving provider keys to URLs inside the documentation projection.\n\n## Invariants\n\n- A reference round-trips without losing its provider or key.\n- The provider and key are non-empty and retain their literal spelling.\n- A reference is never mistaken for a URL.\n- All projections that claim construct provenance derive it from the same resolved typed reference.\n\n## Acceptance Criteria\n\n- `jira:DEV-630` parses and round-trips as one external reference.\n- malformed references and URL-shaped values are refused by name.\n- A specification containing references on supported constructs compiles and its generated documentation includes them.\n- `task check` passes at the migrated repository revision.\n\n## Resolution\n\nImplemented before migration by `9f13631d66583b56b08ce9bdeff01042842e5ade` as the narrower structured-reference contract above and included in the published 0.4.0 release. This migration records what was built and the explicit decision not to build the broader prose field; it does not pretend that every field proposed by the original issue shipped.\n\n## Ambiguities\n\n- `inferable` — the external-reference syntax and URL boundary are stated in `crates/ess-domain/src/refs.rs:15-29`.\n- `inferable` — the excluded `because` field is a deliberate decision in the implementation commit message, not an omission inferred by this migration.\n- `inferable` — the behavior is released: `git tag --contains 9f13631` includes 0.4.0 through 0.9.2.\n\n## Open Questions\n\nNone for the implemented specification. Adding provenance to events or errors would be new work and requires a new artifact rather than widening this completed record after migration.\n\n## Original GitHub Issue\n\nSource: `https://github.com/beyond10x/ess/issues/3`\n\n> `Conversion` requires `because` — prose stating why a crossing is allowed — and the reasoning in\n> `crates/ess-domain/src/types.rs:738-742` is that a conversion with no reason is “a silent widening\n> that someone added to make a build pass”.\n>\n> That reasoning applies to a command and a binding at least as strongly, and neither has anywhere to\n> put it. `RawCommandSpec`, `RawOutcome`, `RawEventSpec` and the binding types are all\n> `#[serde(deny_unknown_fields)]`, so there is no key to write into and no `extra` to fall through to.\n>\n> ### What this costs, concretely\n>\n> Modelling the ACD v3 → backend boundary in `sbf/acd` produced three bindings, each declaring\n> `delivery: at_least_once` and an `on_failure` escalation. Those declarations exist *because* the v1\n> event path silently drops every event other than two, which was found by reading\n> `adapters/backendsvc/svc.go:165`. That sentence is the most valuable thing about the binding and\n> there is nowhere in the model to write it — it lives in a hand-written `README.md` beside the spec,\n> where nothing checks it and `ess generate` cannot reach it.\n>\n> The same is true of a ticket id. A command that exists because of `DEV-630` cannot say so.\n>\n> ### What would fix it\n>\n> Two fields, both optional, on commands, events, errors and bindings:\n>\n> - `because:` — free prose, the same shape `Conversion` already has.\n> - `refs:` — external references, `provider:key`. AEP added exactly this to its planning artifacts in\n>   0.41.0 (`{provider: jira, reference: DEV-630}`, with the URL pattern configured once per project\n>   rather than written per artifact), and the two models would then agree on how a ticket is named.\n>\n> Both should reach `ess generate --kind docs`, since documenting the contract is what the generated\n> docs are for.\n\n## Provenance\n\nMigrated from GitHub issue `beyond10x/ess#3`.\n\n- Created 2026-09-02T14:13:07Z · last updated 2026-09-02T14:13:07Z\n- Original state: `OPEN`\n- Original author: `timofriedlberlin`\n- Original issue body: 31 lines, 22 nonblank\n- Original labels, assignees, milestone, comments, and issue relations: none\n- AEP reference: `github:beyond10x/ess#3`\n","owner":"ess","refs":[{"provider":"github","reference":"beyond10x/ess#3"}],"summary":"Supported ESS constructs retain typed external records explaining why they exist.","tags":["github-issue-migration"],"title":"Construct provenance references"},"args":{"command":"create-entity","data":{"body":"# Specification: Construct provenance references\n\n## Context\n\nThis record migrates `beyond10x/ess` GitHub issue 3, “A command, event or binding has nowhere to record why it exists.” The issue was opened by `timofriedlberlin` on 2026-09-02 at 14:13:07Z, remained open with no labels, assignees, milestone, or comments, and proposed both free-form `because` prose and structured external references on commands, events, errors, and bindings.\n\nNo implementation was added during this migration. Commit `9f13631d66583b56b08ce9bdeff01042842e5ade`, released in ESS 0.4.0 and every later release through 0.9.2, resolved the underlying provenance problem with a deliberately narrower contract: structured `provider:key` references on commands, outcomes, bindings, and components. It explicitly rejected a general `because` field because prose beside the external record can go stale. `crates/ess-domain/src/refs.rs:1-31` owns that decision and spelling; the typed fields are visible at `crates/ess-domain/src/binding.rs:166-170`, `crates/ess-domain/src/command.rs:793-797`, `:922-926`, `:1739-1743`, and `:1810-1814`.\n\nThe original proposal is preserved below, including the broader event/error and `because` requests. Those parts are recorded as deliberately outside the shipped resolution, not silently reported as implemented.\n\n## Requirements\n\n1. A supported ESS construct can name the external record that explains why it exists using a typed `provider:key` reference.\n2. Commands, outcomes, bindings, and components accept an ordered `refs` collection.\n3. References survive validation and compilation into resolved IR and reach `ess generate --kind docs`.\n4. An empty reference collection emits no `refs` key.\n5. A URL is refused where a `provider:key` reference is required; URL expansion belongs to a project-level provider map.\n6. Strict readers continue to deny unknown fields.\n\n## Constraints\n\n- ESS has no AEP dependency; the two systems share only the `provider:key` spelling.\n- References are typed values rather than arbitrary JSON or a generic extension bag.\n- The persisted ESS format changes only through an explicit format decision when compatibility rules require one.\n\n## Out of Scope\n\n- A general free-form `because` field on commands, outcomes, events, errors, bindings, or components. Commit `9f13631` rejects this in favor of the external record as the durable explanation.\n- `refs` on `RawEventSpec` or `RawErrorSpec`; the released implementation does not claim those surfaces (`crates/ess-domain/src/command.rs:1817-1842`).\n- Embedding tracker URLs in each construct.\n- Resolving provider keys to URLs inside the documentation projection.\n\n## Invariants\n\n- A reference round-trips without losing its provider or key.\n- The provider and key are non-empty and retain their literal spelling.\n- A reference is never mistaken for a URL.\n- All projections that claim construct provenance derive it from the same resolved typed reference.\n\n## Acceptance Criteria\n\n- `jira:DEV-630` parses and round-trips as one external reference.\n- malformed references and URL-shaped values are refused by name.\n- A specification containing references on supported constructs compiles and its generated documentation includes them.\n- `task check` passes at the migrated repository revision.\n\n## Resolution\n\nImplemented before migration by `9f13631d66583b56b08ce9bdeff01042842e5ade` as the narrower structured-reference contract above and included in the published 0.4.0 release. This migration records what was built and the explicit decision not to build the broader prose field; it does not pretend that every field proposed by the original issue shipped.\n\n## Ambiguities\n\n- `inferable` — the external-reference syntax and URL boundary are stated in `crates/ess-domain/src/refs.rs:15-29`.\n- `inferable` — the excluded `because` field is a deliberate decision in the implementation commit message, not an omission inferred by this migration.\n- `inferable` — the behavior is released: `git tag --contains 9f13631` includes 0.4.0 through 0.9.2.\n\n## Open Questions\n\nNone for the implemented specification. Adding provenance to events or errors would be new work and requires a new artifact rather than widening this completed record after migration.\n\n## Original GitHub Issue\n\nSource: `https://github.com/beyond10x/ess/issues/3`\n\n> `Conversion` requires `because` — prose stating why a crossing is allowed — and the reasoning in\n> `crates/ess-domain/src/types.rs:738-742` is that a conversion with no reason is “a silent widening\n> that someone added to make a build pass”.\n>\n> That reasoning applies to a command and a binding at least as strongly, and neither has anywhere to\n> put it. `RawCommandSpec`, `RawOutcome`, `RawEventSpec` and the binding types are all\n> `#[serde(deny_unknown_fields)]`, so there is no key to write into and no `extra` to fall through to.\n>\n> ### What this costs, concretely\n>\n> Modelling the ACD v3 → backend boundary in `sbf/acd` produced three bindings, each declaring\n> `delivery: at_least_once` and an `on_failure` escalation. Those declarations exist *because* the v1\n> event path silently drops every event other than two, which was found by reading\n> `adapters/backendsvc/svc.go:165`. That sentence is the most valuable thing about the binding and\n> there is nowhere in the model to write it — it lives in a hand-written `README.md` beside the spec,\n> where nothing checks it and `ess generate` cannot reach it.\n>\n> The same is true of a ticket id. A command that exists because of `DEV-630` cannot say so.\n>\n> ### What would fix it\n>\n> Two fields, both optional, on commands, events, errors and bindings:\n>\n> - `because:` — free prose, the same shape `Conversion` already has.\n> - `refs:` — external references, `provider:key`. AEP added exactly this to its planning artifacts in\n>   0.41.0 (`{provider: jira, reference: DEV-630}`, with the URL pattern configured once per project\n>   rather than written per artifact), and the two models would then agree on how a ticket is named.\n>\n> Both should reach `ess generate --kind docs`, since documenting the contract is what the generated\n> docs are for.\n\n## Provenance\n\nMigrated from GitHub issue `beyond10x/ess#3`.\n\n- Created 2026-09-02T14:13:07Z · last updated 2026-09-02T14:13:07Z\n- Original state: `OPEN`\n- Original author: `timofriedlberlin`\n- Original issue body: 31 lines, 22 nonblank\n- Original labels, assignees, milestone, comments, and issue relations: none\n- AEP reference: `github:beyond10x/ess#3`\n","owner":"ess","refs":[{"provider":"github","reference":"beyond10x/ess#3"}],"status":"draft","summary":"Supported ESS constructs retain typed external records explaining why they exist.","tags":["github-issue-migration"],"title":"Construct provenance references"},"entity_type":"aep.specification/v1","locator":"ep://planning/store/specification/construct-provenance-references"},"payload":{"actor":"human:timo","at":1788446633933,"causation":"cmd-new-specification-construct-provenance-references","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"specification:construct-provenance-references@1#0~f391e246b566d533","executor":null,"recorded_at":"2026-09-03T14:43:53Z"}}
{"entity":"specification","version":1,"id":"static-server-safe-generated-doc-paths","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"in_review","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000010","to":"in_review"},"payload":{"actor":"human:timo","at":1788446648305,"causation":"cmd-move-specification-static-server-safe-generated-doc-paths-1788446648305","change":{"change":"moved","decided_on":{},"from":"draft","to":"in_review"},"correlation":"protocol-artifact-move","event_id":"specification:static-server-safe-generated-doc-paths@2#0~9b3cca1dcc43274f","executor":null,"recorded_at":"2026-09-03T14:44:08Z"}}
{"entity":"specification","version":1,"id":"static-server-safe-generated-doc-paths","revision":2,"type":"aep.evidence.record/v1","from_state":"in_review","to_state":"in_review","changed":{},"args":{"command":"record-evidence","kind":"approval","source":"operator accepted the GitHub issue migration classification in this Codex session","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788446648000,"causation":"cmd-evidence-specification-static-server-safe-generated-doc-paths-approval-1788446648378","change":{"change":"evidence","kind":"approval","source":"operator accepted the GitHub issue migration classification in this Codex session"},"correlation":"protocol-artifact-evidence","event_id":"specification:static-server-safe-generated-doc-paths@2#0~2dbf3647ba3197db","executor":null,"recorded_at":"2026-09-03T14:44:08Z"}}
{"entity":"specification","version":1,"id":"static-server-safe-generated-doc-paths","revision":3,"type":"aep.status.move/v1","from_state":"in_review","to_state":"approved","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"approval\":1}}","target":"01MEM0000000000000010","to":"approved"},"payload":{"actor":"human:timo","at":1788446648515,"causation":"cmd-move-specification-static-server-safe-generated-doc-paths-1788446648515","change":{"change":"moved","decided_on":{"recorded":{"approval":1}},"from":"in_review","to":"approved"},"correlation":"protocol-artifact-move","event_id":"specification:static-server-safe-generated-doc-paths@3#0~d1c5bad6f424a980","executor":null,"recorded_at":"2026-09-03T14:44:08Z"}}
{"entity":"specification","version":1,"id":"construct-provenance-references","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"in_review","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000007","to":"in_review"},"payload":{"actor":"human:timo","at":1788446648656,"causation":"cmd-move-specification-construct-provenance-references-1788446648656","change":{"change":"moved","decided_on":{},"from":"draft","to":"in_review"},"correlation":"protocol-artifact-move","event_id":"specification:construct-provenance-references@2#0~916767c8e35c9697","executor":null,"recorded_at":"2026-09-03T14:44:08Z"}}
{"entity":"specification","version":1,"id":"construct-provenance-references","revision":2,"type":"aep.evidence.record/v1","from_state":"in_review","to_state":"in_review","changed":{},"args":{"command":"record-evidence","kind":"approval","source":"operator accepted the GitHub issue migration classification in this Codex session","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788446648000,"causation":"cmd-evidence-specification-construct-provenance-references-approval-1788446648735","change":{"change":"evidence","kind":"approval","source":"operator accepted the GitHub issue migration classification in this Codex session"},"correlation":"protocol-artifact-evidence","event_id":"specification:construct-provenance-references@2#0~7355b14e29f6d7ab","executor":null,"recorded_at":"2026-09-03T14:44:08Z"}}
{"entity":"specification","version":1,"id":"construct-provenance-references","revision":3,"type":"aep.status.move/v1","from_state":"in_review","to_state":"approved","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"approval\":1}}","target":"01MEM0000000000000007","to":"approved"},"payload":{"actor":"human:timo","at":1788446648870,"causation":"cmd-move-specification-construct-provenance-references-1788446648870","change":{"change":"moved","decided_on":{"recorded":{"approval":1}},"from":"in_review","to":"approved"},"correlation":"protocol-artifact-move","event_id":"specification:construct-provenance-references@3#0~08d6055bfed72a70","executor":null,"recorded_at":"2026-09-03T14:44:08Z"}}
{"entity":"specification","version":1,"id":"static-server-safe-generated-doc-paths","revision":3,"type":"aep.evidence.record/v1","from_state":"approved","to_state":"approved","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"local ESS worktree at 6ef4af76b99a8d2cd861a3cc76140c88c1361129 plus migration artifacts","source":"task check (exit 0)","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788446746000,"causation":"cmd-evidence-specification-static-server-safe-generated-doc-paths-test_result-1788446746273","change":{"change":"evidence","kind":"test_result","reference":"local ESS worktree at 6ef4af76b99a8d2cd861a3cc76140c88c1361129 plus migration artifacts","source":"task check (exit 0)"},"correlation":"protocol-artifact-evidence","event_id":"specification:static-server-safe-generated-doc-paths@3#0~d8d4623aec6957ab","executor":null,"recorded_at":"2026-09-03T14:45:46Z"}}
{"entity":"specification","version":1,"id":"static-server-safe-generated-doc-paths","revision":4,"type":"aep.status.move/v1","from_state":"approved","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"approval\":1}}","target":"01MEM0000000000000010","to":"implemented"},"payload":{"actor":"human:timo","at":1788446746410,"causation":"cmd-move-specification-static-server-safe-generated-doc-paths-1788446746410","change":{"change":"moved","decided_on":{"recorded":{"approval":1,"test_result":1}},"from":"approved","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"specification:static-server-safe-generated-doc-paths@4#0~cfd0a23facbbdabc","executor":null,"recorded_at":"2026-09-03T14:45:46Z"}}
{"entity":"specification","version":1,"id":"construct-provenance-references","revision":3,"type":"aep.evidence.record/v1","from_state":"approved","to_state":"approved","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"local ESS worktree at 6ef4af76b99a8d2cd861a3cc76140c88c1361129 plus migration artifacts","source":"task check (exit 0)","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788446746000,"causation":"cmd-evidence-specification-construct-provenance-references-test_result-1788446746488","change":{"change":"evidence","kind":"test_result","reference":"local ESS worktree at 6ef4af76b99a8d2cd861a3cc76140c88c1361129 plus migration artifacts","source":"task check (exit 0)"},"correlation":"protocol-artifact-evidence","event_id":"specification:construct-provenance-references@3#0~74025d43d0e790bb","executor":null,"recorded_at":"2026-09-03T14:45:46Z"}}
{"entity":"specification","version":1,"id":"construct-provenance-references","revision":4,"type":"aep.status.move/v1","from_state":"approved","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"approval\":1}}","target":"01MEM0000000000000007","to":"implemented"},"payload":{"actor":"human:timo","at":1788446746624,"causation":"cmd-move-specification-construct-provenance-references-1788446746624","change":{"change":"moved","decided_on":{"recorded":{"approval":1,"test_result":1}},"from":"approved","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"specification:construct-provenance-references@4#0~c34617f370bde984","executor":null,"recorded_at":"2026-09-03T14:45:46Z"}}
{"entity":"specification","version":1,"id":"construct-provenance-references","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"# Specification: Construct provenance references\n\n## Context\n\nThis record migrates `beyond10x/ess` GitHub issue 3, “A command, event or binding has nowhere to record why it exists.” The issue was opened by `timofriedlberlin` on 2026-09-02 at 14:13:07Z, remained open with no labels, assignees, milestone, or comments, and proposed both free-form `because` prose and structured external references on commands, events, errors, and bindings.\n\nNo implementation was added during this migration. Commit `9f13631d66583b56b08ce9bdeff01042842e5ade`, released in ESS 0.4.0 and every later release through 0.9.2, resolved the underlying provenance problem with a deliberately narrower contract: structured `provider:key` references on commands, outcomes, bindings, and components. It explicitly rejected a general `because` field because prose beside the external record can go stale. `crates/ess-domain/src/refs.rs:1-31` owns that decision and spelling; the typed fields are visible at `crates/ess-domain/src/binding.rs:166-170`, `crates/ess-domain/src/command.rs:793-797`, `:922-926`, `:1739-1743`, and `:1810-1814`.\n\nThe original proposal is preserved below, including the broader event/error and `because` requests. Those parts are recorded as deliberately outside the shipped resolution, not silently reported as implemented.\n\n## Requirements\n\n1. A supported ESS construct can name the external record that explains why it exists using a typed `provider:key` reference.\n2. Commands, outcomes, bindings, and components accept an ordered `refs` collection.\n3. References survive validation and compilation into resolved IR and reach `ess generate --kind docs`.\n4. An empty reference collection emits no `refs` key.\n5. A URL is refused where a `provider:key` reference is required; URL expansion belongs to a project-level provider map.\n6. Strict readers continue to deny unknown fields.\n\n## Constraints\n\n- ESS has no AEP dependency; the two systems share only the `provider:key` spelling.\n- References are typed values rather than arbitrary JSON or a generic extension bag.\n- The persisted ESS format changes only through an explicit format decision when compatibility rules require one.\n\n## Out of Scope\n\n- A general free-form `because` field on commands, outcomes, events, errors, bindings, or components. Commit `9f13631` rejects this in favor of the external record as the durable explanation.\n- `refs` on `RawEventSpec` or `RawErrorSpec`; the released implementation does not claim those surfaces (`crates/ess-domain/src/command.rs:1817-1842`).\n- Embedding tracker URLs in each construct.\n- Resolving provider keys to URLs inside the documentation projection.\n\n## Invariants\n\n- A reference round-trips without losing its provider or key.\n- The provider and key are non-empty and retain their literal spelling.\n- A reference is never mistaken for a URL.\n- All projections that claim construct provenance derive it from the same resolved typed reference.\n\n## Acceptance Criteria\n\n- `jira:DEV-630` parses and round-trips as one external reference.\n- malformed references and URL-shaped values are refused by name.\n- A specification containing references on supported constructs compiles and its generated documentation includes them.\n- `task check` passes at the migrated repository revision.\n\n## Resolution\n\nImplemented before migration by `9f13631d66583b56b08ce9bdeff01042842e5ade` as the narrower structured-reference contract above and included in the published 0.4.0 release. This migration records what was built and the explicit decision not to build the broader prose field; it does not pretend that every field proposed by the original issue shipped.\n\n## Ambiguities\n\n- `inferable` — the external-reference syntax and URL boundary are stated in `crates/ess-domain/src/refs.rs:15-29`.\n- `inferable` — the excluded `because` field is a deliberate decision in the implementation commit message, not an omission inferred by this migration.\n- `inferable` — the behavior is released: `git tag --contains 9f13631` includes 0.4.0 through 0.9.2.\n\n## Open Questions\n\nNone for the implemented specification. Adding provenance to events or errors would be new work and requires a new artifact rather than widening this completed record after migration.\n\n## Original GitHub Issue\n\nSource: `https://github.com/beyond10x/ess/issues/3`\n\n> `Conversion` requires `because` — prose stating why a crossing is allowed — and the reasoning in\n> `crates/ess-domain/src/types.rs:738-742` is that a conversion with no reason is \"a silent widening\n> that someone added to make a build pass\".\n>\n> That reasoning applies to a command and a binding at least as strongly, and neither has anywhere to\n> put it. `RawCommandSpec`, `RawOutcome`, `RawEventSpec` and the binding types are all\n> `#[serde(deny_unknown_fields)]`, so there is no key to write into and no `extra` to fall through to.\n>\n> ### What this costs, concretely\n>\n> Modelling the ACD v3 → backend boundary in `sbf/acd` produced three bindings, each declaring\n> `delivery: at_least_once` and an `on_failure` escalation. Those declarations exist *because* the v1\n> event path silently drops every event other than two, which was found by reading\n> `adapters/backendsvc/svc.go:165`. That sentence is the most valuable thing about the binding and\n> there is nowhere in the model to write it — it lives in a hand-written `README.md` beside the spec,\n> where nothing checks it and `ess generate` cannot reach it.\n>\n> The same is true of a ticket id. A command that exists because of `DEV-630` cannot say so.\n>\n> ### What would fix it\n>\n> Two fields, both optional, on commands, events, errors and bindings:\n>\n> - `because:` — free prose, the same shape `Conversion` already has.\n> - `refs:` — external references, `provider:key`. AEP added exactly this to its planning artifacts in\n>   0.41.0 (`{provider: jira, reference: DEV-630}`, with the URL pattern configured once per project\n>   rather than written per artifact), and the two models would then agree on how a ticket is named.\n>\n> Both should reach `ess generate --kind docs`, since documenting the contract is what the generated\n> docs are for.\n\n## Provenance\n\nMigrated from GitHub issue `beyond10x/ess#3`.\n\n- Created 2026-09-02T14:13:07Z · last updated 2026-09-02T14:13:07Z\n- Original state: `OPEN`\n- Original author: `timofriedlberlin`\n- Original issue body: 31 lines, 22 nonblank\n- Original labels, assignees, milestone, comments, and issue relations: none\n- AEP reference: `github:beyond10x/ess#3`\n"},"args":{"changes":{"body":"# Specification: Construct provenance references\n\n## Context\n\nThis record migrates `beyond10x/ess` GitHub issue 3, “A command, event or binding has nowhere to record why it exists.” The issue was opened by `timofriedlberlin` on 2026-09-02 at 14:13:07Z, remained open with no labels, assignees, milestone, or comments, and proposed both free-form `because` prose and structured external references on commands, events, errors, and bindings.\n\nNo implementation was added during this migration. Commit `9f13631d66583b56b08ce9bdeff01042842e5ade`, released in ESS 0.4.0 and every later release through 0.9.2, resolved the underlying provenance problem with a deliberately narrower contract: structured `provider:key` references on commands, outcomes, bindings, and components. It explicitly rejected a general `because` field because prose beside the external record can go stale. `crates/ess-domain/src/refs.rs:1-31` owns that decision and spelling; the typed fields are visible at `crates/ess-domain/src/binding.rs:166-170`, `crates/ess-domain/src/command.rs:793-797`, `:922-926`, `:1739-1743`, and `:1810-1814`.\n\nThe original proposal is preserved below, including the broader event/error and `because` requests. Those parts are recorded as deliberately outside the shipped resolution, not silently reported as implemented.\n\n## Requirements\n\n1. A supported ESS construct can name the external record that explains why it exists using a typed `provider:key` reference.\n2. Commands, outcomes, bindings, and components accept an ordered `refs` collection.\n3. References survive validation and compilation into resolved IR and reach `ess generate --kind docs`.\n4. An empty reference collection emits no `refs` key.\n5. A URL is refused where a `provider:key` reference is required; URL expansion belongs to a project-level provider map.\n6. Strict readers continue to deny unknown fields.\n\n## Constraints\n\n- ESS has no AEP dependency; the two systems share only the `provider:key` spelling.\n- References are typed values rather than arbitrary JSON or a generic extension bag.\n- The persisted ESS format changes only through an explicit format decision when compatibility rules require one.\n\n## Out of Scope\n\n- A general free-form `because` field on commands, outcomes, events, errors, bindings, or components. Commit `9f13631` rejects this in favor of the external record as the durable explanation.\n- `refs` on `RawEventSpec` or `RawErrorSpec`; the released implementation does not claim those surfaces (`crates/ess-domain/src/command.rs:1817-1842`).\n- Embedding tracker URLs in each construct.\n- Resolving provider keys to URLs inside the documentation projection.\n\n## Invariants\n\n- A reference round-trips without losing its provider or key.\n- The provider and key are non-empty and retain their literal spelling.\n- A reference is never mistaken for a URL.\n- All projections that claim construct provenance derive it from the same resolved typed reference.\n\n## Acceptance Criteria\n\n- `jira:DEV-630` parses and round-trips as one external reference.\n- malformed references and URL-shaped values are refused by name.\n- A specification containing references on supported constructs compiles and its generated documentation includes them.\n- `task check` passes at the migrated repository revision.\n\n## Resolution\n\nImplemented before migration by `9f13631d66583b56b08ce9bdeff01042842e5ade` as the narrower structured-reference contract above and included in the published 0.4.0 release. This migration records what was built and the explicit decision not to build the broader prose field; it does not pretend that every field proposed by the original issue shipped.\n\n## Ambiguities\n\n- `inferable` — the external-reference syntax and URL boundary are stated in `crates/ess-domain/src/refs.rs:15-29`.\n- `inferable` — the excluded `because` field is a deliberate decision in the implementation commit message, not an omission inferred by this migration.\n- `inferable` — the behavior is released: `git tag --contains 9f13631` includes 0.4.0 through 0.9.2.\n\n## Open Questions\n\nNone for the implemented specification. Adding provenance to events or errors would be new work and requires a new artifact rather than widening this completed record after migration.\n\n## Original GitHub Issue\n\nSource: `https://github.com/beyond10x/ess/issues/3`\n\n> `Conversion` requires `because` — prose stating why a crossing is allowed — and the reasoning in\n> `crates/ess-domain/src/types.rs:738-742` is that a conversion with no reason is \"a silent widening\n> that someone added to make a build pass\".\n>\n> That reasoning applies to a command and a binding at least as strongly, and neither has anywhere to\n> put it. `RawCommandSpec`, `RawOutcome`, `RawEventSpec` and the binding types are all\n> `#[serde(deny_unknown_fields)]`, so there is no key to write into and no `extra` to fall through to.\n>\n> ### What this costs, concretely\n>\n> Modelling the ACD v3 → backend boundary in `sbf/acd` produced three bindings, each declaring\n> `delivery: at_least_once` and an `on_failure` escalation. Those declarations exist *because* the v1\n> event path silently drops every event other than two, which was found by reading\n> `adapters/backendsvc/svc.go:165`. That sentence is the most valuable thing about the binding and\n> there is nowhere in the model to write it — it lives in a hand-written `README.md` beside the spec,\n> where nothing checks it and `ess generate` cannot reach it.\n>\n> The same is true of a ticket id. A command that exists because of `DEV-630` cannot say so.\n>\n> ### What would fix it\n>\n> Two fields, both optional, on commands, events, errors and bindings:\n>\n> - `because:` — free prose, the same shape `Conversion` already has.\n> - `refs:` — external references, `provider:key`. AEP added exactly this to its planning artifacts in\n>   0.41.0 (`{provider: jira, reference: DEV-630}`, with the URL pattern configured once per project\n>   rather than written per artifact), and the two models would then agree on how a ticket is named.\n>\n> Both should reach `ess generate --kind docs`, since documenting the contract is what the generated\n> docs are for.\n\n## Provenance\n\nMigrated from GitHub issue `beyond10x/ess#3`.\n\n- Created 2026-09-02T14:13:07Z · last updated 2026-09-02T14:13:07Z\n- Original state: `OPEN`\n- Original author: `timofriedlberlin`\n- Original issue body: 31 lines, 22 nonblank\n- Original labels, assignees, milestone, comments, and issue relations: none\n- AEP reference: `github:beyond10x/ess#3`\n"},"command":"update-entity","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788446797654,"causation":"cmd-protocol-artifact-body-specification-construct-provenance-references-1788446797654","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"specification:construct-provenance-references@5#0~4e9ec09d729f7fe2","executor":null,"recorded_at":"2026-09-03T14:46:37Z"}}
{"entity":"story","version":1,"id":"a-skipped-scenario-is-not-a-failed-one","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## What is wrong\n\n`ess-conformance-report/1` carries `scenarios_failed`, and the Go runner counts **skipped**\nscenarios in it.\n\n`crates/ess-conformance/src/go/runtime.go:465-476`:\n\n```go\nfailed := make([]string, 0)\nfor _, result := range results {\n    switch result.status {\n    case statusPassed:\n        continue\n    case statusFailed:\n        anyFailed = true\n    case statusSkipped:\n        anySkipped = true\n    }\n    failed = append(failed, result.status+\" \"+result.id)\n}\n```\n\nBoth non-passing statuses land in `failed`, and `ScenariosFailed: len(failed)` publishes the count\nunder a name that says one of them.\n\nMeasured on `sbf/acd`, 2026-09-03, ess 0.11.0:\n\n```json\n{\"status\": \"inconclusive\", \"scenarios_total\": 24, \"scenarios_failed\": 9}\n```\n\nNothing failed. Nine scenarios were skipped, all of them `acd.routing.DispatchCall`, which that\nimplementation exposes no surface for.\n\n## Why it is worth fixing rather than documenting\n\n`failed_scenarios` — the **list** — is honest: every entry is prefixed with its own status, so a\nreader who opens it sees `skipped …`. The count is what consumers read, and a consumer cannot\nrecover the split from it.\n\nAEP's `ess-conformance` principle\n(`principles/verification/ess-conformance.yaml:28`) asserts:\n\n```yaml\npredicates:\n  - ess_conformance.passed\n  - ess_conformance.scenarios.failed == 0\n```\n\nThe second reads this number. As it stands, a suite with any skip can never satisfy it — which may\neven be the wanted rule, but it is being enforced by the wrong fact. `ess_conformance.passed` is\nalready false for an `inconclusive` run and already carries that claim; `scenarios.failed` is\nsupposed to carry a different one and does not.\n\nRecorded downstream in `sbf/acd` at\n`.engineering/planning/executable-system-specification/acd-v3.md`, where the consequence is that\nthe artifact cannot honestly be moved to `conforming` and the reason is two-thirds wording.\n\n## Acceptance\n\n- `scenarios_failed` counts scenarios whose status is `failed`.\n- The report carries `scenarios_skipped` beside it, so the split is readable without parsing\n  `failed_scenarios`.\n- `status` is unchanged: `passed`, `failed`, `inconclusive` already distinguish the three cases and\n  that part was right.\n- A test that a run with a skip and no failure reports `scenarios_failed: 0`.\n- The Rust runner is checked for the same defect rather than assumed clean.\n","title":"A conformance report counts skipped scenarios as failed"},"args":{"command":"create-entity","data":{"body":"## What is wrong\n\n`ess-conformance-report/1` carries `scenarios_failed`, and the Go runner counts **skipped**\nscenarios in it.\n\n`crates/ess-conformance/src/go/runtime.go:465-476`:\n\n```go\nfailed := make([]string, 0)\nfor _, result := range results {\n    switch result.status {\n    case statusPassed:\n        continue\n    case statusFailed:\n        anyFailed = true\n    case statusSkipped:\n        anySkipped = true\n    }\n    failed = append(failed, result.status+\" \"+result.id)\n}\n```\n\nBoth non-passing statuses land in `failed`, and `ScenariosFailed: len(failed)` publishes the count\nunder a name that says one of them.\n\nMeasured on `sbf/acd`, 2026-09-03, ess 0.11.0:\n\n```json\n{\"status\": \"inconclusive\", \"scenarios_total\": 24, \"scenarios_failed\": 9}\n```\n\nNothing failed. Nine scenarios were skipped, all of them `acd.routing.DispatchCall`, which that\nimplementation exposes no surface for.\n\n## Why it is worth fixing rather than documenting\n\n`failed_scenarios` — the **list** — is honest: every entry is prefixed with its own status, so a\nreader who opens it sees `skipped …`. The count is what consumers read, and a consumer cannot\nrecover the split from it.\n\nAEP's `ess-conformance` principle\n(`principles/verification/ess-conformance.yaml:28`) asserts:\n\n```yaml\npredicates:\n  - ess_conformance.passed\n  - ess_conformance.scenarios.failed == 0\n```\n\nThe second reads this number. As it stands, a suite with any skip can never satisfy it — which may\neven be the wanted rule, but it is being enforced by the wrong fact. `ess_conformance.passed` is\nalready false for an `inconclusive` run and already carries that claim; `scenarios.failed` is\nsupposed to carry a different one and does not.\n\nRecorded downstream in `sbf/acd` at\n`.engineering/planning/executable-system-specification/acd-v3.md`, where the consequence is that\nthe artifact cannot honestly be moved to `conforming` and the reason is two-thirds wording.\n\n## Acceptance\n\n- `scenarios_failed` counts scenarios whose status is `failed`.\n- The report carries `scenarios_skipped` beside it, so the split is readable without parsing\n  `failed_scenarios`.\n- `status` is unchanged: `passed`, `failed`, `inconclusive` already distinguish the three cases and\n  that part was right.\n- A test that a run with a skip and no failure reports `scenarios_failed: 0`.\n- The Rust runner is checked for the same defect rather than assumed clean.\n","status":"draft","title":"A conformance report counts skipped scenarios as failed"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/a-skipped-scenario-is-not-a-failed-one"},"payload":{"actor":"human:timo","at":1788471790044,"causation":"cmd-new-story-a-skipped-scenario-is-not-a-failed-one","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:a-skipped-scenario-is-not-a-failed-one@1#0~3418ae9412e4de64","executor":null,"recorded_at":"2026-09-03T21:43:10Z"}}
{"entity":"story","version":1,"id":"the-generated-go-runtime-is-gofmt-clean","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## What is wrong\n\n`crates/ess-conformance/src/go/runtime.go` is a Go file inside a Rust crate. `cargo fmt` does not\nsee it, `task check` does not `gofmt` it, and its struct tags are aligned by whoever last edited\nthem.\n\nThe generator copies it out verbatim. So an adopter who runs `gofmt` over the tree it was written\ninto — which is a normal thing to do while editing the hand-written half beside it — changes bytes\nthe generator did not choose, and the next `ess conform synthesize` puts them back.\n\nObserved in `sbf/acd` on 2026-09-03: `gofmt -w tests/conformance/` while editing\n`tests/conformance/target.go` reformatted `tests/conformance/essconform/runtime.go` beside it. The\nreformatted bytes were committed and pushed, and `task ess:check` — which regenerates and compares\n— went red on a tree nobody had meaningfully changed.\n\n```diff\n-\tPayload map[string]Node `json:\"payload,omitempty\"`\n-\tShape   map[string]Held `json:\"shape,omitempty\"`\n+\tPayload     map[string]Node `json:\"payload,omitempty\"`\n+\tShape       map[string]Held `json:\"shape,omitempty\"`\n```\n\nNine lines, no semantic difference, and a red drift check on the adopter's branch.\n\n## Why it is this crate's problem and not the adopter's\n\nEvery adopter of the Go target has a directory holding one generated file and one hand-written\nfile, and `gofmt` is directory-scoped. Telling each of them *do not format that directory* is a\nconvention in a comment; emitting bytes `gofmt` already agrees with is a property.\n\n## Acceptance\n\n- `crates/ess-conformance/src/go/runtime.go` and `predicate.go` are `gofmt`-clean.\n- `task check` fails when they are not — a step that runs `gofmt -l` over\n  `crates/ess-conformance/src/go/` and refuses non-empty output. Go is not a build dependency of\n  this workspace, so this step is skipped rather than failed where `gofmt` is absent, and says so.\n- The generated tree in `sbf/acd` is byte-identical before and after `gofmt -w`.\n","title":"The emitted Go runtime is not gofmt-stable, so an adopter's formatter changes it"},"args":{"command":"create-entity","data":{"body":"## What is wrong\n\n`crates/ess-conformance/src/go/runtime.go` is a Go file inside a Rust crate. `cargo fmt` does not\nsee it, `task check` does not `gofmt` it, and its struct tags are aligned by whoever last edited\nthem.\n\nThe generator copies it out verbatim. So an adopter who runs `gofmt` over the tree it was written\ninto — which is a normal thing to do while editing the hand-written half beside it — changes bytes\nthe generator did not choose, and the next `ess conform synthesize` puts them back.\n\nObserved in `sbf/acd` on 2026-09-03: `gofmt -w tests/conformance/` while editing\n`tests/conformance/target.go` reformatted `tests/conformance/essconform/runtime.go` beside it. The\nreformatted bytes were committed and pushed, and `task ess:check` — which regenerates and compares\n— went red on a tree nobody had meaningfully changed.\n\n```diff\n-\tPayload map[string]Node `json:\"payload,omitempty\"`\n-\tShape   map[string]Held `json:\"shape,omitempty\"`\n+\tPayload     map[string]Node `json:\"payload,omitempty\"`\n+\tShape       map[string]Held `json:\"shape,omitempty\"`\n```\n\nNine lines, no semantic difference, and a red drift check on the adopter's branch.\n\n## Why it is this crate's problem and not the adopter's\n\nEvery adopter of the Go target has a directory holding one generated file and one hand-written\nfile, and `gofmt` is directory-scoped. Telling each of them *do not format that directory* is a\nconvention in a comment; emitting bytes `gofmt` already agrees with is a property.\n\n## Acceptance\n\n- `crates/ess-conformance/src/go/runtime.go` and `predicate.go` are `gofmt`-clean.\n- `task check` fails when they are not — a step that runs `gofmt -l` over\n  `crates/ess-conformance/src/go/` and refuses non-empty output. Go is not a build dependency of\n  this workspace, so this step is skipped rather than failed where `gofmt` is absent, and says so.\n- The generated tree in `sbf/acd` is byte-identical before and after `gofmt -w`.\n","status":"draft","title":"The emitted Go runtime is not gofmt-stable, so an adopter's formatter changes it"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-generated-go-runtime-is-gofmt-clean"},"payload":{"actor":"human:timo","at":1788471806007,"causation":"cmd-new-story-the-generated-go-runtime-is-gofmt-clean","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-generated-go-runtime-is-gofmt-clean@1#0~880e5c0c44be3888","executor":null,"recorded_at":"2026-09-03T21:43:26Z"}}
{"entity":"epic","version":1,"id":"area-layout","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Epic: Crates grouped by bounded context\n\n## Outcome\n\n`crates/` shows the four contexts the repository already has — specify, generate, verify, infra —\nplus `edge/` for the binary and repo tooling. Crate names are unchanged, so the four consumers that\npin `ess-compiler`, `ess-domain`, `ess-gen` and `ess-synth` by git revision (`agentide`,\n`service-sdk`) are unaffected.\n\n## Why now\n\nTwenty crates sit flat under `crates/`. `infra-*` is a second bounded context that shares only\n`ess-primitives` with the specification side, and `AGENTS.md` keeps `EssIr` and `InfraIr` apart by\nrule; nothing in the tree shows either fact. Same operator finding as AEP's `epic:area-layout`;\nanalysis in `~/.cache/beyond10x-notes/2026-09-03-aep-ess-structure.md`.\n\n## Scope\n\nArea directories under `crates/`; workspace member and dependency paths; the seven fixture paths\nin `Taskfile.yml`; the crate list in `README.md` and `AGENTS.md`.\n\n## Out of scope\n\nRenaming any crate; splitting `infra/` into its own repository; moving `examples/`, `generated/`,\n`schemas/` or `suites/`.\n","summary":"Group the 20 crates into specify, generate, verify, infra and edge; no crate renamed, no consumer re-pins.","title":"Crates grouped by bounded context"},"args":{"command":"create-entity","data":{"body":"# Epic: Crates grouped by bounded context\n\n## Outcome\n\n`crates/` shows the four contexts the repository already has — specify, generate, verify, infra —\nplus `edge/` for the binary and repo tooling. Crate names are unchanged, so the four consumers that\npin `ess-compiler`, `ess-domain`, `ess-gen` and `ess-synth` by git revision (`agentide`,\n`service-sdk`) are unaffected.\n\n## Why now\n\nTwenty crates sit flat under `crates/`. `infra-*` is a second bounded context that shares only\n`ess-primitives` with the specification side, and `AGENTS.md` keeps `EssIr` and `InfraIr` apart by\nrule; nothing in the tree shows either fact. Same operator finding as AEP's `epic:area-layout`;\nanalysis in `~/.cache/beyond10x-notes/2026-09-03-aep-ess-structure.md`.\n\n## Scope\n\nArea directories under `crates/`; workspace member and dependency paths; the seven fixture paths\nin `Taskfile.yml`; the crate list in `README.md` and `AGENTS.md`.\n\n## Out of scope\n\nRenaming any crate; splitting `infra/` into its own repository; moving `examples/`, `generated/`,\n`schemas/` or `suites/`.\n","status":"draft","summary":"Group the 20 crates into specify, generate, verify, infra and edge; no crate renamed, no consumer re-pins.","title":"Crates grouped by bounded context"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/area-layout"},"payload":{"actor":"human:timo","at":1788465553419,"causation":"cmd-new-epic-area-layout","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:area-layout@1#0~7db30d7fddf752f1","executor":null,"recorded_at":"2026-09-03T19:59:13Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Crates move under area directories; names and consumers unchanged\n\n## Context\n\nTarget layout:\n\n```\ncrates/specify/   ess-primitives ess-domain ess-compiler ess-composition ess-realization\ncrates/generate/  ess-gen ess-synth ess-openapi schema-contract ess-deployment\ncrates/verify/    ess-conformance ess-diff\ncrates/infra/     infra-domain infra-compiler infra-analyze infra-spec infra-project ess-kubernetes\ncrates/edge/      ess-cli ess-xtask\n```\n\n`ess-deployment` sits under `generate/` because its dependencies are `ess-compiler` and\n`ess-realization`, not `infra-*`. Known literal paths: `Cargo.toml` `[workspace] members` and\n`[workspace.dependencies]` (the `examples/*` members and the `exclude` list stay);\n`Taskfile.yml:55-72` names `crates/ess-composition/tests/fixtures/...` and\n`crates/ess-cli/tests/fixtures/schema-contract/...`. Anything else `rg 'crates/'` finds outside\n`target/` and `CHANGELOG.md` is in scope, including path dependencies from `examples/` or\n`generated/` workspaces if any exist.\n\n## Acceptance\n\n`git mv` has placed every crate under its area directory, `task check` exits 0 from a clean\ncheckout, and `rg 'crates/(ess|infra|schema)' --glob '!target' --glob '!CHANGELOG.md'` reports no\npath that does not exist.\n\n## Notes\n\nNo crate is renamed and no `Cargo.toml` `name` changes. `CHANGELOG.md` gains an Unreleased entry.\nThe README gains a short area list. Atlas records components as `crates/<name>/Cargo.toml`; the\ncatalog update is the coordinator's step after this lands.\n","summary":"git mv the 20 crates into crates/{specify,generate,verify,infra,edge}; fix member paths, workspace dependency paths and the seven Taskfile fixture paths; no crate renamed.","title":"Crates move under area directories; names and consumers unchanged"},"args":{"command":"create-entity","data":{"body":"# Story: Crates move under area directories; names and consumers unchanged\n\n## Context\n\nTarget layout:\n\n```\ncrates/specify/   ess-primitives ess-domain ess-compiler ess-composition ess-realization\ncrates/generate/  ess-gen ess-synth ess-openapi schema-contract ess-deployment\ncrates/verify/    ess-conformance ess-diff\ncrates/infra/     infra-domain infra-compiler infra-analyze infra-spec infra-project ess-kubernetes\ncrates/edge/      ess-cli ess-xtask\n```\n\n`ess-deployment` sits under `generate/` because its dependencies are `ess-compiler` and\n`ess-realization`, not `infra-*`. Known literal paths: `Cargo.toml` `[workspace] members` and\n`[workspace.dependencies]` (the `examples/*` members and the `exclude` list stay);\n`Taskfile.yml:55-72` names `crates/ess-composition/tests/fixtures/...` and\n`crates/ess-cli/tests/fixtures/schema-contract/...`. Anything else `rg 'crates/'` finds outside\n`target/` and `CHANGELOG.md` is in scope, including path dependencies from `examples/` or\n`generated/` workspaces if any exist.\n\n## Acceptance\n\n`git mv` has placed every crate under its area directory, `task check` exits 0 from a clean\ncheckout, and `rg 'crates/(ess|infra|schema)' --glob '!target' --glob '!CHANGELOG.md'` reports no\npath that does not exist.\n\n## Notes\n\nNo crate is renamed and no `Cargo.toml` `name` changes. `CHANGELOG.md` gains an Unreleased entry.\nThe README gains a short area list. Atlas records components as `crates/<name>/Cargo.toml`; the\ncatalog update is the coordinator's step after this lands.\n","status":"draft","summary":"git mv the 20 crates into crates/{specify,generate,verify,infra,edge}; fix member paths, workspace dependency paths and the seven Taskfile fixture paths; no crate renamed.","title":"Crates move under area directories; names and consumers unchanged"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/crates-under-area-directories"},"payload":{"actor":"human:timo","at":1788465553558,"causation":"cmd-new-story-crates-under-area-directories","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:crates-under-area-directories@1#0~f88e777ddff412a4","executor":null,"recorded_at":"2026-09-03T19:59:13Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:area-layout"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000049","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788465553564,"causation":"cmd-story-crates-under-area-directories-decomposes-epic-area-layout","change":{"change":"related","relation":"decomposes","target":"epic:area-layout"},"correlation":"protocol-artifact-relate","event_id":"story:crates-under-area-directories@1#0~fe06f059cdbb8b7c","executor":null,"recorded_at":"2026-09-03T19:59:13Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"Taskfile.yml"},{"confidence":"cited","path":"crates"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"CHANGELOG.md"},{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"Taskfile.yml"},{"confidence":"cited","path":"crates"}]},"command":"update-entity","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788465553637,"causation":"cmd-protocol-artifact-scope-story-crates-under-area-directories-1788465553637","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:crates-under-area-directories@2#0~c1b53c137829b042","executor":null,"recorded_at":"2026-09-03T19:59:13Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Crates move under area directories; names and consumers unchanged\n\n## Context\n\nTarget layout:\n\n```\ncrates/specify/   ess-primitives ess-domain ess-compiler ess-composition ess-realization\ncrates/generate/  ess-gen ess-synth ess-openapi schema-contract ess-deployment\ncrates/verify/    ess-conformance ess-diff\ncrates/infra/     infra-domain infra-compiler infra-analyze infra-spec infra-project ess-kubernetes\ncrates/edge/      ess-cli ess-xtask\n```\n\n`ess-deployment` sits under `generate/` because its dependencies are `ess-compiler` and\n`ess-realization`, not `infra-*`. Known literal paths: `Cargo.toml` `[workspace] members` and\n`[workspace.dependencies]` (the `examples/*` members and the `exclude` list stay);\n`Taskfile.yml:55-72` names `crates/ess-composition/tests/fixtures/...` and\n`crates/ess-cli/tests/fixtures/schema-contract/...`. Anything else `rg 'crates/'` finds outside\n`target/` and `CHANGELOG.md` is in scope, including path dependencies from `examples/` or\n`generated/` workspaces if any exist.\n\n## Acceptance\n\n`git mv` has placed every crate under its area directory, `task check` exits 0 from a clean\ncheckout, and `rg 'crates/(ess|infra|schema)' --glob '!target' --glob '!CHANGELOG.md'` reports no\npath that does not exist.\n\n## Notes\n\nNo crate is renamed and no `Cargo.toml` `name` changes. `CHANGELOG.md` gains an Unreleased entry.\nThe README gains a short area list. Atlas records components as `crates/<name>/Cargo.toml`; the\ncatalog update is the coordinator's step after this lands.\n\n## ## Acceptance\n\n`git mv` has placed every crate under its area directory, `task check` exits 0 from a clean\ncheckout, and every literal `crates/…` path that names a workspace crate resolves on disk in every\ntracked file except `target/`, `CHANGELOG.md`, `.engineering/` (append-only journal and artifacts)\nand the root `docs/` engineering record (`docs/design`, `docs/plan`, `docs/reviews`,\n`docs/extraction.md`), which is dated prose and is not rewritten. `website/docs/` is published\nsource and is in scope. The check is a test in `crates/edge/ess-xtask/tests/layout.rs` that (a)\ncollects area-qualified paths `crates/<area>/<crate>/…`, (b) asserts it collected at least one, and\n(c) matches excluded directories by root-relative path.\n"},"args":{"changes":{"body":"# Story: Crates move under area directories; names and consumers unchanged\n\n## Context\n\nTarget layout:\n\n```\ncrates/specify/   ess-primitives ess-domain ess-compiler ess-composition ess-realization\ncrates/generate/  ess-gen ess-synth ess-openapi schema-contract ess-deployment\ncrates/verify/    ess-conformance ess-diff\ncrates/infra/     infra-domain infra-compiler infra-analyze infra-spec infra-project ess-kubernetes\ncrates/edge/      ess-cli ess-xtask\n```\n\n`ess-deployment` sits under `generate/` because its dependencies are `ess-compiler` and\n`ess-realization`, not `infra-*`. Known literal paths: `Cargo.toml` `[workspace] members` and\n`[workspace.dependencies]` (the `examples/*` members and the `exclude` list stay);\n`Taskfile.yml:55-72` names `crates/ess-composition/tests/fixtures/...` and\n`crates/ess-cli/tests/fixtures/schema-contract/...`. Anything else `rg 'crates/'` finds outside\n`target/` and `CHANGELOG.md` is in scope, including path dependencies from `examples/` or\n`generated/` workspaces if any exist.\n\n## Acceptance\n\n`git mv` has placed every crate under its area directory, `task check` exits 0 from a clean\ncheckout, and `rg 'crates/(ess|infra|schema)' --glob '!target' --glob '!CHANGELOG.md'` reports no\npath that does not exist.\n\n## Notes\n\nNo crate is renamed and no `Cargo.toml` `name` changes. `CHANGELOG.md` gains an Unreleased entry.\nThe README gains a short area list. Atlas records components as `crates/<name>/Cargo.toml`; the\ncatalog update is the coordinator's step after this lands.\n\n## ## Acceptance\n\n`git mv` has placed every crate under its area directory, `task check` exits 0 from a clean\ncheckout, and every literal `crates/…` path that names a workspace crate resolves on disk in every\ntracked file except `target/`, `CHANGELOG.md`, `.engineering/` (append-only journal and artifacts)\nand the root `docs/` engineering record (`docs/design`, `docs/plan`, `docs/reviews`,\n`docs/extraction.md`), which is dated prose and is not rewritten. `website/docs/` is published\nsource and is in scope. The check is a test in `crates/edge/ess-xtask/tests/layout.rs` that (a)\ncollects area-qualified paths `crates/<area>/<crate>/…`, (b) asserts it collected at least one, and\n(c) matches excluded directories by root-relative path.\n"},"command":"update-entity","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788468219214,"causation":"cmd-protocol-artifact-body-section-story-crates-under-area-directories-1788468219214","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:crates-under-area-directories@3#0~17ecc13bab68bd78","executor":null,"recorded_at":"2026-09-03T20:43:39Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Crates move under area directories; names and consumers unchanged\n\n## Context\n\nTarget layout:\n\n```\ncrates/specify/   ess-primitives ess-domain ess-compiler ess-composition ess-realization\ncrates/generate/  ess-gen ess-synth ess-openapi schema-contract ess-deployment\ncrates/verify/    ess-conformance ess-diff\ncrates/infra/     infra-domain infra-compiler infra-analyze infra-spec infra-project ess-kubernetes\ncrates/edge/      ess-cli ess-xtask\n```\n\n`ess-deployment` sits under `generate/` because its dependencies are `ess-compiler` and\n`ess-realization`, not `infra-*`. Known literal paths: `Cargo.toml` `[workspace] members` and\n`[workspace.dependencies]` (the `examples/*` members and the `exclude` list stay);\n`Taskfile.yml:55-72` names `crates/ess-composition/tests/fixtures/...` and\n`crates/ess-cli/tests/fixtures/schema-contract/...`. Anything else `rg 'crates/'` finds outside\n`target/` and `CHANGELOG.md` is in scope, including path dependencies from `examples/` or\n`generated/` workspaces if any exist.\n\n## Acceptance\n\n`git mv` has placed every crate under its area directory, `task check` exits 0 from a clean\ncheckout, and every literal `crates/…` path that names a workspace crate resolves on disk in every\ntracked file except `target/`, `CHANGELOG.md`, `.engineering/` (append-only journal and artifacts)\nand the root `docs/` engineering record (`docs/design`, `docs/plan`, `docs/reviews`,\n`docs/extraction.md`), which is dated prose and is not rewritten. `website/docs/` is published\nsource and is in scope. The check is a test in `crates/edge/ess-xtask/tests/layout.rs` that (a)\ncollects area-qualified paths `crates/<area>/<crate>/…`, (b) asserts it collected at least one, and\n(c) matches excluded directories by root-relative path.\n\n## Notes\n\nNo crate is renamed and no `Cargo.toml` `name` changes. `CHANGELOG.md` gains an Unreleased entry.\nThe README gains a short area list. Atlas records components as `crates/<name>/Cargo.toml`; the\ncatalog update is the coordinator's step after this lands.\n"},"args":{"changes":{"body":"# Story: Crates move under area directories; names and consumers unchanged\n\n## Context\n\nTarget layout:\n\n```\ncrates/specify/   ess-primitives ess-domain ess-compiler ess-composition ess-realization\ncrates/generate/  ess-gen ess-synth ess-openapi schema-contract ess-deployment\ncrates/verify/    ess-conformance ess-diff\ncrates/infra/     infra-domain infra-compiler infra-analyze infra-spec infra-project ess-kubernetes\ncrates/edge/      ess-cli ess-xtask\n```\n\n`ess-deployment` sits under `generate/` because its dependencies are `ess-compiler` and\n`ess-realization`, not `infra-*`. Known literal paths: `Cargo.toml` `[workspace] members` and\n`[workspace.dependencies]` (the `examples/*` members and the `exclude` list stay);\n`Taskfile.yml:55-72` names `crates/ess-composition/tests/fixtures/...` and\n`crates/ess-cli/tests/fixtures/schema-contract/...`. Anything else `rg 'crates/'` finds outside\n`target/` and `CHANGELOG.md` is in scope, including path dependencies from `examples/` or\n`generated/` workspaces if any exist.\n\n## Acceptance\n\n`git mv` has placed every crate under its area directory, `task check` exits 0 from a clean\ncheckout, and every literal `crates/…` path that names a workspace crate resolves on disk in every\ntracked file except `target/`, `CHANGELOG.md`, `.engineering/` (append-only journal and artifacts)\nand the root `docs/` engineering record (`docs/design`, `docs/plan`, `docs/reviews`,\n`docs/extraction.md`), which is dated prose and is not rewritten. `website/docs/` is published\nsource and is in scope. The check is a test in `crates/edge/ess-xtask/tests/layout.rs` that (a)\ncollects area-qualified paths `crates/<area>/<crate>/…`, (b) asserts it collected at least one, and\n(c) matches excluded directories by root-relative path.\n\n## Notes\n\nNo crate is renamed and no `Cargo.toml` `name` changes. `CHANGELOG.md` gains an Unreleased entry.\nThe README gains a short area list. Atlas records components as `crates/<name>/Cargo.toml`; the\ncatalog update is the coordinator's step after this lands.\n"},"command":"update-entity","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788468236180,"causation":"cmd-protocol-artifact-body-story-crates-under-area-directories-1788468236180","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:crates-under-area-directories@4#0~242065d2224a4a9c","executor":null,"recorded_at":"2026-09-03T20:43:56Z"}}
{"entity":"review-result","version":1,"id":"adversary-area-layout-round-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary, round 1 — story:crates-under-area-directories\n\nVerdict: NEEDS-CHANGE. Cases executed 1307 → 1310, red 3. Origin: introduced 5 / pre-existing 0.\nAgent: `adp:adversary` (opus). Full transcript-derived report kept by the orchestrator; findings verbatim below.\n\nMutation probe (scratch copy of `layout.rs`, 2-crate workspace):\n\n| Mutation | `every_workspace_crate_lives_under_an_area_directory` | `every_literal_path_naming_a_workspace_crate_exists` |\n|---|---|---|\n| member at `crates/ess-orphan` (no area) | FAILED (guard works) | — |\n| member at `crates/misc/ess-stray` (unknown area) | FAILED (guard works) | — |\n| stale pre-move literal `crates/ess-domain/src/gone.rs` in a `.rs` file | — | FAILED (guard works) |\n| stale post-move literal `crates/specify/ess-domain/src/gone.rs` | — | ok — 1 passed |\n\nCases added by the adversary (red when written): `crates/edge/ess-xtask/tests/layout.rs:194`,\n`:226`; `crates/edge/ess-xtask/tests/layout_acceptance.rs:101` (72 dangling paths in 19 files\nunder the story's own acceptance command; 52 in `docs/`, 11 in `.engineering/planning/journal.jsonl`).\n\nAttacked, did not break: every area-qualified `crates/…` literal (0 dangling over 495 files); every\n`CARGO_MANIFEST_DIR`/`include_str!`/`include_bytes!` site; `cargo xtask generate --check` from a\nsubdirectory; `ess synthesize --target rust|web` emits no path into `crates/`; 28 manifests incl.\nexcluded workspaces reach no `crates/` path; `examples/billing-realization` builds and tests from its\nown directory; `.github/workflows/*` name no crate path; `ess-kubernetes` redaction tests: same 2 by\nname before and after.\n\n```findings\n- file: crates/edge/ess-xtask/tests/layout.rs\n  line: 91\n  category: mutant\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    quoted_crate_paths tests only the segment directly after `crates/`, which is now always an area and never a crate name, so a stale `crates/<area>/<crate>/...` path leaves the scan green — proven on a scratch copy where the pre-move spelling of the same path turns it red\n- file: crates/edge/ess-xtask/tests/layout.rs\n  line: 153\n  category: mutant\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    every_literal_path_naming_a_workspace_crate_exists reads 492 files and collects zero paths, so it asserts that an empty set is empty and would pass on an empty repository; it lacks the non-empty half infra-spec's equivalent source scan writes\n- file: .engineering/planning/story/crates-under-area-directories.md\n  line: 49\n  category: acceptance\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: >-\n    the acceptance statement's own rg command finds 72 non-existent paths in 19 files where the same scan against origin/main finds none, because the unit excluded docs/ and .engineering/ in its new test although the story's Context puts everything outside target/ and CHANGELOG.md in scope\n- file: crates/edge/ess-xtask/tests/layout.rs\n  line: 26\n  category: judgement\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    UNSCANNED_DIRECTORIES matches a bare directory name, so website/docs/ — the published documentation source per b10x.docs.yaml's `root: website` — is skipped by the scan under a comment that justifies skipping the engineering record instead\n- file: AGENTS.md\n  line: 21\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    the new Crate tree section and README:117 restate `[workspace] members` as prose in two further places with nothing comparing the three lists, which is the drift the unit's own layout test was written to prevent for paths\n```\n","title":"Adversary, round 1: crates under area directories"},"args":{"command":"create-entity","data":{"body":"# Adversary, round 1 — story:crates-under-area-directories\n\nVerdict: NEEDS-CHANGE. Cases executed 1307 → 1310, red 3. Origin: introduced 5 / pre-existing 0.\nAgent: `adp:adversary` (opus). Full transcript-derived report kept by the orchestrator; findings verbatim below.\n\nMutation probe (scratch copy of `layout.rs`, 2-crate workspace):\n\n| Mutation | `every_workspace_crate_lives_under_an_area_directory` | `every_literal_path_naming_a_workspace_crate_exists` |\n|---|---|---|\n| member at `crates/ess-orphan` (no area) | FAILED (guard works) | — |\n| member at `crates/misc/ess-stray` (unknown area) | FAILED (guard works) | — |\n| stale pre-move literal `crates/ess-domain/src/gone.rs` in a `.rs` file | — | FAILED (guard works) |\n| stale post-move literal `crates/specify/ess-domain/src/gone.rs` | — | ok — 1 passed |\n\nCases added by the adversary (red when written): `crates/edge/ess-xtask/tests/layout.rs:194`,\n`:226`; `crates/edge/ess-xtask/tests/layout_acceptance.rs:101` (72 dangling paths in 19 files\nunder the story's own acceptance command; 52 in `docs/`, 11 in `.engineering/planning/journal.jsonl`).\n\nAttacked, did not break: every area-qualified `crates/…` literal (0 dangling over 495 files); every\n`CARGO_MANIFEST_DIR`/`include_str!`/`include_bytes!` site; `cargo xtask generate --check` from a\nsubdirectory; `ess synthesize --target rust|web` emits no path into `crates/`; 28 manifests incl.\nexcluded workspaces reach no `crates/` path; `examples/billing-realization` builds and tests from its\nown directory; `.github/workflows/*` name no crate path; `ess-kubernetes` redaction tests: same 2 by\nname before and after.\n\n```findings\n- file: crates/edge/ess-xtask/tests/layout.rs\n  line: 91\n  category: mutant\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    quoted_crate_paths tests only the segment directly after `crates/`, which is now always an area and never a crate name, so a stale `crates/<area>/<crate>/...` path leaves the scan green — proven on a scratch copy where the pre-move spelling of the same path turns it red\n- file: crates/edge/ess-xtask/tests/layout.rs\n  line: 153\n  category: mutant\n  severity: blocker\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    every_literal_path_naming_a_workspace_crate_exists reads 492 files and collects zero paths, so it asserts that an empty set is empty and would pass on an empty repository; it lacks the non-empty half infra-spec's equivalent source scan writes\n- file: .engineering/planning/story/crates-under-area-directories.md\n  line: 49\n  category: acceptance\n  severity: warning\n  verdict: NEEDS-CHANGE\n  origin: introduced\n  message: >-\n    the acceptance statement's own rg command finds 72 non-existent paths in 19 files where the same scan against origin/main finds none, because the unit excluded docs/ and .engineering/ in its new test although the story's Context puts everything outside target/ and CHANGELOG.md in scope\n- file: crates/edge/ess-xtask/tests/layout.rs\n  line: 26\n  category: judgement\n  severity: warning\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    UNSCANNED_DIRECTORIES matches a bare directory name, so website/docs/ — the published documentation source per b10x.docs.yaml's `root: website` — is skipped by the scan under a comment that justifies skipping the engineering record instead\n- file: AGENTS.md\n  line: 21\n  category: judgement\n  severity: note\n  verdict: CONFIRMED\n  origin: introduced\n  message: >-\n    the new Crate tree section and README:117 restate `[workspace] members` as prose in two further places with nothing comparing the three lists, which is the drift the unit's own layout test was written to prevent for paths\n```\n","status":"active","title":"Adversary, round 1: crates under area directories"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-area-layout-round-1"},"payload":{"actor":"human:timo","at":1788468236325,"causation":"cmd-new-review-result-adversary-area-layout-round-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-area-layout-round-1@1#0~65bca66128f01748","executor":null,"recorded_at":"2026-09-03T20:43:56Z"}}
{"entity":"review-result","version":1,"id":"adversary-area-layout-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:crates-under-area-directories"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000054","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788468236336,"causation":"cmd-review-result-adversary-area-layout-round-1-reviews-story-crates-under-area-directories","change":{"change":"related","relation":"reviews","target":"story:crates-under-area-directories"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-area-layout-round-1@1#0~b6f20ef4dbb12442","executor":null,"recorded_at":"2026-09-03T20:43:56Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":4,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-area-layout-round-1","source":"correction round 1: findings 1-4 (layout.rs:91, :153, :26; acceptance amended to revision 4); task check exit 0, 1310 tests","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788468622000,"causation":"cmd-evidence-story-crates-under-area-directories-review_outcome-1788468622976","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-area-layout-round-1","source":"correction round 1: findings 1-4 (layout.rs:91, :153, :26; acceptance amended to revision 4); task check exit 0, 1310 tests"},"correlation":"protocol-artifact-evidence","event_id":"story:crates-under-area-directories@4#0~2ed210c022e9d015","executor":null,"recorded_at":"2026-09-03T20:50:22Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":4,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"escalated","review":"review-result:adversary-area-layout-round-1","source":"finding 5: AGENTS.md and README.md restate the workspace member list; not acted on, reported to the operator","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788468623000,"causation":"cmd-evidence-story-crates-under-area-directories-review_outcome-1788468623052","change":{"change":"evidence","kind":"review_outcome","outcome":"escalated","review":"review-result:adversary-area-layout-round-1","source":"finding 5: AGENTS.md and README.md restate the workspace member list; not acted on, reported to the operator"},"correlation":"protocol-artifact-evidence","event_id":"story:crates-under-area-directories@4#0~d77ce050ac3d9e9f","executor":null,"recorded_at":"2026-09-03T20:50:23Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Crates move under area directories; names and consumers unchanged\n\n## Context\n\nTarget layout:\n\n```\ncrates/specify/   ess-primitives ess-domain ess-compiler ess-composition ess-realization\ncrates/generate/  ess-gen ess-synth ess-openapi schema-contract ess-deployment\ncrates/verify/    ess-conformance ess-diff\ncrates/infra/     infra-domain infra-compiler infra-analyze infra-spec infra-project ess-kubernetes\ncrates/edge/      ess-cli ess-xtask\n```\n\n`ess-deployment` sits under `generate/` because its dependencies are `ess-compiler` and\n`ess-realization`, not `infra-*`. Known literal paths: `Cargo.toml` `[workspace] members` and\n`[workspace.dependencies]` (the `examples/*` members and the `exclude` list stay);\n`Taskfile.yml:55-72` names `crates/ess-composition/tests/fixtures/...` and\n`crates/ess-cli/tests/fixtures/schema-contract/...`. Anything else `rg 'crates/'` finds outside\n`target/` and `CHANGELOG.md` is in scope, including path dependencies from `examples/` or\n`generated/` workspaces if any exist.\n\n## Acceptance\n\n`git mv` has placed every crate under its area directory, `task check` exits 0 from a clean\ncheckout, and every literal `crates/…` path that names a workspace crate resolves on disk in every\ntracked file, with these exclusions only: `target/`, `CHANGELOG.md`, `.engineering/` (append-only\njournal and CLI-owned artifacts), and the dated record `docs/design/`, `docs/reviews/`,\n`docs/extraction.md`. `docs/plan/` is live and is rewritten; `website/docs/` is published source and\nis in scope. The check is the test in `crates/edge/ess-xtask/tests/layout.rs`: it collects\narea-qualified paths `crates/<area>/<crate>/…`, asserts it collected at least one, and matches\nexcluded paths root-relative.\n\n## Notes\n\nNo crate is renamed and no `Cargo.toml` `name` changes. `CHANGELOG.md` gains an Unreleased entry.\nThe README gains a short area list. Atlas records components as `crates/<name>/Cargo.toml`; the\ncatalog update is the coordinator's step after this lands.\n"},"args":{"changes":{"body":"# Story: Crates move under area directories; names and consumers unchanged\n\n## Context\n\nTarget layout:\n\n```\ncrates/specify/   ess-primitives ess-domain ess-compiler ess-composition ess-realization\ncrates/generate/  ess-gen ess-synth ess-openapi schema-contract ess-deployment\ncrates/verify/    ess-conformance ess-diff\ncrates/infra/     infra-domain infra-compiler infra-analyze infra-spec infra-project ess-kubernetes\ncrates/edge/      ess-cli ess-xtask\n```\n\n`ess-deployment` sits under `generate/` because its dependencies are `ess-compiler` and\n`ess-realization`, not `infra-*`. Known literal paths: `Cargo.toml` `[workspace] members` and\n`[workspace.dependencies]` (the `examples/*` members and the `exclude` list stay);\n`Taskfile.yml:55-72` names `crates/ess-composition/tests/fixtures/...` and\n`crates/ess-cli/tests/fixtures/schema-contract/...`. Anything else `rg 'crates/'` finds outside\n`target/` and `CHANGELOG.md` is in scope, including path dependencies from `examples/` or\n`generated/` workspaces if any exist.\n\n## Acceptance\n\n`git mv` has placed every crate under its area directory, `task check` exits 0 from a clean\ncheckout, and every literal `crates/…` path that names a workspace crate resolves on disk in every\ntracked file, with these exclusions only: `target/`, `CHANGELOG.md`, `.engineering/` (append-only\njournal and CLI-owned artifacts), and the dated record `docs/design/`, `docs/reviews/`,\n`docs/extraction.md`. `docs/plan/` is live and is rewritten; `website/docs/` is published source and\nis in scope. The check is the test in `crates/edge/ess-xtask/tests/layout.rs`: it collects\narea-qualified paths `crates/<area>/<crate>/…`, asserts it collected at least one, and matches\nexcluded paths root-relative.\n\n## Notes\n\nNo crate is renamed and no `Cargo.toml` `name` changes. `CHANGELOG.md` gains an Unreleased entry.\nThe README gains a short area list. Atlas records components as `crates/<name>/Cargo.toml`; the\ncatalog update is the coordinator's step after this lands.\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788468910213,"causation":"cmd-protocol-artifact-body-story-crates-under-area-directories-1788468910213","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:crates-under-area-directories@5#0~5e21afa3bbd5d797","executor":null,"recorded_at":"2026-09-03T20:55:10Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":5,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","source":"task check in worktree wt-6cb2ff14f31d at 43cb46a: exit 0, 1310 tests, 99 binaries","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788471092000,"causation":"cmd-evidence-story-crates-under-area-directories-test_result-1788471093104","change":{"change":"evidence","kind":"test_result","source":"task check in worktree wt-6cb2ff14f31d at 43cb46a: exit 0, 1310 tests, 99 binaries"},"correlation":"protocol-artifact-evidence","event_id":"story:crates-under-area-directories@5#0~f309d9c1e03235cf","executor":null,"recorded_at":"2026-09-03T21:31:32Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":6,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"review_outcome\":2}}","target":"01MEM0000000000000019","to":"proposed"},"payload":{"actor":"human:timo","at":1788471093244,"causation":"cmd-move-story-crates-under-area-directories-1788471093244","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":2,"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:crates-under-area-directories@6#0~fc41e5f1d9ccc640","executor":null,"recorded_at":"2026-09-03T21:31:33Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":7,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"review_outcome\":2}}","target":"01MEM0000000000000019","to":"active"},"payload":{"actor":"human:timo","at":1788471093382,"causation":"cmd-move-story-crates-under-area-directories-1788471093382","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":2,"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:crates-under-area-directories@7#0~8866ab85e02d8eac","executor":null,"recorded_at":"2026-09-03T21:31:33Z"}}
{"entity":"story","version":1,"id":"crates-under-area-directories","revision":8,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"review_outcome\":2}}","target":"01MEM0000000000000019","to":"implemented"},"payload":{"actor":"human:timo","at":1788471093519,"causation":"cmd-move-story-crates-under-area-directories-1788471093519","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":2,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:crates-under-area-directories@8#0~905b8e5c96c7fd1c","executor":null,"recorded_at":"2026-09-03T21:31:33Z"}}
{"entity":"epic","version":1,"id":"area-layout","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"proposed"},"payload":{"actor":"human:timo","at":1788471093657,"causation":"cmd-move-epic-area-layout-1788471093657","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:area-layout@2#0~d1b8d2c3ff0c3c64","executor":null,"recorded_at":"2026-09-03T21:31:33Z"}}
{"entity":"epic","version":1,"id":"area-layout","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"active"},"payload":{"actor":"human:timo","at":1788471093800,"causation":"cmd-move-epic-area-layout-1788471093800","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:area-layout@3#0~e48f181f24deda08","executor":null,"recorded_at":"2026-09-03T21:31:33Z"}}
{"entity":"epic","version":1,"id":"area-layout","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"implemented"},"payload":{"actor":"human:timo","at":1788471093942,"causation":"cmd-move-epic-area-layout-1788471093942","change":{"change":"moved","decided_on":{},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"epic:area-layout@4#0~ca625d24465ff570","executor":null,"recorded_at":"2026-09-03T21:31:33Z"}}
