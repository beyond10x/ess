---
format: aep.planning-md/1
id: story:review-observation-completeness
kind: story
status: draft
title: Preserve observation scope and selector uncertainty
tags:
- P1
- review-2026-09-05
relations:
- decomposes: epic:review-boundary-remediation
- serves: vision:O2
- depends_on: story:review-secret-sanitization
- depends_on: story:review-infra-ir-invariants
scope:
- confidence: cited
  path: crates/edge/ess-cli
- confidence: cited
  path: crates/infra/ess-kubernetes
- confidence: cited
  path: crates/infra/infra-analyze
- confidence: cited
  path: crates/infra/infra-compiler
- confidence: cited
  path: crates/infra/infra-domain
- confidence: cited
  path: crates/infra/infra-project
- confidence: cited
  path: crates/infra/infra-spec
- confidence: inferred
  path: docs/design/review-observation-completeness.md
- confidence: inferred
  path: examples/k3d-dev-cluster
revision: 12
---
## Finding and source

F06 (P1) from `docs/reviews/2026-09-05-architecture-review.md:286`, reviewed at `fd06a4d61bfb7b4990617810655dc181d6a3ab00`. Evidence locations: `crates/infra/infra-domain/src/raw.rs:159`, `crates/infra/ess-kubernetes/src/lib.rs:70`. Findings are attributed to the review; proposed implementation choices below are coordinator inferences.

## Acceptance

Unsupported selectors or a reduced collection scope produce qualified unknown/refused infrastructure conclusions instead of complete match-all or absence claims.

## Implementation boundary

First design typed scope/per-kind completeness and unsupported-term accounting against the existing observation/IR formats, with strict old-reader fixtures. Preserve selector semantics or refuse their use; never reduce an unrecognized expression-only selector to a broad empty conjunction. Retry only when scope is preserved or visibly acknowledged; ensure scope qualifications reach analysis, drift and projection.

## Validation

Fake kubectl fixtures distinguish permission failure, namespace fallback and genuinely scope-preserving retries. Selector fixtures cover matchLabels, matchExpressions, mixed and unknown terms. Round-trip incomplete observations and assert consumers retain uncertainty.

Run package-scoped checks while implementing; the integration coordinator runs every step of `task check` and `task site-build` when required by `AGENTS.md`, retaining individual exit statuses and executed-case counts.

## Compatibility and exclusions

No live cluster needed; keep the separate infra bounded context and obtain an explicit format decision before changing persisted fields.

## Scope

- **Derivation:** cited — refreshed 2026-09-05 by independent `aep-drive:story-scoper` from revision 11, F06, the binding InfraIr ownership design and source frozen at `acb7859e3202ffdc1ca840dde67f7ca4da33c746`. Read-only source examination; no new runtime, old-reader, regeneration or live-cluster evidence is claimed.
- **Primary acquisition surface:** cited — `crates/infra/ess-kubernetes`; `src/lib.rs:58` owns `scan`, `:70` the ordered 17-kind loop, `:76` unconditional retry without `-A`, and `:95` the writer literal `infra-observation/1`. No requested/effective collection ledger or completion claim is persisted. Counting an `items` array is not proof of completeness. Keep credentials and collection at this edge; downstream libraries never select authority.
- **Observation and selector admission:** cited — `crates/infra/infra-domain`; `src/raw.rs:23` admits a permissive bundle, `:162` retains only `matchLabels`, `:860` distinguishes absent optional kinds, and `:871` checks `items` while discarding List metadata during typed extraction. `src/workload.rs:308` and `src/policy.rs:64` reduce missing selectors to empty maps. `src/observation.rs:259` owns the typed observation and `:308` the exact format check. Qualifications and unsupported terms must be accounted for before permissive deserialization loses them; required missing kinds already refuse, while optional absent and present-empty kinds differ.
- **Compilation and persisted schema owner:** cited — `crates/infra/infra-compiler`; `src/compile.rs:270` derives no-observed-pod selector facts and `:294` onward derives other unresolved references. `src/ir.rs:463` owns `InfraModel`, `:518` provenance, `:627` the owner, `:636` the persisted document, `:700` canonical model hashing and `:712` the writer. `src/read.rs:62` validates the version, digest, closed mirrors and resolved-reference membership, then remints handles. Compiler construction, persisted reading and downstream reasoning all need the same qualification meaning; unresolved is currently evidence of non-observation, not independently proven absence.
- **Merged API boundary:** cited — the binding `docs/design/review-infra-ir-invariants.md` is now implemented: `InfraIr.model` is crate-private, `model()` at `ir.rs:665` returns an immutable borrow, and `try_transform` at `:680` edits a detached clone, preserves provenance, serializes its candidate document and calls `read_document`. There is no new mutable owner accessor or unchecked replacement. The admission contract explicitly excludes rederiving observation completeness and all unresolved facts; the completed handle work does not complete F06.
- **Admission obligations for the future design:** inferred — define how collection/selector qualifications are admitted, kept internally consistent and preserved through compile, persisted reload, clone and `try_transform`; do not assume a fresh digest or valid handles establish completeness. Update writer and closed reader mirrors together for any chosen representation. Test ordinary detached edits and no-op transforms as well as malformed/rehashed evidence; a transform must not silently upgrade incomplete or legacy evidence. Keep the single persisted admission owner and source-on-failure guarantee.
- **Analysis consumers:** cited — `crates/infra/infra-analyze`; `src/diagnose.rs:496` owns shared equality-only `pdb_covers`, whose empty conjunction matches everything, while `:531` can say a budget guards nothing and `:542` checks coverage. `src/properties.rs:120` counts observed pods and uses optional-map presence for PDB/HPA counts; `src/invariants.rs:182` derives population coverage; `src/graph.rs:571` matches Services to workload templates and `:769` chooses ownership behavior from ReplicaSet kind presence. These consumers now use `model()`; the ownership migration did not add collection qualifications.
- **Simulation, facts and drift:** cited — `crates/infra/infra-spec`; `src/simulate.rs:58` already carries typed unknown reasons, but `:438` turns missing workload membership into `WorkloadAbsent`, `:663` uses optional PDB presence as coverage and `:674` turns required unresolved configuration references into gaps. `src/facts.rs:194` counts unresolved references and `:281` emits exact counts from present optional collections. `src/drift.rs:460` checks cluster context, then `:517` derives additions/removals from membership without collection-scope comparison. Preserve useful observed facts while qualifying conclusions that need applicable complete populations.
- **Projection and checked mutation:** cited — `crates/infra/infra-project`; `src/project.rs:552` now returns `Result<Projection, ValidationErrors>`. `:636` runs the fixed-point loop, `:663` uses `try_transform`, and `:692` admits the candidate before recording artifacts, created objects, slots or progress. `:705` disposes remaining gaps, while undecidable outcomes currently chiefly affect simulation summaries. `:1029` synthesizes PDBs from workload selector maps and `:1377` emits `matchLabels`. Qualifications must survive hypothetical models and remain actionable in output; unsupported selector or unproven-absence claims cannot justify a generated patch.
- **CLI admission, reporting and output:** cited — `crates/edge/ess-cli`; `src/load.rs:138` dispatches only the current observation and IR constants. `src/main.rs:539` and `:548` name current versions in help; `:2791` invokes the scanner, `:2797` writes the compiled IR, `:2815` initializes empty Kubernetes coverage/obligation/refusal lists and `:2823` prints literal `infra-ir/1`. `:3236` already propagates projection admission failure before `:3240` writes artifacts. Future migration must cover import help/dispatch/reporting and all persisted CLI consumer paths, preserving this pre-write refusal order.
- **Binding design reservation:** inferred — `docs/design/review-observation-completeness.md` must settle requested/effective scope and authority, per-kind completion, selector support/refusal, legacy admission, evidence validation, semantic digest placement, affected versions and rollout before production work. This scoping refresh does not choose those decisions or authorize a format default.
- **Collection decision retained from revision 11:** inferred — distinguish requested/effective namespaces and filters and complete-within-scope, incomplete, unscanned and legacy-unknown evidence. Removing arbitrary fallback may refuse without replacing the destination, or a separately chosen narrower collection may carry explicit qualification. Same-scope retries are a different policy; current value-free status diagnostics cannot distinguish permission and transient causes by themselves, and raw stderr must not return as a classifier.
- **Kubernetes semantic baseline retained:** cited — revision 11 records [API concepts](https://kubernetes.io/docs/reference/using-api/api-concepts/), [labels](https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/) and [PDB documentation](https://kubernetes.io/docs/tasks/run-application/configure-pdb/) as its primary semantic sources: discovery is not collection; nonempty List `continue` signals more results; 17 sequential requests do not form one atomic snapshot; structured selectors are conjunctions, and policy/v1beta1 versus policy/v1 empty PDB selector semantics differ. Current `raw.rs:162` discards unmodeled selector terms, and `RawPodDisruptionBudget` at `:733` retains no `apiVersion`. These references were carried forward, not newly browsed or experimentally verified by this refresh.
- **Selector decision retained:** inferred — unsupported expression-only, mixed or unknown terms may be refused before they are discarded. If expression support is chosen, one explicit shared evaluator must own its semantics. Keep Service equality maps distinct from structured selectors, and preserve absent/null/empty plus API-version distinctions or refuse ambiguous input; this scope does not invent supported operators or silently broaden the language.
- **Consumer decision retained:** inferred — choose precise scoped reasoning or operation-level refusal. Missing objects/workloads, zero matching pods, missing PDB coverage, exact counts, universal claims and drift removals require the relevant scope evidence. A positive observed witness can remain useful under partial collection; a missing key in an observed ConfigMap/Secret differs from a missing object. Unknown/refusal reasons must reach analysis, simulation/facts, drift, projection and CLI rather than disappearing into counts.
- **Persisted compatibility facts:** cited — observation/1 admits unknown fields but requires its format claim, so adding a qualification under that same claim can be ignored by old readers. InfraIR uses closed document/provenance/model/nested mirrors in `read.rs:132` onward. `ProvenanceMirror` retains the existing optional ignored `compiler_version` exception. `InfraIr.provenance` is still public and outside the canonical model digest; the handle design intentionally left it so. A qualification that changes permissible conclusions cannot be treated as ordinary provenance without an explicit semantic identity and mutation decision.
- **Format obligations:** inferred — explicitly decide new observation/IR format consequences before adding persisted fields or changing meaning, as required by `AGENTS.md`; retain deliberate legacy-unknown handling instead of retroactively asserting a historical scan complete. Decide whether changed graph, simulation, drift and projection meanings/envelopes require their own migrations. No version number, field placement, blanket version bump or frozen-v1 reinterpretation is selected by this refresh.
- **Reader/writer order:** inferred — prepare the compatible ESS readers before enabling a changed scanner default; independently retained old readers must refuse new version claims, and new readers must distinguish legacy from qualified documents. Add frozen old-reader/new-writer and legacy/new-reader tests, including CLI dispatch, instead of relying on a changed reader roundtripping itself. A persisted evidence claim can be checked for internal consistency; a local reader cannot independently verify what a remote cluster returned.
- **Actual schema boundary:** cited — the infrastructure persisted schema currently lives in Rust raw/validated types, writer documents and strict compiler mirrors. Read-only inspection of `schemas/generated/README.md` and `crates/edge/ess-xtask/src/main.rs:423` finds only generation of `RawSpecFile` into the ESS source schema. There is no existing infrastructure JSON Schema generation lane to update or to use as proof of IR compatibility; those read-only references add no schema/xtask reservation.
- **Existing fallback tests to supersede deliberately:** cited — `ess-kubernetes/tests/secret_boundary.rs:239` pins both all-namespace and narrower attempts, and `:318` requires successful narrower fallback to produce the same bytes as a full scan. `tests/fixtures/fake_command.rs:40` calls the `-A` invocation the first attempt and continues failing every `-A` call, so it cannot currently demonstrate success on a repeated same-scope invocation. F06 must replace those fallback order/byte-equality expectations according to the chosen policy while preserving their independent security assertions.
- **Security obligations retained:** cited — revision 11 carries the published Secret and diagnostic boundaries (`7130468ce5baa8178407bc02df557f27a7cae7ee`, `b26829a571c0569ba2f63a5da495b987397b43a4`). Current scanner tests assert no raw stdout/stderr or context leakage, value-free UTF-8 diagnostics, visible failure status, destination preservation and deterministic behavior; adversarial process-status/large-output/invalid-input cases remain guards. The package `AGENTS.md` also requires the sanitizer mutation check. Superseding fallback equality does not supersede these protections.
- **Red-first validation:** inferred — extend Rust fake-command fixtures to independently record argv, attempt number and populations; distinguish failed all-namespace collection followed by narrower apparent success from a genuinely identical-scope retry. Assert refusal/qualification, no destination replacement on refusal and preserved security guards. Cover malformed/partial lists and completion metadata; selector labels, expressions, mixed/unknown terms, absent/null/empty and API version; the same observed objects under different collection evidence; and observation → compile → persisted read → analysis/simulation/facts/drift/projection/CLI reason retention. Include positive witnesses and missing-key versus missing-object controls.
- **Example reservation:** inferred — `examples/k3d-dev-cluster` is the only currently identified generated-product reservation. `infra-spec/tests/determinism.rs:43` pins simulation/drift and `infra-project/tests/determinism.rs:60` pins emitted projection products. Legacy policy and any coordinated migration may change derived IR, reports or projection artifacts; exact files/bytes remain to be measured. Preserve historical observation evidence and do not manufacture complete scope for it.
- **Frozen fixture added by InfraIr work:** cited — `infra-compiler/tests/read.rs:359` compares current compile/read/no-op-transform output with `examples/k3d-dev-cluster/cluster.ir.json`, captured from the unchanged writer at `28e97095d9e06c8b4585876a681a5eda5278c1ab`. The binding ownership design also requires all six handle-family and failed-transform source-integrity tests, plus actual emitted-patch application/recompilation in `infra-project/tests/round_trip.rs`.
- **Frozen fixture migration obligation:** inferred — if that example changes for F06, first retain truly frozen legacy bytes and the old-reader lane in package fixtures; merely regenerating the example path would erase the existing old-writer compatibility evidence. Preserve the ownership and actual patch-application assertions while adding qualified-observation controls.
- **Actual relying parties and coordination baseline:** cited — current writer/reader ownership is the scanner → infra-domain/CLI → infra-compiler persisted reader → typed consumers described above. Revision 11 records archived infra-scout `c618843a4295e8fb414125a0a4d1115a2cc46b95` as a historical writer, bounded prior AEP/entity-runtime/service-sdk inspection finding no active parser, and historical Atlas ADR 0017 coordination. This refresh does not revalidate external repositories or claim deployed consumer compatibility; those bounded historical limits remain.
- **Rollout coordination:** inferred — refresh clean Atlas authority and the actual relying inventory before approving any changed format/default; record reader/writer order and retained old-consumer obligations there. Keep the InfraIr bounded context separate from EssIr and introduce no ESS dependency on AEP. That coordination and the future binding design remain pending.
- **Validation packages:** cited — the exact affected package set remains `ess-kubernetes`, `infra-domain`, `infra-compiler`, `infra-analyze`, `infra-spec`, `infra-project`, and `ess-cli`, including their source, unit/integration tests and local fixtures. Current xtask exposes generate/schema/release, not the `cargo xtask infra` named by stale example test comments. The implementation must use real package tests and authorized generation commands; coordinator-owned `task check` and applicable `task site-build` remain integration gates. No baseline pass/count is newly claimed here.
- **Would collide with:** cited — scanner collection/sanitization and fake-command fixtures; domain observation/selector structs and validation; compiler model/provenance/document/mirrors/transform tests; analysis selectors/populations/graph; simulation/fact/unknown/drift contracts; projector fixed-point admission, PDB emission and round-trip tests; CLI import/load/help/report/projection paths. The merged ownership implementation and its frozen-byte/handle assertions are binding compatibility surfaces, not work to redo.
- **Would also collide with:** inferred — the proposed observation-completeness design page and any regeneration of the k3d example IR, simulation, drift or projection tree. These two reservations remain inferred until the binding design determines the actual changed products.
- **Confidence:** cited — high for the seven package owners and the material API/test changes because the frozen source and existing assertions establish them directly. The design/example reservations and concrete migration effects remain contingent on the future binding decision; no implementation, completion or next-wave-selection claim follows from this refresh.
