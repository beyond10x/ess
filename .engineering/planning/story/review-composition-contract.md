---
format: aep.planning-md/1
id: story:review-composition-contract
kind: story
status: implemented
title: State the composition client plan's actual guarantees
tags:
- P2
- review-2026-09-05
relations:
- decomposes: epic:review-boundary-remediation
- serves: vision:O2
scope:
- confidence: cited
  path: crates/specify/ess-composition
- confidence: cited
  path: website/docs/reference/cli.md
- confidence: cited
  path: website/docs/reference/formats.md
revision: 10
---
## Finding and source

F17 (P2) from `docs/reviews/2026-09-05-architecture-review.md:549`, reviewed at `fd06a4d61bfb7b4990617810655dc181d6a3ab00`. Evidence locations: `crates/specify/ess-composition/src/lib.rs:470`, `crates/specify/ess-composition/src/lib.rs:898`. Findings are attributed to the review; proposed implementation choices below are coordinator inferences.

## Acceptance

Composition documentation and examples describe exact surface selection with byte-buffer transport without implying end-to-end typed payload compatibility.

## Implementation boundary

Document operation membership and pinned model identity separately from payload shape/codec compatibility. Demonstrate what the current plan verifies and does not verify. If a concrete typed-client requirement is selected, draft a versioned plan design referencing existing resolved shapes before decomposing its implementation.

## Validation

Trace the worked example to ResolvedService and the generated Transport<Authority> trait, with a negative boundary case showing that payload compatibility is not established by membership alone; run composition tests and site-build.

Run package-scoped checks while implementing; the integration coordinator runs every step of `task check` and `task site-build` when required by `AGENTS.md`, retaining individual exit statuses and executed-case counts.

## Compatibility and exclusions

F17 is an explicit tradeoff, not a broken typed-client implementation. New typed-client generation is not required merely by the review. Authority is injected through a separate argument; opaque payload bytes are not inspected or sanitized, so the client cannot exclude application-chosen authentication coordinates inside them.

## Scope

Opening scope derived 2026-09-06 by story-scoper against ESS e339f4ce3d5046443ff8e99b0ea72099bc23e410. The confirmed implementation paths and corrections below supersede inferred choices while preserving their origin.
Every reservation distinguishes cited evidence from inferred work.

- **Primary surface:** `crates/specify/ess-composition` — cited; composition admission, selected-service metadata, client-plan generation and generated byte-buffer transport belong here.
- **Production documentation:** `src/lib.rs:1`, `:469`, `:583`, `:624`, `:685`, `:726` within that package — cited; crate/API guarantees around ResolvedService, EssCompositionIr::client_plan, ClientServicePlan, EssClientPlan and rust_artifacts. No persisted shape/behavior change.
- **Transport evidence:** `src/lib.rs:864`, `:884`, `:890`, `:898`, `:941` — cited; generated Operation, EndpointProvider, AuthorityProvider, Transport<Authority> and Client::execute.
- **Existing test surface:** `tests/composition.rs:93`, `:155`, `:185`, `:245`, `:262`, `:318`, `:344`, `:370`, `:404` — cited; nine declared integration cases cover selection, excluded references, determinism, client compilation, identities, duplicates and strict inputs.
- **Worked fixtures:** `tests/fixtures/two-components/components.yaml`, `tests/fixtures/two-components/domains/todo.yaml`, `tests/fixtures/compositions/workbench.yaml`, `tests/fixtures/two-components/expected/rust-client/{Cargo.toml,ess-client-plan.json,src/lib.rs}` — cited; existing exact Todo/Usage metadata and operation constants. Preserve golden bytes.
- **Required boundary example:** package-local executable recording transport showing a selected operation forwarding both intended and incompatible payloads unchanged, response bytes unchanged and authority through its separate parameter — inferred at opening, when no runtime case existed. The implementor confirmed this reservation with tests/fixtures/client_boundary.rs and its parent runner in tests/composition.rs.
- **Public command documentation:** `website/docs/reference/cli.md` — cited, line46; smallest established worked-invocation and byte-buffer guarantee location.
- **Public format documentation:** `website/docs/reference/formats.md` — cited, lines43–45 at original baseline; composition IR/client plan selection/pinned identity is distinct from payload schema/codec compatibility.
- **Exclusions:** no CLI production, compiler shape, generated typed payload, authentication verifier, remote compatibility handshake or new format — cited, story boundary.
- **Confidence:** high — cited; package owners, nine test declarations and two exact public claim locations establish this reservation.
- **Would collide with:** package docs/tests/golden fixtures and the exact public CLI/formats pages — cited; directory-only website reservations in other stories are an additional real prose overlap despite the scheduler's exact-token matching.
- **Coordinator records:** planning journal, wave records, final gates and public delivery evidence remain root-owned — inferred.


## Source refresh and measured baseline

Root reconciled published main6c6620b into a24ef59 using the complete incoming planning store and semantic CLI replay. The composition package and fixtures are byte-identical to the inspected e339 baseline; preserve incoming Binary64 rows in the formats reference. The post-reconciliation cargo test --locked -p ess-composition command passed9 cases,0failed/ignored,3summaries in5.320seconds. This was opening evidence only; no recording-transport runtime example had executed at that point.


## Implementor scope confirmation and measured handoff

At frozen source327b968acb0fb7237acf005972aa80f16074cd8a, the implementor confirmed five changed files: package src/lib.rs documentation comments; additive tests/composition.rs runner; new tests/fixtures/client_boundary.rs; and website/docs/reference/cli.md plus formats.md. Root verified all five SHA256 values and retained fixture/golden hashes. No production statement, persisted shape, generated golden byte, dependency or Cargo.lock changed. Source: target/review-boundaries-9/implementor-report.md in the managed composition unit, SHA256bcfc1563e5909f53ce1c4b6a9adbff4183a67d27fc856199d01beaf291a78b1c.

The original complete-type-closure inference is corrected: traversal includes command inputs, event/error fields and query row shapes/fields but excludes view parameters. The original authority wording is corrected above: separate injection does not imply payload sanitization or authentication verification. The actual public trait is Transport<Authority>. These are confirmed source readings, not new typed-codec or handshake features.

The added test compiles actual emitted code as a separate library and executes three downstream cases. Independently declared String-title and numeric-title buffers reach Todo/CreateList unchanged with separate authority; arbitrary binary response bytes are unchanged. MissingEndpoint precedes authority/transport access and the original transport error is retained. Package tests passed10 cases,0failed/ignored,3summaries; the child binary separately passed3,0failed/ignored. Package formatting, fixture formatting, strict package all-target Clippy and diff checks each exited0. The first focused characterization passed before documentation correction. The package baseline was9, not13.

This handoff supplies unit evidence and confirmed scope only. Independent source attack1 is running; full integration task-check/site-build, publication and story completion remain pending. Root owns their shared planning, changelog and delivery surfaces.

## Integrated acceptance evidence

The actual emitted byte-buffer counterexample and exact selected-operation metadata are documented. Both source attacks found no remaining defect; their immutable records and empty final ledger are retained. The corrected copyable command and conflicting-destination refusal executed as documented without changing generated corpus bytes.

The full integration at4777c1de3a80ea7645a255e4e229ad30a8a5cc8e passed1983 workspace cases,0failed/ignored,170 summaries and all10 recorded lanes, including site-build. verification-report:review-boundaries-9-integrated binds each real exit and log hash to its source. This includes the other session's published native-codec/positional changes without changing this unit's scope. Root records source publication, public delivery and managed cleanup separately in the wave page.
