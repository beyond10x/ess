---
format: aep.planning-md/1
id: story:review-format-catalog
kind: story
status: draft
title: Catalog format identities and canonical byte contracts
tags:
- P1
- review-2026-09-05
relations:
- decomposes: epic:review-boundary-remediation
- serves: vision:O2
scope:
- confidence: inferred
  path: docs/design/review-format-catalog.md
- confidence: inferred
  path: website/docs
- confidence: cited
  path: website/docs/guides/track-change.md
- confidence: inferred
  path: website/docs/reference/formats.md
- confidence: cited
  path: website/sidebars.ts
revision: 8
---
## Finding and source

F13 (P1) from `docs/reviews/2026-09-05-architecture-review.md:449`, reviewed at `fd06a4d61bfb7b4990617810655dc181d6a3ab00`. Evidence locations: `crates/specify/ess-composition/src/lib.rs:277`, `crates/specify/ess-composition/src/lib.rs:548`, `crates/specify/ess-compiler/src/ir.rs:1569`, `crates/generate/ess-deployment/src/identity.rs:128`. Findings are attributed to the review; proposed implementation choices below are coordinator inferences.

## Acceptance

Every currently persisted ESS format has a cited catalog entry separating its discriminator, semantic/release version, validation level and canonical digest profile.

## Implementation boundary

Inventory actual producers and readers, supported versions and strictness, including differing composition input/output shapes and realization type discriminator. Name source_digest accurately in prose as a compiled-model digest. Describe existing canonical bytes and inventory relying parties; do not change bytes merely for uniform naming.

## Validation

Cross-check the inventory against format constants/Serde envelopes and readers; canonical fixture examples identify compact versus pretty-plus-newline hashing. Each proposed successor names migration prerequisites and unknowns instead of presenting a new endpoint as shipped.

Run package-scoped checks while implementing; the integration coordinator runs every step of `task check` and `task site-build` when required by `AGENTS.md`, retaining individual exit statuses and executed-case counts.

## Compatibility and exclusions

Schema resource identity has a separate design; no ess-ir/2 or mass format rename.

## Scope

Derived 2026-09-05 by aep-drive:story-scoper from exact story:review-format-catalog, .engineering/planning/story/review-format-catalog.md revision 6, and frozen ESS acb7859e3202ffdc1ca840dde67f7ca4da33c746; citations below describe that source revision, not a release or deployed reader — cited.

- **Primary surface:** documentation only; catalog existing discriminator, semantic/release version, validation boundary and canonical digest profile without changing any producer, reader, byte contract or schema identity (story Acceptance/Implementation boundary/Compatibility; review F13 at docs/reviews/2026-09-05-architecture-review.md:449) — cited.
- **New engineering document:** docs/design/review-format-catalog.md, the story's reserved internal catalog; record one entry per actual producing/reading surface, even where discriminator text is shared — inferred.
- **New public document:** website/docs/reference/formats.md, the narrow adopter reference; link public source owners and separate current contracts from successor prerequisites — inferred.
- **Existing public files:** website/sidebars.ts:30–34 needs reference/formats in its explicit Reference category; website/docs/guides/track-change.md:64,112 still names diff/1 and impact/2 as current defaults, and :107–109 advertises --generated committed-tree verification. At this exact revision VerifyCommand::Impact defines only from/to/suite/format (main.rs:241–250), verify_area forwards those (:1010–1015), command() only adjusts presentation (:867–886), and impact passes no GeneratedTree (:2025). This is source inspection, not a measured CLI rejection or an accepted-but-ignored flag. Correct these current-format/caller claims as part of the catalog landing; the existing CLI reference itself names no stale format default and need not expand scope — cited.
- **Conservative scheduling token:** website/docs remains reserved while other independently scoped public-document units retain that exact shared token; actual edits are the reference page and change-tracking guide above. It is a scheduling reservation, not permission to rewrite unrelated public pages — inferred.
- **Catalog columns and identity axes:** record writer entry point, reader entry point or absence, discriminator location/value, supported-version admission, closed/open decoding, separate semantic checks, canonical output layout, hash input/prefix, known byte consumers and migration prerequisites. Keep major-only specification Version separate from release SemVer and package version 0.18.0 (Cargo.toml:40; ess-domain/src/name.rs:233; deployment identity.rs) — cited.
- **Authored/compiled ESS:** authored format ess/1 has syntax parsing separate from support [1] and whole-system validation (crates/specify/ess-domain/src/system.rs:53,106,148,756). EssIr is currently unversioned, not ess-ir/1; it is Serialize-only and compilation establishes its invariants (crates/specify/ess-compiler/src/ir.rs:1254,1562). Numeric serialized specification Version and displayed vN are different representations of the same major-only label (ess-domain/src/name.rs:233) — cited.
- **Composition and realization inventory:** ess-composition/1 is both the closed authored service-array input and Serialize-only resolved-service-map output; compilation, not DTO parsing, checks supported marker and supplied service identities (crates/specify/ess-composition/src/lib.rs:29,277,303,548,1093). ess-client-plan/1 is a separate Serialize-only generated plan (:32,686). ess-realization/1 and ess-realization-ir/1 use the key type; authored closed decoding precedes compile checks against actual ESS/synthesis authority, and realization IR is Serialize-only (crates/specify/ess-realization/src/lib.rs:18,21,397,449,648) — cited.
- **Delivery inventory:** all current majors are /1: ess-build, ess-build-ir, ess-runtime, ess-runtime-ir, ess-component, ess-component-ir, ess-release, ess-release-bundle, ess-release-catalog, ess-stack, ess-stack-lock, ess-environment and ess-deployment (constants at crates/generate/ess-deployment/src/build.rs:9, runtime.rs:11, component.rs:11, release.rs:11, stack.rs:11, environment.rs:13). CLI additionally emits ess-deployment-diff/1 at crates/edge/ess-cli/src/main.rs:1591 — cited.
- **Delivery validation boundary:** current generic Serde for BuildIr, RuntimeIr, ComponentIr, ReleaseManifest, ReleaseBundle, ReleaseCatalog, StackLock and DeploymentIr crosses closed checked_deserialize and validate, including nested readers (crates/generate/ess-deployment/src/validation.rs:39 and its eight invocations). Catalog authored DTO reads separately from semantic compile and mutable-object revalidation; these checks do not establish unavailable ESS/realization authority, remote OCI contents or external trust — cited.
- **Current F01 defaults and reader support:** ess-diff/2 is the default; supported majors are exactly [1,2], with frozen /1 vocabulary/canonical order/bytes and explicit legacy-writer refusal for new kinds (crates/verify/ess-diff/src/delta.rs:13,31,34; raw.rs:43,67,105). Raw parsing is not admission; RawEssDelta -> EssDelta checks format, same-system identity, derived IDs/relations, order and uniqueness. Serialize validates selected version so mutation cannot relabel v2-only content as /1. Current ess-impact/3 embeds /2 delta and 26 graph relations; no ImpactReport reader is implemented (impact.rs:92–97; compiler graph.rs:126,134). Do not publish stale /1 and /2 default claims from earlier Scope/design source baselines — cited.
- **Current sliced profile:** every Constructs digest uses slice-sha256/2:<64 lowercase hex>; source and WholeModel remain bare 64 lowercase hex. The final 26-relation graph includes row-shape plus actual network-domain view exposure as well as CLI exposure (crates/generate/ess-gen/src/provenance.rs:248–253; crates/specify/ess-compiler/src/graph.rs:697,778; binding addendum docs/design/ess-semantic-diff-impact-evolution-design-v0.1.md:10). Bare legacy slice stamps owe regeneration even if their suffix hash matches; this is profile identity, not an ess-impact/4 or a source_digest reinterpretation — cited.
- **Stamp admission and consumer limits:** public Provenance::read_digests uses complete authoritative comment/JSON/YAML writer envelopes, exclusive aliases and supported profiles; paired API attribution agrees, actual synthesis Cargo frames have no paired YAML and do not validate TOML (crates/generate/ess-gen/src/stamp.rs:20,52,134). Generic Provenance/SlicedProvenance String Serde and ess-docs/1 nested page parsing do not prove profile admission; the old substring reader can fall back into model text. Actual committed-tree verification is library ess_diff::impact/verify_committed at impact.rs:1007–1046; CLI main.rs:2025 passes no GeneratedTree. Do not claim every old reader rejects profiles or every individual manifest has a CLI impact reader — cited.
- **Current conformance inventory:** authored type ess-scenario/1 (crates/verify/ess-conformance/src/authored.rs:222,255); suite provenance.suite_version currently ess-conformance/4 with declared support [1,2,3,4] (scenario.rs:238,308,363). SuiteFormat Deserialize only checks syntax (:423), so that list is not universal execution admission. Standalone ess-conformance-report/1 has checked closed generic Serde, current suite-major/count/list/status validation (evidence.rs:12,42–121), but does not establish exact-suite coverage or unique opaque result IDs. Detailed ConformanceReport JSON/YAML is currently unversioned (report.rs:535,607); it is separate from --report-out standalone JSON — cited.
- **Planned conformance contracts, not current formats:** docs/design/review-conformance-coverage.md:3,7,76,132,157,227,235,327–343 binds suite ess-conformance/5, standalone ess-conformance-report/2, detailed ess-conformance-run/2 and SuiteReference.digest_profile sha256-json-bytes/1; all 75 matrix rows and 10 version/mode pairings remain implementation obligations. Reader/opt-in phases retain suite/4 and report/1 defaults; coordinated default movement requires reader migration, Atlas ADR, actual generated-runtime and downstream checks. Counts-only report/run v2 against legacy suites stays diagnostic with unknown coverage and does not implement suite/5. Its historical S9 source citation to impact/2 is a frozen design baseline, superseded for current implementation inventory by F01's impact/3 — cited.
- **Timestamp compatibility:** current Rust report timestamps are Timestamp(u64), current generated Go completed_at is int64; retain those distinct legacy domains. Planned report/run v2 requires exact unsigned decimal integer tokens across 0..=18446744073709551615, full-range admission and checked conversion/arithmetic; that is not a claim of current browser/Go/AEP implementation (crates/specify/ess-primitives/src/time.rs:40; conformance/src/go/runtime.go:581–590; docs/design/review-conformance-coverage.md:172–179,303–315) — cited.
- **Other generated/imported families:** ess-docs/1 has ordinary derived Deserialize plus separate validate_page_ids, not uniform format/profile admission (crates/generate/ess-gen/src/document.rs:55–80); ess-browser-catalog/1 is the actual emitted browser catalog (crates/generate/ess-synth/src/web/catalog.rs:38–77). ess-service-interface/1 has closed decoding then separate validate; source_openapi and service.version are distinct labels (crates/generate/ess-openapi/src/lib.rs:12–78). External generated dialects are JSON Schema 2020-12, OpenAPI 3.1.0 and AsyncAPI 3.0.0, not new ESS format names (ess-gen/src/types.rs:323, openapi.rs:210, asyncapi.rs:154) — cited.
- **Infrastructure inventory and admission:** infra-observation/1, infra-ir/1, infra-spec/1, infra-drift/1, infra-simulation/1, infra-graph/1 and infra-projection/1 remain current (crates/infra/infra-domain/src/observation.rs:25; infra-compiler/src/ir.rs:52; infra-spec/src/spec.rs:37, drift.rs:57, simulate.rs:49; infra-analyze/src/graph.rs:46; infra-project/src/project.rs:68). Observation DTOs tolerate collected fields and still require their validation boundary; do not describe that as closed IR parsing. InfraIr now privately owns its model and offers checked try_transform; persisted read_document still checks format, closed shape, model digest and resolved reference membership without rederiving all original observation facts or domain values (infra-compiler/src/ir.rs:627,665,680; read.rs:62–123). This privacy repair added no new wire version — cited.
- **Unversioned and reused-marker outputs:** plan.json/SynthesisPlan and target.json/TargetReport remain unversioned (crates/generate/ess-synth/src/plan.rs:74; lib.rs:65,75,129). Rust and Go HTTP startup log records also emit ess/1 for a different shape (src/rust/http.rs:47, src/go/http.rs:49). Classify by actual surface and reader, not discriminator text alone; no generic format registry is required — cited.
- **Canonical-byte examples:** EssIr rendered output is pretty JSON+LF but source_digest hashes compact typed IR with no LF and bare hex (compiler ir.rs:1562,1575); slice/whole contract digest hashes a different compact construct payload (gen provenance.rs:248–356). Delivery canonical JSON is pretty+LF and Digest::of_bytes spells sha256:<hex> (deployment identity.rs:90,135); realization hashes the compact specification/synthesis/implementation tuple (realization lib.rs:1047). Infra model identity hashes key-sorted compact model JSON, excluding observation provenance (infra-compiler ir.rs:700–718). Do not call any of these raw source-file hashing, equate them by coincident hashes, or present the planned exact-suite byte profile as current — cited.
- **Relying-party inventory boundary:** current local entry points above and preserved tests identify ESS consumers. Existing bound conformance design records both AEP adapt_json and independent recorded_from_report plus the closed domain/predicate seam at AEP 00c742e4179593738a2e8aa69e2ecc07d3c89402; realization suite/report digests are references, not an established byte-verifying parser (docs/design/review-conformance-coverage.md:17–36; realization lib.rs:375). F01 records Atlas ADR0036 and SDK whole-file generator/checking, not a discovered external delta/impact parser (.engineering/planning/story/review-semantic-diff-coverage.md implementation confirmation). Those are cited prior source pins/records, not fresh external-main, release or deployment verification — cited.
- **Verification for this unit:** cross-check every catalog row against its actual constants/Serde/conversion/canonical writer, show representative compact versus pretty+LF hash inputs using existing frozen fixtures, and check source links/navigation. Documentation-only acceptance needs no invented prose tests or production/package edits; coordinator retains required task check/task site-build and publication evidence, and any actual compatibility execution must be named separately from source inspection (story Validation; AGENTS.md Gate) — inferred.
- **Website delivery:** both selected public pages are inside the existing website-root docs/**/*.md allowlist, so b10x.docs.yaml and workflow edits are unnecessary (b10x.docs.yaml:83–90). Engineering catalog stays outside that public source root. Workspace/repository instructions require ESS source publication first, then coordinator-owned managed Website deterministic source-lock refresh, Atlas snapshot rendering and Website/Atlas delivery gates; a local site build does not establish publication (AGENTS.md Public documentation operations; workspace AGENTS.md) — cited.
- **Would collide with:** the exact catalog, reference page, change-tracking guide and sidebar files; public stories reserving the same website/docs token; coordinator-owned Website source-lock/Atlas snapshot work at delivery. Source owners cited as inventory evidence are read-only, not package edit reservations; no schema identity/primitive semantics implementation or conformance/F01 production work is admitted by this scope — inferred.
- **Confidence:** high for documentation-only scope and the frozen source version/default/reader findings because the story and actual owners establish them; no complete external/deployed inventory or newly executed compatibility claim is implied — cited.
