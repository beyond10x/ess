---
format: aep.planning-md/1
id: story:review-consumer-coverage
kind: story
status: draft
title: Require explicit consumer coverage for model extensions
tags:
- P1
- review-2026-09-05
relations:
- decomposes: epic:review-boundary-remediation
- serves: vision:O2
- depends_on: story:review-semantic-diff-coverage
scope:
- confidence: inferred
  path: Cargo.lock
- confidence: cited
  path: Taskfile.yml
- confidence: cited
  path: crates/edge/ess-xtask
- confidence: inferred
  path: docs/design/review-consumer-coverage.md
revision: 6
---
## Finding and source

F16 (P1) from `docs/reviews/2026-09-05-architecture-review.md:522`, reviewed at `fd06a4d61bfb7b4990617810655dc181d6a3ab00`. Evidence locations: `Taskfile.yml:138`, `crates/edge/ess-xtask/src/main.rs:54`, `docs/reviews/2026-09-05-architecture-review.md:532`. Findings are attributed to the review; proposed implementation choices below are coordinator inferences.

## Acceptance

The extension gate fails when a new semantic construct or field has no tested support or explicit refusal for an inventoried consumer.

## Implementation boundary

Maintain a typed consumer matrix for validation, IR, references, diff/impact, projections, synthesis and conformance, with links to executable cases or explicit unsupported/refusal evidence. Add a Rust xtask gate that checks the matrix against the actual authoritative model surface; avoid a parallel hand-maintained list silently omitting fields. A coverage record is not a substitute for running its behavioral test.

## Validation

Mutation proof: add a representative semantic field/consumer obligation without coverage and observe failure, then supply supported/refused behavior evidence and pass. Include concrete F01 rows. The existing fuzz story owns general document fuzzing rather than this matrix.

Run package-scoped checks while implementing; the integration coordinator runs every step of `task check` and `task site-build` when required by `AGENTS.md`, retaining individual exit statuses and executed-case counts.

## Compatibility and exclusions

Do not rewrite passing gates or claim every target supports every construct; unknown is a valid visible matrix entry with owned follow-up work.

## Scope

Derived 2026-09-05 by aep-drive:story-scoper from current story:review-consumer-coverage revision 4 and its implemented F01 dependency (revision 26), at published ESS 0d08e219a61cd1911b913d3da20c8e4b87993b94 with production acb7859e3202ffdc1ca840dde67f7ca4da33c746 — cited.

- **Primary surface:** crates/edge/ess-xtask; add a repository-only Rust consumer-coverage command, typed inventory/matrix checking and its own focused tests. Existing Command/run and workspace_root at src/main.rs:54,110,168 are the command seam; schema at :423 already derives the actual RawSpecFile JsonSchema, and generate at :443 checks actual published generator output — cited.
- **Binding design:** docs/design/review-consumer-coverage.md, currently reserved but absent; bind model roots, schema/AST authority, stable row identity, supported/refused/unknown dispositions, actual test execution and failure semantics before coding. The proposed source-AST option below is a scoping choice for the coordinator, not an already accepted implementation — inferred.
- **Gate wiring:** Taskfile.yml:138,174 owns projection-check and the ordered check tasks; add a separate consumer-coverage check while preserving existing fmt, Clippy, tests, rustdoc, examples, projection/schema, release and action checks. Neither the new matrix nor a source scan substitutes for their behavioral executions — cited.
- **Dependency edit:** own ess-xtask/Cargo.toml under the package token; a real Rust AST parser is needed for the proposed option. syn 2.0.119 is already locked at Cargo.lock:1245, but ess-xtask's current dependency entry (:468) does not include it and its manifest has no parser. Reserve Cargo.lock as inferred for the added direct dependency/features. No source establishes a need to edit root Cargo.toml; a package-local parser dependency can use the existing locked version without introducing a workspace alias — inferred.
- **Authoritative authored surface:** derive schema nodes/properties/tagged alternatives/references from schemars::schema_for!(ess_domain::spec::RawSpecFile), not schemas/generated/ess.schema.json or a hand-maintained field list (ess-domain/src/spec.rs:45; xtask main.rs:423). That schema is necessary but insufficient: TypeRef deliberately exposes String (ess-domain/src/types.rs:274), and Ranking uses schemars(with = String) (view.rs:225), hiding semantic internal structure — cited.
- **Authoritative resolved surface:** actual EssIr and Resolved* declarations are in crates/specify/ess-compiler/src/ir.rs:200–1254, with EssSemanticRef at refs.rs:269 and DependencyRelation at graph.rs:74. EssIr is Serialize-only, has private and optional fields, and resolves nested domain/primitives types. An inhabited Billing IR or its serialized output cannot establish that every declaration/field exists — cited.
- **Proposed AST inventory, no compiler edits:** keep parsing/traversal inside xtask and combine the raw schema with structural Rust declarations rooted at RawSpecFile/Specification, EssIr/Resolved* and the actual semantic-reference/graph vocabularies. Traverse declared domain/primitives dependencies needed by those roots, including types.rs TypeRef/Primitive, view.rs Ranking, entity lifecycle, and ess-primitives predicate.rs Predicate/Operand/CompareOp and facts.rs FactValue; state the exact owner/module/import resolution policy in the design. Compiler fields or new attributes must not silently disappear behind a fixture or a manually listed set of field names — inferred.
- **AST boundaries that must fail closed:** named/tuple fields, enum alternatives, aliases, Option/Vec/BTreeMap/BTreeSet wrappers, serde/schemars renames/flatten/skip/default/custom representations and production cfg must be explicit. Existing handles! (compiler ir.rs:99) and semantic_refs! (refs.rs:39) require a narrowly specified macro treatment or explicit identity-leaf treatment; syn parses macro tokens but does not prove their expansion. Unsupported declaration/macro/cfg/import/type shapes, unknown newly reachable owners and failed resolution must stop the gate, never become an empty inventory. Exclude cfg(test) deliberately and test that exclusion. This is structural inventory, not behavioral source-token checking — inferred.
- **Consumer inventory:** validation/assembly, resolved IR, stable references/graph, diff and impact, individual projections, synthesis targets and conformance are required by the story. Derive/check the projection set against ess_gen::generators() (gen src/lib.rs:55: Docs, Site, JsonSchema, OpenApi, AsyncApi) and synthesis targets against actual Target (synth src/lib.rs:83: Rust, Go, Web, Clap), rather than letting a new generator/target evade a stale parallel list. Distinguish docs IR/Markdown/HTML and graph rendering, shared schema mapping, HTTP payload embedding, suite building, Rust execution, generated Go execution and generic browser replay — cited.
- **Additional real semantic consumers:** composition compile (crates/specify/ess-composition/src/lib.rs:1093), realization compile (:648) and deployment runtime compile (crates/generate/ess-deployment/src/runtime.rs:484) consume EssIr; they need inventoried dispositions, not an unmentioned exclusion. External OpenAPI importing/schema-contract TypeScript are downstream external-document consumers, and infra-* is a separate observed model; do not invent direct EssIr support rows or import all their own models into this first semantic-extension gate — cited.
- **Typed cell contract:** a finite discovered construct/field/variant ID plus consumer ID has supported behavior, explicit tested refusal/obligation, or visible unknown with owned follow-up. Record concrete case identity and expectation, not only a file path or test count. Detect duplicate/stale cells, new uncovered model IDs and new consumers; reject blanket future-field wildcards that automatically qualify additions. Design must separate grandfathered visible baseline unknowns from a new unaccounted field, for which acceptance requires failure; an owned unknown is not tested support — inferred.
- **Execution contract:** resolve linked cases to actual runnable package/target/test identities and require their behavioral assertions to execute with nonzero expected selection; missing/renamed, filtered-to-zero, ignored, setup-failed or failing cases cannot satisfy support/refusal. Root must bind whether xtask executes the finite linked cases itself or consumes same-run machine results. Counts alone are insufficient: CLI tests/go_conformance.rs:164–168 returns normally when Go is absent, so its Rust harness can report a pass without running Go. Require the real toolchain/lane precondition and actual generated-runtime scenario counts for claimed Go coverage, not only the outer test result. Avoid recursive cargo/test invocation from workspace unit tests or incidental network/live-service dependence — inferred.
- **F01 evidence rows to retain:** relations name/kind/target/cardinality/via/order; component reach plus CLI top/grouped views; outcome sets/refuses; view parameter types/naming/order and ranking; reusable shape and residual owner relocation. Concrete cases are families.rs:1831–1973; graph.rs:204,290,326,420; review_adversary_f01.rs and review_adversary_f01_pass2.rs:115–156 under crates/verify/ess-diff/tests. These prove typed differences, conservative residual obligations, graph paths and actual emitted OpenAPI digest/body obligations after F01; they do not show every projection/synthesis/conformance consumer supports all those fields — cited.
- **Minimal meaningful invariants:** discovered inventory equals explicitly accounted cells; every current concrete consumer is included; each supported/refused cell runs its stated behavior; additions and unsupported AST/schema shapes fail; false/missing case attribution fails; real semantic mutations change the expected typed IR/reference/projection/synthesis/conformance answer or an explicit refusal, with an unchanged/equivalent control. Use F01's real network view and reusable shape tests as cross-consumer examples, not only their diff-kind assertions — inferred.
- **Mutation direction:** first add a representative field/variant through the same authoritative schema/AST extraction path (a copy or fixture under assigned scratch), leaving its cell absent, and assert gate failure naming owner/field/consumer. Cover an optional field absent from the inhabited example and a string-backed semantic type so a schema-only or fixture-only implementation goes red. Also remove a real F01 cell, point one at a nonexistent/zero-selected case, and provide a deliberately false behavioral result. Only explicit supported/refused behavior plus successful execution should repair the corresponding new obligation; changing a label alone must not — inferred.
- **Existing executable coverage beyond F01:** compiler tests/billing.rs validates typed handles/IR, tests/view_shapes.rs:48 checks reusable row handles and fields, tests/oracle_fixture.rs:226,303,602 checks non-Billing semantic reachability; gen tests/agreement.rs:740 compares shared Schema/OpenAPI/AsyncAPI fragments and :862 asserts actual keyword coverage, with explicit limits for constructs a fixture does not expose. gen tests/relations.rs:97,138 covers carrier annotations; tests/docs.rs/corpus.rs cover documentation content/bytes. These are useful mapped cases, not a complete automatic matrix — cited.
- **Synthesis/conformance evidence:** synth tests/synthesis.rs:703,730 connects plan obligations to actual refusing stubs; tests/relations.rs:80 checks generated carrier fields; tests/web.rs:153,184 distinguishes recorded weakenings and emitted source; tests/clap.rs:181,233 checks declared CLI grammar. Conformance tests/synthesis.rs:2100,2156,2191,2334,2349 covers explicit unobservable refusal, value-object invariants, suite roundtrip and dependencies; tests/witness.rs:776 checks classified undecidability. The actual generated-Go defect/order execution lane is CLI tests/go_conformance.rs:164,194,241,408, not a source-string assertion or generic browser animation — cited.
- **Exact edits versus evidence paths:** narrow proposed edits remain ess-xtask (new module/matrix/test/fixtures and its manifest), Taskfile.yml, new design, and inferred Cargo.lock. All domain/compiler/gen/synth/diff/conformance/CLI/composition/realization/deployment paths above are source or existing-test evidence, read-only in this option. If matrix gaps require new owner-package assertions or production metadata, re-scope those exact owners before dispatch rather than silently widening the unit — inferred.
- **Counts-wave collision:** no planned ess-conformance or ess-cli source/test writes under the proposed xtask-owned option, so source-file separation from the conformance-count story is possible. Its runner/evidence/Go/CLI changes can still alter linked case names/results; validate the matrix against the final integrated tests and preserve current suite/4/report/1 versus opt-in report/run2 distinctions. Do not snapshot report bytes or claim suite/5 coverage as semantic-field support. Cargo.lock is a real potential shared edit requiring coordinator sequencing; Taskfile gate ownership must also remain singular — inferred.
- **Generated/public outputs:** no generated/**, schemas/generated/**, website/** or public sidebar edit is needed for a repository-only gate/internal design. Existing byte checks should stay unchanged; no regenerated matrix is placed in public artifacts. Taskfile is a validation workflow change, so AGENTS.md still requires task site-build beside the integration task check; Website/Atlas publication work is needed only if the final unit intentionally gains public source changes, which are not reserved here — inferred.
- **Would collide with:** any ess-xtask source/tests/manifest work, Taskfile gate edits, the new design or Cargo.lock dependency resolution. Conditional collisions with compiler/model owners or conformance/CLI arise only if the coordinator chooses production metadata or new tests outside xtask; those are not silently authorized by this scope — inferred.
- **Confidence:** medium, because owner paths and executable-case locations are grounded but the AST roots/resolution, baseline unknown policy and same-run behavioral evidence still require a binding coordinator decision. This scope proposes a bounded candidate and does not select a wave or claim F16 complete — inferred.
