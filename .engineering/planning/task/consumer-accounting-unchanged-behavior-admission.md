---
format: aep.planning-md/1
id: task:consumer-accounting-unchanged-behavior-admission
kind: task
status: active
title: Admit exact current behavior for unchanged eligible obligations
relations:
- decomposes: story:consumer-accounting-baseline-never-extended
- serves: vision:O2
revision: 5
---
## Required outcome

Permit exact current Supported or named Refused behavior to qualify a model-consumer tuple that
is still eligible under the immutable initial baseline. The six stable direct-file wire targets
required by the accepted aggregate/2 frontier must be admitted from their actual cases instead
of colliding with a simultaneously inserted BaselineUnknown cell.

## Evidence and boundaries

Current model-behavior authority has99claims; six stable targets Field,Naming,Predicate,QualifiedName,
StateName,TypeRef retain the exact historical shape/profile and frozen eligibility. plan_v4 combines
the reviewed claims, plan_versioned inserts every unchanged unknown first, and insert_v2 refuses
their duplicate cells. reconciliation::resolve correctly refuses changed-identity decisions for
unchanged tuples. Independent scope inspection confirms no alternative promotion path.

Preserve the baseline byte for byte, exact historical identity/provenance, complete current matrix,
all changed/retired tuple requirements, mandatory acquisition, metadata and aggregate guards.
No broad alias, blanket support, eligibility extension, hidden duplicate overwrite or forced format
migration is authorized. The binding design must explicitly decide whether any persisted meaning
changes; internal implementation capability alone does not require a new format.

## Acceptance

1. Reproduce the exact unchanged-unknown/current-behavior collision using the real current planner
   and resolver, retaining actual runner results before repair.
2. Review a binding design for explicit reviewed-behavior admission, before source implementation.
3. Preserve all ordinary duplicate/conflicting behavior refusals, wrong model/profile/source/case
   rejection, and no-proof BaselineUnknown fallback. Metadata and aggregate claims cannot silently
   override unknown eligibility. A claim without its actual native evidence never qualifies.
4. Exercise all six actual stable targets, one unrelated unproven unchanged tuple, Supported and
   named Refused claims, missing/tampered execution, and historical readers/byte stability.
5. Preserve exact current cell conservation, full reconciliation and source freshness. Run affected
   strict checks, two independent code examinations, and all complete consumer/repository/site gates
   before parent integration. A focused success is not complete accounting or ESS evolution.

Source scope is inherited from the existing complete accounting story; coordinator owns all DATA,
design and AEP writes. Dependencies and actor ownership are recorded before dispatch.

## Accepted design and actual current-refusal reproduction

Independent design pass1 approves with findings[]; original report SHA
1f9b51ed968e6add9cdefc566ac13aeac7a9ec214c56b78452f0f0be0489581e recorded unchanged.
It supports retaining accounting/4 because initial eligibility is unproven fallback and the
existing Supported/Refused identity/envelope meanings already describe executed current behavior.
No correction was required, so no additional design round is manufactured.

Root applied the original external reproduction patch after the99claim native diagnostic ended,
then ran the actual locked/offline focused test. Session48492 terminated0:1passed/0failed/0ignored/
177filtered, asserting the sole precise duplicate current-behavior/unchanged-unknown diagnostic.
Log5958e19d1915b81d69dbbfb06eb95edb347974e8ba651098d4733f94b026e6f9.
This passing regression demonstrates the current refusal; it is not a passing repair. Independent
source analysis confirms the same exact condition on all6actual stable target cells.
The review's unexecuted-patch limitation describes its read-only input, not root's subsequent
execution. Original review remains unchanged; the actual execution is recorded separately here.

Coordinator accepted the status-only design amendment. Implementation must first add and execute
the positive desired-admission regression against unchanged production, preserving its actual
failure, before repairing only the active v4 planner. Root owns all DATA/design/AEP; original
aggregate author may own only enforce.rs and accounting_v3_tests.rs under the separate brief.
Immutable baseline, historical planners/readers, strict duplicate/execution guards, full-matrix
conservation, native qualification, independent code examinations and full gates remain required.

## Bounded implementation assignment completed

The accepted unchanged-behavior admission repair is implemented in enforce.rs and
accounting_v3_tests.rs. It prevalidates exact current claims before v4 fallback selection, retains
original unknown provenance for unclaimed tuples, and preserves historical v2/v3 planner behavior
and ordinary duplicate, metadata and aggregate collision refusals. Persisted formats are unchanged.

Actual desired-admission regression failed with the expected duplicate cell before repair and
passed after it. The exact final affected ess-xtask suite passed184unit and70integration tests,
with3existing ignored cases; final strict Clippy and scoped formatting passed. Four explicit
predicate mutants (model-prefix, profile-only, unconditional omission, generic duplicate overwrite)
each failed with exit101 under named assertions. Each mutation restored identical production bytes;
the restored focused test passed. Raw logs, exits, patches and the immutable author handoff remain
at local-evidence:ess-evolution/waves/0004-ess-accounting/aggregate-reference-evidence/.

Final enforce.rs SHA-256148f5cc47c734888a312852e4fa3a0d386aeb51d729a2d428aac74eb02ec7865;
accounting_v3_tests.rs b695e087020b37f1ac112559c8a0e4cd06a68fbcbcb58119d7fa7472ac4d12cf.
Author report SHA-25627878467d6d884420effe7db6fcf61ec78e2181bf9129937965e29d6ff4a0909.
The coordinator read the actual final receipts and rechecked source and unchanged baseline hashes.

The worker assignment is closed, source frozen, build token yielded and only the worker's own
lease released. The owning task stays active: submitted-code examinations, complete current
reconciliation and actual qualified behavior, consumer-check, task check and site-build are still
required. No parent or evolution milestone is closed by this implementation handoff. Corrections
outside this fixed assignment require separately justified scope, not an implicit extension.
