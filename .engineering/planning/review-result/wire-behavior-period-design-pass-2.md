---
format: aep.planning-md/2
id: review-result:wire-behavior-period-design-pass-2
kind: review-result
status: active
title: Wire behavior and string schema design, final technical review
relations:
- reviews: task:consumer-accounting-wire-behavior-period-parity
revision: 1
---
approve

What I read: the immutable first review, task draft 3 and inherited parent scope 26, both revised binding documents, the retained ExternalRef source finding, and the exact Period, ExternalRef, loader, wire extractor, binding, selection, reading, component, command, type/reference, model-behavior, and aggregate contracts, using read-only `nl -ba ... | sed -n`, `rg -n`, `git status`, and `git diff`.

Facts observed: the revised companion makes every selected whole-node claim conditional on a complete finite member/default/null/branch ledger, literal source-pointer/reference-target assertions, site-specific returned-IR observations, named single-edit P/A controls, and a machine-readable candidate ledger that refuses missing entries. It covers all eleven RawBindingSpec members, all four complete NamedType branches and shared properties, all ten RawComponentSpec members, complete RawCommandSpec and RawErrorSpec surfaces, RawOutcome at the command edge, every selected sparse-map level, and the finite shared reference targets at each source site. The Period range schema remains exact. The separately demonstrated ExternalRef object projection contradicts its existing string Serde/parser boundary; the selected explicit string JsonSchema repair preserves the authored bytes and deliberately leaves stricter lexical admission to the loader. Fresh post-both-repairs inventory determines every direct, removal, reference, and aggregate-current transition; the five aggregate identities, frozen old witnesses, unchanged-profile ShapeDelta residuals, and 27 existing Rust claim meanings remain fixed.

What I could not establish: post-repair hashes/counts, generated schema bytes, emitted ledger contents, actual pointer/loader observations, refreshed source bindings, or diagnostic receipts, because implementation has not started. The design treats each as required implementation evidence and authorizes no claim or aggregate adoption before it exists.

```findings
[]
```
