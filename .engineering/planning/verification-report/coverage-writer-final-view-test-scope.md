---
format: aep.planning-md/1
id: verification-report:coverage-writer-final-view-test-scope
kind: verification-report
status: draft
title: 'Coverage writer final test scope: full admission and current unused view projection'
relations:
- verifies: story:review-conformance-coverage
revision: 1
---
# Final source pass 2 view-value assertion disposition

Decision: root, 2026-09-06, under the accepted coverage story and standing remediation approval.
This is a scope interpretation grounded in the existing binding, not a new execution promise or
an F15 closure. The final adversary report must be sealed and recorded unchanged before routing.

## Measured behavior and reached surface

The reviewer reports the new actual Firefox case admits the parent/child input with 12 declared
Node payload owners and refuses each of 12 changes to a surviving scenario after the changed
child digest is recalculated. The returned values retain the seven arbitrary keys, including an
own __proto__ data property. The test then asserts equality between every returned payload and
an ordinary JSON-number payload. That last assertion fails for the three nested view expectation
owners (indices 6, 7 and 9); their numeric values retain NumberToken raw spellings. The first full
package command executed 648 cases: 647 passed, 1 failed, none ignored, across 64 summaries. The
reviewer's final sealed command and source hashes remain authoritative for the pass.

Root independently read coverage-admission.js:268-283 and :308-333: expectation fields enter the
same finite Number normalization as other Node owners for admitted semantic lineage comparison.
The final return at :504-511 instead constructs the reduced player surface from selected.document
and normalizes only top-level input, params, payload and fields. coverage-player.js:73-102 has no
expect_view or eventually_view execution consumer. These observations distinguish full admission
from the returned internal projection; root found no supported caller consuming those nested
view fields as normalized execution values.

The accepted transport binding's Browser pairing section preserves the finite projection and
its reduced view behavior. The main coverage binding states suite/5 adds no execution semantics,
requires full payload admission and preserves arbitrary Node keys. The coverage story's
Preservation boundary expressly leaves F15 open. story:review-browser-replay-fidelity separately
owns supported view behavior and visible unsupported semantics. Consequently the final whole
returned-object equality assertion overstates this migration's supported surface. The 12 full
lineage checks and arbitrary-key preservation remain required.

## Exact correction authorized after immutable pass recording

Preserve every original red source copy, raw output and command receipt. Change only the new
CLI adversary case: compare the nine normalized payload owners with the existing exact ordinary
payload; compare owners 6, 7 and 9 with their exact observed token-bearing payload, retaining all
seven keys and nested values. Keep all 12 accepted/refused lineage assertions and the independent
own-property assertions. Name story:review-browser-replay-fidelity in the state assertion's
message and require deliberate update when that owner adds a supported view execution surface.
Do not ignore, delete or convert the test into an acceptance-only check. No inherited test,
production source, fixture, binding semantics or default changes are authorized by this decision.
Root reads the exact correction and runs the focused case plus package/lint checks on frozen
corrected bytes. This is verification of the final disposition; no third source attack runs.

All observations above distinguish the reviewer's measured results from root source inspection.
The final review-result and a subsequent verification record will retain the exact report and
correction results. No green verdict, integration gate, actual producer correspondence or
publication is asserted here.
