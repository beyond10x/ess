---
format: aep.planning-md/1
id: verification-report:coverage-refusal-rendering-decision
kind: verification-report
status: draft
title: Coverage refusal rendering D1 decision
relations:
- verifies: story:review-conformance-coverage
revision: 1
---
# Coverage refusal rendering: explicit D1 correspondence decision

The coordinator retains the sealed D1 rule: new coverage refusal message is the complete original
typed `Refusal::to_string()` diagnostic, including its header, original cause and help, with the
baseline code also retained separately. Authored file identities are already checked relative
identities before formatting. Never add the CLI outer `refused: ` wrapper, normalize source text,
deduplicate equal occurrences or append ordinal text.

This is a concrete narrowing of the main binding's permitted nonempty diagnostic text including
original cause. That earlier minimum did not independently require full Display/help. The initial
unit brief repeated that minimum; the independently prepared mapper separately fixed full Display
under root D1. This record makes the chosen correspondence explicit in the owning transport design,
rather than silently changing mapper expectations to fit a writer result.

Source readback on frozen writer `874962d3c7f84d0337bb1477892da065aeadc2fd` found
`coverage_build` using `refusal.cause.to_string()` for generated and authored records. In the
retained S10 original suite, the message starts with the `reminder_count >= 0` cause, whereas D1
expects the full ESS-SYNTH-011 diagnostic. This check read suite inventory, not report semantics;
no actual mapper or AEP helper ran. The original source suite, producer output, final handoff and
preparation remain intact. The sealed resolution SHA256 remains
`6feb6754888aefd7fbd9b371fdc09ed42ec30bbcbef3962176d7e1a9f8ca9f8b`.

Correction authority after source attack1 is recorded: change the new generated/authored inventory
message mapping to full typed Display; keep every code/cause/identity/occurrence and selection
assertion. Add independent full-rendering regressions. The new producer test expectation may load a
byte-exact pinned copy of the existing D1 resolution in a new fixture, keeping semantic-plan.json,
input-catalog.json and all legacy input/oracle bytes unchanged. The newly introduced M41 cause-only
message equality may be strengthened to the exact full original generated diagnostic; retain its
actual defensive-insertion reachability limitation. No inherited test weakening is authorized.

The earlier final642-case package result and seal remain historical. Corrected source needs its
own affected package checks, second source attack, fresh final source/export records and root
integration/correspondence before publication. This decision does not claim those results, change
legacy default formats, select a release, or authenticate coverage inventory.
