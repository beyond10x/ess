---
format: aep.planning-md/1
id: verification-report:normalization-recovery-20260906
kind: verification-report
status: draft
title: Recover interrupted normalization qualification
relations:
- verifies: story:go-normalization-pattern-semantics
revision: 3
---
## Recovery and authority

The operator requested continuation of the saved source-pinned normalization session
and invoked planning, wave and managed-worktree skills. Recovered authorization covers
closing the ESS gaps, integration to main and a release after complete mapping and
passing release gates. This record closes a bounded integration checkpoint; it does
not assert completion of the parent objective or a new release.

Wave skill 0.8.0. The harness exposes generic collaboration dispatch, so agents read
the installed implementor, adversary and story-scoper charters explicitly. Models
were inherited without override. Token costs were not exposed. Builds remained
serial; specification and read-only design work ran alongside them.

## Unit, resources and stage

story:go-normalization-pattern-semantics, serving vision:O2. Recovered base
9899faed9eb10eb2e4d9e6dc946f6d732e87c6b6 in manager record wt-bf45625a6a50;
branch impl/normalization-base64-resume. Build cache target/ and assigned scratch
and logs target/resume-base64/. Initial target size was 15 GB, later 16 GB.
Available filesystem space varied from 23 to 31 GB during unrelated external activity.
No cache or managed tree was deleted. Stage: green integration checkpoint.

Source commit c8b8eb1 is authored and committed by the organization bot. Incoming
main 95a93ca is included. Other review-wave worktrees retain their owners and were
not relaunched, modified or retired.

## Review and runtime evidence

Pass 1 found one introduced confirmed blocker: qualifying Bytes made a map reachable
whose unqualified propertyNames pattern had no separate structural obligation. The
unchanged test was reproduced red before correction. Private schema-position
collection now discovers that pattern without changing structural report obligations.
Class coverage enumerates all eight primitive map-key projections and exact escaped
nested source pointers. The original adversarial case is retained in the source tree.

Pass 2 found no additional issue; the unchanged case executed 1/1, exit 0. Two passes
were used. Immutable review-result records preserve each finding set and separate
review_outcome evidence records the correction and the subsequent no-op.

The shared corpus has 830 entries across bundle values, model values and model keys:
2,490 native Go subtests passed, none failed/skipped. Generated Rust executes the same
three contexts with both default and arbitrary_precision serde_json configurations.
The accepted language is the exact frozen base64 pattern, including nonzero unused
pad bits. Other patterns, including equivalent spellings, remain explicit refusals.
Focused structural/CLI regressions, formatting and strict Clippy also passed.

## Complete gate

Each required step ran separately with its own captured exit. Then the repository's
literal task check command also exited 0. Workspace test summaries total 1,783 passed,
0 failed, 0 ignored and 0 filtered across 136 Rust summary groups. This is the default
workspace lane; opt-in native Go evidence is recorded separately above.

| Step | Exit | Seconds |
| --- | --- | --- |
| fmt-check | 0 | 3.876 |
| clippy | 0 | 3.073 |
| test | 0 | 44.553 |
| doc-check | 0 | 1.752 |
| example-check | 0 | 1.226 |
| projection-check | 0 | 0.544 |
| release-check | 0 | 0.083 |
| action-check | 0 | 0.048 |
| site-build | 0 | 19.002 |

Logs: target/resume-base64/gate/<step>.log; machine-readable exits:
target/resume-base64/gate/results.json; literal gate: gate/task-check.log. Site
validation generated the static site successfully. It is not remote publication
or unified Website delivery evidence. No release tag was created at this checkpoint.

## Canonical planning replay

A private snapshot preserved local planning artifacts and appended events before
version-control reconciliation. Incoming main's journal is canonical; local intentions
were replayed through the AEP CLI. No divergent journal tails were concatenated and
incoming historical scope/review records were retained.

The original pass-2 log includes a machine-specific path. Its private record and
verbatim log remain in recovery scratch. The distinct
normalization-base64-adversary-pass2-public review explicitly declares path-normalized
output and preserves findings/exits. Private adopter fixture evidence is summarized
here without publishing adopter identities. The original reports remain private.

AEP validation exits 0. It warns about 14 historical review records and also the
present empty pass-2 findings list. No immutable review was edited to silence it.
An initial validation overlapped an outstanding CLI write and observed transient
revision drift; after the write completed, the sequential validation is valid.

## Remaining work

The parent source-pinned-data-normalization stays active. TypeScript execution,
lexical JSON capture, positional input decoding and truthful model-owned binary64
fields are separate recorded gaps. Binding designs precede overlapping code work.
The TypeScript design uses JSON text and internal bigint as the stated engineering
assumption; no optional user reply is claimed. Release, adoption and published-docs
delivery remain pending until the complete requested capability is integrated.
