["er.request/1",{"definition_version":1,"fields":{"body":"## Outcome\n\nA guard such as `tags.count > 1` (over command input or over a stored field) gets synthesized\nscenarios for both branches.\n\n## Why\n\nFound during the 2026-09-25 wave. Unit A1 (#94) synthesizes list guards from one-element lists, so\n`.count >= 1` and `exists`/`forall` work, but `.count > 1` refuses: over input with `ESS-SYNTH-003`\n(\"no candidate of the 2 tried satisfies labels.count > 1\"), over a stored list with `ESS-SYNTH-001`\nand a hint that tells the author to drop the field (review-result:c-adversary-pass-1, finding 3;\nunit A1's own report). Cases in `crates/verify/ess-conformance/tests/stored_field_guards_adversary.rs`\npin today's refusal.\n\n## Acceptance\n\n- The witness builds a list of `N + 1` (and `N`) elements for a `.count` comparison against literal\n  `N`, bounded, for input and stored lists.\n- The re-pinned adversary cases flip to asserting both scenarios.\n- The misleading \"drop it from the command's input\" hint is not printed for a count guard.\n","document":{"events":[{"args":{"command":"create-entity","data":{"body":"## Outcome\n\nA guard such as `tags.count > 1` (over command input or over a stored field) gets synthesized\nscenarios for both branches.\n\n## Why\n\nFound during the 2026-09-25 wave. Unit A1 (#94) synthesizes list guards from one-element lists, so\n`.count >= 1` and `exists`/`forall` work, but `.count > 1` refuses: over input with `ESS-SYNTH-003`\n(\"no candidate of the 2 tried satisfies labels.count > 1\"), over a stored list with `ESS-SYNTH-001`\nand a hint that tells the author to drop the field (review-result:c-adversary-pass-1, finding 3;\nunit A1's own report). Cases in `crates/verify/ess-conformance/tests/stored_field_guards_adversary.rs`\npin today's refusal.\n\n## Acceptance\n\n- The witness builds a list of `N + 1` (and `N`) elements for a `.count` comparison against literal\n  `N`, bounded, for input and stored lists.\n- The re-pinned adversary cases flip to asserting both scenarios.\n- The misleading \"drop it from the command's input\" hint is not printed for a count guard.\n","status":"draft","title":"A count guard above one gets synthesized scenarios"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/count-guards-above-one-are-synthesized"},"changed":{"$aep":{"archived":false,"metadata":{"created_at":1790381478223,"id":"ent-22f5dc31318fb4f95e98f11d288053fe","locator":"ep://planning/store/story/count-guards-above-one-are-synthesized","provenance":{"created_by":"human:timo","updated_by":"human:timo"},"revision":1,"type":"aep.story/v1","updated_at":1790381478223}},"body":"## Outcome\n\nA guard such as `tags.count > 1` (over command input or over a stored field) gets synthesized\nscenarios for both branches.\n\n## Why\n\nFound during the 2026-09-25 wave. Unit A1 (#94) synthesizes list guards from one-element lists, so\n`.count >= 1` and `exists`/`forall` work, but `.count > 1` refuses: over input with `ESS-SYNTH-003`\n(\"no candidate of the 2 tried satisfies labels.count > 1\"), over a stored list with `ESS-SYNTH-001`\nand a hint that tells the author to drop the field (review-result:c-adversary-pass-1, finding 3;\nunit A1's own report). Cases in `crates/verify/ess-conformance/tests/stored_field_guards_adversary.rs`\npin today's refusal.\n\n## Acceptance\n\n- The witness builds a list of `N + 1` (and `N`) elements for a `.count` comparison against literal\n  `N`, bounded, for input and stored lists.\n- The re-pinned adversary cases flip to asserting both scenarios.\n- The misleading \"drop it from the command's input\" hint is not printed for a count guard.\n","status":"draft","title":"A count guard above one gets synthesized scenarios"},"entity":"aep.entity","from_state":null,"id":"ent-22f5dc31318fb4f95e98f11d288053fe","payload":{"actor":"human:timo","at":1790381478223,"causation":"cmd-child-13b6637487240e51638cb455a44f5267f046801032250f66bc13757a2faf94b5","correlation":"protocol-artifact-new","event_id":"aep.entity:ent-22f5dc31318fb4f95e98f11d288053fe@1#0~2436510862671382","executor":null,"recorded_at":"2026-09-26T00:11:18Z"},"revision":1,"to_state":"draft","type":"aep.entity.create/v1","version":1}],"fields":{"$aep":{"archived":false,"metadata":{"created_at":1790381478223,"id":"ent-22f5dc31318fb4f95e98f11d288053fe","locator":"ep://planning/store/story/count-guards-above-one-are-synthesized","provenance":{"created_by":"human:timo","updated_by":"human:timo"},"revision":1,"type":"aep.story/v1","updated_at":1790381478223}},"body":"## Outcome\n\nA guard such as `tags.count > 1` (over command input or over a stored field) gets synthesized\nscenarios for both branches.\n\n## Why\n\nFound during the 2026-09-25 wave. Unit A1 (#94) synthesizes list guards from one-element lists, so\n`.count >= 1` and `exists`/`forall` work, but `.count > 1` refuses: over input with `ESS-SYNTH-003`\n(\"no candidate of the 2 tried satisfies labels.count > 1\"), over a stored list with `ESS-SYNTH-001`\nand a hint that tells the author to drop the field (review-result:c-adversary-pass-1, finding 3;\nunit A1's own report). Cases in `crates/verify/ess-conformance/tests/stored_field_guards_adversary.rs`\npin today's refusal.\n\n## Acceptance\n\n- The witness builds a list of `N + 1` (and `N`) elements for a `.count` comparison against literal\n  `N`, bounded, for input and stored lists.\n- The re-pinned adversary cases flip to asserting both scenarios.\n- The misleading \"drop it from the command's input\" hint is not printed for a count guard.\n","status":"draft","title":"A count guard above one gets synthesized scenarios"},"lifecycle_state":"draft"},"title":"A count guard above one gets synthesized scenarios"},"kind":"create","recording":{"actor":"human:timo","causation":"cmd-child-13b6637487240e51638cb455a44f5267f046801032250f66bc13757a2faf94b5","correlation":"protocol-artifact-new","record_id":"aep.entity:ent-22f5dc31318fb4f95e98f11d288053fe@1#0~2436510862671382","recorded_at":"2026-09-26T00:11:18Z"},"subject":["story","ent-22f5dc31318fb4f95e98f11d288053fe"]}]